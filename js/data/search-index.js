// Generated by tools/build-search-index.js. Do not edit by hand.
window.SEARCH_INDEX = {"python/environment":"Before writing any Python, get comfortable with how Python code actually runs and how dependencies get isolated per project. This is the Python-world equivalent of understanding your JDK, classpath, and Maven/Gradle setup. Java: JVM + classpath + Maven/Gradle dependency resolution. Python: the interpreter directly executes .py files; dependencies are just installed packages sitting on a path. Java: a build tool assembles dependencies into/alongside a .jar. Python: a virtual environment (venv) is a private, isolated copy of the interpreter's package directory per project, so installing package X for project A never collides with project B needing a different version of X. Java: pom.xml / build.gradle declare dependencies, resolved automatically. Python: a requirements.txt file declares them, installed explicitly via pip. Java: one canonical entrypoint (`public static void main`) run via `java -jar` or an IDE config. Python has no enforced entrypoint — there are several legitimate ways to run the same code, covered below. A venv keeps this project's installed packages separate from every other project and from your system Python. Create one per project, always. requirements.txt The Python equivalent of a Maven pom.xml's block — plain text, one package (optionally pinned to a version) per line. requirements.txt is the simplest, most universal dependency manifest in Python — every tool and CI system understands it. Pinning versions (==) gives reproducible installs, like a Maven lock file; pytest with no version means \"latest\" the first time it's installed. `pip freeze` is how you generate the file from a working environment rather than writing it by hand. The five ways to run Python code Unlike Java's single `java -jar app.jar` convention, Python code can be executed severa   What is a virtual environment and why would you use one? A virtual environment is an isolated directory containing its own Python interpreter link and its own site-packages . Each project installs its dependencies there, so project A can use requests 2.25 while project B uses 2.31 without conflict, and nothing is installed into the system Python. It also makes environments reproducible: you can delete and rebuild it from a dependency file. How do you reproduce the same environment on another machine or in CI? Record dependencies in a file and install from it: pip freeze requirements.txt , then pip install -r requirements.txt in a fresh venv. Pin exact versions ( == ) for reproducibility. For transitive dependencies, use a lock file (pip-tools, Poetry, or uv) so builds are deterministic rather than \"latest at the time\". What is the difference between python app.py and python -m package.module ? Running a file puts that file's directory on sys.path and treats it as a standalone script. -m runs a module by its import path with the package context intact, so relative imports ( from .models import X ) work. Once code is organised into packages, prefer -m . As a Java developer, what would you compare pip , requirements.txt and a venv to? Roughly: pip is Maven/Gradle's dependency downloader, requirements.txt is the pom.xml / build.gradle dependency list (without a build lifecycle), and a venv is a per-project classpath and JDK-like sandbox. Unlike Java, Python installs are global by create virtual environment named venv current project folder python3 activate macos linux source windows powershell scripts your shell prompt shows installs here system wide leave deactivate requirements requests flask pytest install everything listed inside active snapshot exactly what currently installed regenerate freeze script directly most common like running java file main interactive repl jshell quick experiments module import path runs __main__ __name__ block resolves imports correctly p","python/syntax-variables":"Python's syntax removes most of Java's ceremony — no type declarations, no braces, no semicolons — while quietly changing the rules underneath: everything is an object, types are dynamic but strict, and mutability lives on the object, not the variable name. Java: int x = 5; declares a typed variable. Python: x = 5 — x is just a name bound to an int object; the same name can be rebound to any type later. Java: blocks delimited by { } ; statements end with ; . Python: blocks delimited by indentation (4 spaces is convention); no semicolons needed. Java: static typing, checked at compile time. Python: dynamic typing, checked at runtime — but still strongly typed. No implicit \"5\" + 5 coercion like JavaScript; that raises a TypeError. Java: primitives (int, boolean) are distinct from wrapper objects (Integer, Boolean). Python: everything, including integers and booleans, is an object. Java: a variable is a typed container. Python: a name is just a label pointing at an object; mutability is a property of the object (list is mutable, tuple/str/int are immutable), not of the variable. Assignment binds a name to an object — it never declares a type. Dynamic typing, but still strong typing Show what's legal to rebind versus what Python still refuses to silently coerce. Java catches type errors at compile time; Python catches them at runtime, so the same category of mistake surfaces as an exception during execution instead of a build failure. Crucially, 'dynamically typed' does not mean 'loosely typed' — Python still refuses to silently coerce a string and an int the way JavaScript's + would. Mutability lives on the object, not the name Show what happens when two names point at the same mutable object versus a real copy. Assignment in Python never copies — it binds a name to an exi   Is Python statically or dynamically typed? Strongly or weakly? Dynamically and strongly typed. Types belong to objects and are checked at runtime, so a name can be rebound to a different type. It is strong because there is no implicit coercion between unrelated types: \"5\" + 5 raises TypeError . Optional type hints add static checking through tools like mypy, but the interpreter does not enforce them. What happens when you write b = a where a is a list? Nothing is copied. b becomes a second name bound to the same list object, so mutating through either name is visible via both. Make a shallow copy with a[:] , list(a) or a.copy() , and use copy.deepcopy when nested mutable objects must also be duplicated. Explain the difference between == and is . == compares values (it calls __eq__ ), like Java's .equals() . is compares identity, whether two names refer to the same object, like Java's == on references. Use is for singletons such as None ( x is None ) and == for everything else. What are mutable and immutable types, and why does it matter? Immutable objects ( int , str , tuple , frozenset , bytes ) cannot change after creation; mutable ones ( list , dict , set ) can. It matters for hashing (only hashable, effectively immutable objects can be dict keys or set members), for aliasing bugs, for safe sharing across threads, and for default arguments. string legal rebound retyped multiple assignment swap temp variable needed print type class count zero python would compile error java total this raises typeerror only concatenate explicit conversion required same discipline points list object append changed because slicing makes real independent copy unaffected different","python/numbers-strings":"Python numbers and strings behave differently enough from Java's to trip up experienced developers: integers have arbitrary precision with no overflow, strings are immutable sequences with rich built-in slicing, and f-strings replace String.format and manual concatenation entirely. Java: int overflows silently at 2^31-1 (or use long / BigInteger). Python: int has arbitrary precision automatically — no overflow, ever. Java: String is immutable; concatenating with + in a loop is a classic anti-pattern, fixed with StringBuilder. Python: str is also immutable, but f-strings and .join() are the idiomatic tools, not a builder class. Java: String.format(\"%s is %d\", name, age) or manual + concatenation. Python: f-strings — f\"{name} is {age}\" . Java: substring(start, end) via a method call. Python: slicing syntax s[start:end] is built into the language and works on any sequence (str, list, tuple). Java: char is a distinct primitive type. Python: there is no char type — a single character is just a string of length 1. f-strings for interpolation, slicing for substrings — the two tools you'll reach for constantly. Arbitrary precision integers Show that Python ints never overflow, contrasted with Java's fixed-width int/long. Java's int maxes out at 2^31 - 1 and long at 2^63 - 1; beyond that you need java.math.BigInteger explicitly. Python's int automatically grows to hold arbitrarily large values with no special type or import — the tradeoff is a small per-operation performance cost compared to a fixed-width machine int. String building the idiomatic way Show why += concatenation in a loop is discouraged, and the join() alternative. Because str is immutable, every += creates a brand new string object and copies the old contents into it — exactly the same trap as Java string concate   Can a Python integer overflow? What about floats? Integers have arbitrary precision, so they grow as needed and never overflow (limited only by memory), unlike Java's fixed-width int / long . Floats are IEEE 754 doubles, so they still have rounding error ( 0.1 + 0.2 != 0.3 ). Use decimal.Decimal or integer cents for money. Why is building a string with += in a loop discouraged? Strings are immutable, so each concatenation can allocate and copy the whole string so far, making the loop O(n&sup2;) in general (CPython has an optimisation for some cases, but it is not guaranteed). Collect the pieces in a list and use \"\".join(parts) , which computes the final size once and copies each piece once. What is the difference between str and bytes ? str is a sequence of Unicode code points; bytes is a sequence of raw 8-bit values. You convert with s.encode(\"utf-8\") and b.decode(\"utf-8\") . Bugs appear when the encoding is assumed rather than specified, which is common with legacy files (cp1252) and network data, so always be explicit. Compare f-strings, str.format() and % formatting. f-strings (3.6+) evaluate expressions inline ( f\"{name} is {age}\" ) and are the most readable and usually the fastest. .format() is older but useful when the template is separate from the data. % formatting is legacy. Note that f-strings evaluate immediately, so do not use them for logging calls that should be lazy; pass arguments to the logger instead. name print years string variables interpolated directly debug shortcut prints hello world slice start excluded negative indices count from dlrow olleh reversed step type still just class separate biginteger needed words forward deployed engineer works rebuilds object every iteration result idiomatic join single pass","python/lists-tuples":"Python's list is Java's ArrayList with a far friendlier API baked directly into the language. tuple has no direct Java equivalent — it's an immutable, fixed-size sequence used constantly for multiple return values and as dict keys. Java: ArrayList Integer nums = new ArrayList (); requires a type parameter and boxing. Python: nums = [1, 2, 3] — a list literal, no type declared, can even hold mixed types. Java: arrays ( int[] ) and List are separate concepts with different syntax. Python: list is the one general-purpose sequence type used everywhere. Java: returning multiple values requires a wrapper class or an array. Python: return a, b returns a tuple — unpack directly at the call site. Java: List methods are .add() , .get(i) , .remove(i) . Python: .append() , indexing with [i] , and .remove(value) — by value, not index ( del list[i] removes by index). Java: Collections.unmodifiableList() gives an immutable view of a list. Python: tuple is immutable by construction — (1, 2, 3) , a genuinely different type, not a wrapped list. The list methods you'll reach for constantly. Tuples for multiple returns and unpacking Return more than one value from a function without a wrapper class. In Java, this would require a small record/wrapper class (or returning an int[]) just to carry two values out of a method. Python's tuple makes 'return several values' a zero-ceremony, first-class pattern — the function packs them, the caller unpacks them by position. Slicing, and the shared-reference gotcha List slicing beyond substrings, plus a classic bug when multiplying a list of mutable elements. [[0]*3] * 3 creates one inner list, then repeats the reference to it three times — the same b = a mutability trap from the syntax page. Mutating one row mutates all of them since they're literall   What are the differences between a list and a tuple, and when would you choose a tuple? Lists are mutable and typically homogeneous collections; tuples are immutable and often used as fixed-size records. Choose a tuple for values that should not change, for multiple return values, and when you need a hashable key (for example (x, y) as a dict key). Tuples are also slightly smaller and faster to create. What is the time complexity of common list operations? Index and append / pop() at the end are O(1) (append is amortised). insert(0, x) , pop(0) and x in list are O(n). Sorting is O(n log n). For queue behaviour use collections.deque , which gives O(1) at both ends. What is wrong with grid = [[0] * 3] * 3 ? The outer multiplication repeats a reference to one inner list three times, so all rows are the same object and grid[0][0] = 1 changes every row. Build it with a comprehension so each row is a fresh list: [[0] * 3 for _ in range(3)] . How is a Python list implemented, and why is append amortised O(1)? It is a dynamic array of object pointers. When it fills, CPython over-allocates (grows by a proportional factor) so that resizes happen rarely; the occasional O(n) copy is spread over many appends, giving O(1) amortised cost. nums append insert index remove return last element sort place sorted_copy sorted list original untouched min_max packs into tuple automatically unpacked directly variables print every other classic gotcha multiplying lists shares inner grid wrong rows same object changed range correct fresh each iteration","python/dicts-sets":"dict is Java's HashMap and set is Java's HashSet, but both come with literal syntax and comprehensions built directly into the language, making them far more idiomatic to construct and query on the fly. Java: Map String, Integer m = new HashMap (); requires explicit types. Python: m = {\"a\": 1, \"b\": 2} — a dict literal. Java: m.get(key) returns null if missing; .getOrDefault(key, default) for a fallback. Python: m[key] raises KeyError if missing; m.get(key, default) mirrors getOrDefault; dict also has a first-class in operator. Java: HashSet String s = new HashSet (); . Python: s = {\"a\", \"b\", \"c\"} — a set literal (note: {} alone is an empty dict, not an empty set). Java: iterating a HashMap via .entrySet() or .keySet() . Python: for key, value in m.items(): — direct unpacking, no wrapper Entry objects. Java 8+: Map.computeIfAbsent / merge for default-on-missing patterns. Python: collections.defaultdict does this structurally, and dict.setdefault() is the inline equivalent. The dict/set moves that show up in nearly every DSA pattern. dict as a frequency counter Count character frequency in a string, the manual way and the idiomatic way. .get(key, 0) is the standard way to avoid a KeyError when a key might not exist yet — Python's version of Java's getOrDefault. collections.Counter (covered fully on the stdlib page) is purpose-built for exactly this pattern and adds convenience methods like .most_common() on top. Dict comprehension + set algebra Build a dict from two lists, and use real set operators for comparisons. zip pairs up two iterables element-wise, and the dict comprehension builds the mapping in one expression — no explicit loop needed, unlike Java's typical for-loop-plus-put() construction. Python's set supports mathematical set operators directly (&, |, -), whe   How does a dict work and what is its lookup complexity? It is a hash table: the key's hash selects a slot, with collisions resolved by probing. Lookup, insert and delete are O(1) on average and O(n) in the pathological worst case. Since Python 3.7 dicts also preserve insertion order as a language guarantee. Which objects can be dictionary keys or set members? Hashable ones: objects with a stable __hash__ consistent with __eq__ . Immutable built-ins (str, int, tuple of hashables, frozenset) qualify; lists, dicts and sets do not because their contents can change and would corrupt the hash table. What are the ways to handle a missing key? d.get(key, default) for a read with a fallback key in d before access (LBYL) try/except KeyError (EAFP) d.setdefault(key, default) to insert if absent collections.defaultdict to auto-create values How would you remove duplicates from a list while preserving order? list(dict.fromkeys(items)) : dict keys are unique and ordered by insertion. list(set(items)) also dedupes but loses order. For unhashable items you need a different approach, such as comparing with a list of seen values (O(n&sup2;)) or a key function. freq classic count occurrences idiom value items print seen average membership check from collections import counter mississippi manual version idiomatic does exactly this call freq2 most_common keys values lookup dict comprehension intersection union difference","python/control-functions":"Python's control flow trades Java's C-style for loop and switch statement for for...in iteration and, since 3.10, structural pattern matching. Functions trade method overloading for default arguments and *args/**kwargs, with optional type hints bridging back toward Java-like signatures. Java: for (int i = 0; i . Python: for i in range(n): — range() generates the sequence, no manual counter. Java: for (String s : list) is the for-each form. Python: for s in list: is the ONLY for-loop form — there's no C-style counter loop without range(). Java: ternary is cond ? a : b . Python: a if cond else b — the condition sits in the middle. Java: switch statement/expression matches on a value. Python 3.10+: match does structural pattern matching — shapes, not just values. Java: method overloading — multiple methods, same name, different signatures. Python: no overloading — one function per name; use default arguments or *args/**kwargs for flexible signatures instead. Java: optional parameters require overloads. Python: def greet(name, greeting=\"Hello\"): — default values live directly in the signature. Default arguments, keyword-only arguments, and the *args/**kwargs escape hatches. range, ternary, and match The three most common control-flow substitutions coming from Java. range(5) produces 0 through 4, filling the role of Java's counter loop without a manual index variable. Python's ternary reads 'value if condition else other value' rather than Java's 'condition ? value : other'. match (3.10+) can do far more than switch — matching multiple values with |, unpacking structures, and guards — but a simple case behaves like a switch statement. No overloading — defaults and *args instead Replicate what Java would need three overloaded methods for, using one Python function. Python has   What do *args and **kwargs do? *args collects extra positional arguments into a tuple; **kwargs collects extra keyword arguments into a dict. At a call site the same operators unpack an iterable or mapping into arguments. They let one function accept flexible signatures, which is how Python compensates for having no method overloading. What is the mutable default argument problem? Default values are evaluated once, when the function is defined, so a mutable default such as def f(x, cache=[]) is shared across every call. Use None as the default and create the object inside: def f(x, cache=None): if cache is None: cache = [] Is Python pass-by-value or pass-by-reference? Neither exactly: it is \"pass by object reference\" (call by assignment). The parameter name is bound to the same object the caller passed. Mutating that object (for example appending to a list) is visible to the caller; rebinding the parameter to a new object is not. Python has no method overloading. How do you handle multiple call shapes? Use default arguments, keyword arguments, *args/**kwargs , or functools.singledispatch to dispatch on the type of the first argument. Defining two functions with the same name simply replaces the first. For alternative constructors, use @classmethod factories. greet name greeting hello loud false bare forces everything after passed keyword only return upper else print true total args kwargs tuple positional dict range like status label error ternary condition middle match python structural case problem unknown java would need list integer numbers unpack into","python/comprehensions":"List/dict/set comprehensions replace the explicit for-loop-plus-append pattern with a single, more declarative expression — the Python idiom Java developers most need to consciously adopt. Java's closest analog, Streams, is more verbose and arrived over a decade later. Java: List Integer squares = new ArrayList (); for (int n : nums) squares.add(n*n); . Python: squares = [n*n for n in nums] — one line, no explicit accumulator. Java 8+: nums.stream().map(n - n*n).collect(Collectors.toList()) is the closest analog — more ceremony, and a pipeline mental model rather than Python's direct 'for x in y' reading. Java: filtering requires .stream().filter(...) . Python: an if clause appended directly to the comprehension. Java: a Cartesian product needs nested streams or manual loops. Python: comprehensions support multiple for clauses in one expression. Comprehensions exist for all three main collection types — [...] list, {...} set, {k: v ...} dict — same syntax shape, different brackets. The three comprehension shapes, plus filtering and nesting. Comprehension vs. explicit loop Transform a list of names to their uppercase initials, only for names longer than 2 characters. Both versions do exactly the same work with the same O(n) time complexity — the comprehension isn't faster in any meaningful way, it's a more direct expression of 'build a new list by transforming and filtering an existing one.' Once the pattern clicks, comprehensions read closer to the problem statement than the accumulator-loop version does. Nested comprehension: flatten and transpose Flatten a 2D grid into a single list, and build a transposed version. Multiple for clauses in one comprehension read left to right in the same order as the equivalent nested loops — 'for row in grid for num in row' means 'for   What is the difference between a list comprehension and a generator expression? [x*x for x in data] builds the whole list in memory immediately. (x*x for x in data) creates a lazy generator that yields one value at a time and can be consumed once. Use the generator form for large or streaming data and when feeding sum() , any() or max() . Are comprehensions faster than for-loops? Slightly, by a constant factor, because they avoid repeated method lookups for append . They do not change the complexity class. The real benefit is that they express \"build a new collection from another\" declaratively. Prefer clarity over micro-optimisation. Flatten a list of lists with a comprehension. In what order do the for clauses read? flat = [x for row in grid for x in row] The clauses read in the same order as nested loops: outer loop first, inner loop second. When should you avoid a comprehension? When it has side effects (printing, mutating external state), when the logic needs several statements, or when it nests three or more levels and becomes hard to scan. In those cases a plain loop is more readable and easier to debug. nums squares list comprehension evens with filter pairs nested cartesian square_set square_map dict names grace alan explicit loop version what java developer reaches first result name append upper same logic expression print grid flat flatten outer inner second transposed range","python/oop":"Python classes cover the same ground as Java's — encapsulation, inheritance, polymorphism — but with far less enforcement by the language: no access modifiers, no interfaces (duck typing / ABCs instead), and behavior customized via dunder methods instead of overriding well-known base-class methods. Java: private, protected, public are enforced by the compiler. Python: no enforced access control — a single leading underscore ( _name ) signals 'internal, don't touch' by convention only; double underscore ( __name ) triggers name-mangling, a weaker form of privacy, not true protection. Java: this is implicit inside instance methods. Python: self is the explicit first parameter of every instance method, written every time. Java: constructors are named after the class. Python: __init__ is the initializer (technically __new__ is the true constructor, rarely touched directly). Java: interfaces declare a contract, implemented explicitly with implements . Python: duck typing — if an object has the right methods, it works, no declared interface required; abc.ABC exists for when you do want an enforced contract. Java: single inheritance for classes. Python: multiple inheritance is directly supported, resolved via C3 linearization (the Method Resolution Order). Java: toString() , equals() , hashCode() are methods you override. Python: __str__ , __eq__ , __hash__ are 'dunder' methods — same idea, and there are many more ( __len__ , __iter__ , __add__ ) that hook into built-in syntax. A basic class — attributes assigned in __init__, no field declarations needed. Dunder methods hook into built-in syntax Make a custom class work with len(), ==, and print() the way built-in types do. In Java, you'd override .equals() and add a .size() method, then call them explicitly: t1.equals(t2), t1   What is self , and how do __init__ and __new__ differ? self is the explicit first parameter that refers to the instance (Java's implicit this ). __new__ creates the object and returns it; __init__ then initialises it. You rarely override __new__ , mainly for singletons or immutable subclasses. How does Python do encapsulation without private ? By convention: a leading underscore ( _x ) means \"internal\". A double underscore ( __x ) triggers name mangling to _Class__x , which avoids accidental subclass clashes but is not real protection. Use @property to expose controlled access while keeping the attribute syntax. What is the method resolution order (MRO)? The order in which Python searches base classes for an attribute, computed with C3 linearisation, which keeps a consistent, monotonic order under multiple inheritance. Inspect it with Class.__mro__ . super() follows the MRO, not just the direct parent, which is why cooperative multiple inheritance works. Explain instance methods, @classmethod and @staticmethod . Instance method : receives self , works on one object. @classmethod : receives cls ; used for alternative constructors and works correctly with subclasses. @staticmethod : receives nothing; just a function namespaced inside the class. Why does defining __eq__ make instances unhashable, and how do you fix it? Python sets __hash__ = None when you define __eq__ without __hash__ , because equal objects must hash equally. If instances are effectively immutable, define __ha class employee __init__ self name salary instance attributes field declarations needed raise_salary __str__ like java tostring return print calls automatically team members __len__ powers __eq__ other __repr__ fallback string repr shown repl collections alan true identity speak woof duck quack make_it_speak animal type check interface just","python/exceptions":"try/except covers the same ground as try/catch, but Python drops checked exceptions entirely and leans on a different philosophy — EAFP (easier to ask forgiveness than permission) over Java's more defensive LBYL (look before you leap) style. Java: checked exceptions must be declared (throws) or caught — the compiler enforces handling. Python: all exceptions are unchecked — nothing forces you to catch anything, ever. Java: try/catch/finally. Python: try/except/else/finally — the extra else clause runs only if no exception occurred, keeping the 'happy path' separate from error handling. Java: catch (IOException e) catches one type (or a union with | since Java 7). Python: except (TypeError, ValueError) as e: catches multiple types via a tuple, or stack multiple except blocks. Java: idiomatic style is LBYL — check preconditions before acting. Python: idiomatic style is EAFP — just try the operation and catch the exception if it fails, often faster and more readable for the common case. Java: custom exceptions extend Exception or RuntimeException, a meaningful choice (checked vs unchecked). Python: custom exceptions subclass Exception — no checked/unchecked distinction to choose between. The full try/except/else/finally shape. EAFP vs. LBYL Look up a key in a dict, the defensive way and the idiomatic Python way. LBYL checks preconditions before acting, mirroring Java's defensive style — but it does the lookup twice in the worst case (once to check, once to use) and has a subtle race-condition risk in multithreaded code between the check and the use. EAFP just attempts the operation and handles failure, which is both the more idiomatic Python style and typically faster when the exception is rare. For this exact case, .get() sidesteps the whole question — but EAFP is the patt   What are EAFP and LBYL, and which does Python prefer? LBYL (\"look before you leap\") checks preconditions first; EAFP (\"easier to ask forgiveness than permission\") tries the operation and handles the exception. Python favours EAFP: it avoids races between check and use, avoids double lookups, and is usually cleaner when failure is the rare case. What do else and finally do in try/except, and does finally run after return ? else runs only if the try block raised nothing, keeping the happy path out of the guarded block. finally always runs, including when the try block returns, breaks, or raises, which makes it the place for cleanup (though with is usually better). Why is a bare except: a problem, and what is exception chaining? A bare except also catches KeyboardInterrupt and SystemExit and hides real bugs; catch specific types. Chaining preserves the cause when you translate errors: raise ServiceError(\"sync failed\") from exc keeps the original traceback attached, which is important when wrapping vendor exceptions. Java has checked exceptions. What is the Python equivalent? There is none: all exceptions are unchecked and nothing forces callers to handle them. The trade-off is that you must document what a function can raise and be deliberate about where errors are caught, typically at boundaries where you can act on them. result divisor except zerodivisionerror print divide zero typeerror valueerror input else only runs exception raised finally always cleanup whether occurred lbyl java style defensive check first value eafp python idiomatic just handle failure keyerror usually simplest this specific case class insufficientfundserror __init__ self balance amount super cannot cover withdrawal withdraw raise return error custom exceptions carry extra data","python/functional":"Python treats functions as first-class objects — they can be passed around, returned, and wrapped, which is what makes decorators possible. Java only got closures (lambdas) in version 8, and still has no direct decorator equivalent. Java: methods aren't values — you need a functional interface (Runnable, Function T,R ) to pass behavior around, or a lambda since Java 8. Python: functions are objects from day one — assign them to variables, put them in lists, pass them as arguments, no wrapper interface needed. Java 8+: lambda syntax is (a, b) - a + b , single-expression only, and can only capture effectively final variables. Python: lambda a, b: a + b — same idea, also single-expression only, but closures can read any enclosing variable (and reassign with nonlocal). Java: no direct decorator equivalent — annotations are metadata read by a framework, not functions that wrap other functions. Python: @decorator syntax literally wraps a function in another function at definition time — closer to a manual proxy/wrapper pattern than to an annotation. Java: Function.andThen() / .compose() for chaining. Python: decorators, plus functools.reduce, achieve composition and pipelines directly. Java: closures over local variables must be effectively final. Python: closures can read AND, with the nonlocal keyword, reassign an enclosing function's variables. A lambda, and the shape of a decorator factory. Closures Build a counter function using a closure, contrasted with Java's effectively-final restriction. increment is a closure — it 'closes over' the count variable from its enclosing scope, keeping it alive and private even after make_counter() has returned. Java's lambdas can read an enclosing local variable, but only if it's 'effectively final' (never reassigned) — Java has no equi   What is a closure and what does nonlocal do? A closure is a function that keeps access to variables from its enclosing scope after that scope has returned. Reading such variables just works; to reassign one from the inner function you must declare it nonlocal , otherwise assignment creates a new local variable. Write a decorator that logs how long a function takes. Why use functools.wraps ? import time from functools import wraps def timed(fn): @wraps(fn) def wrapper(*args, **kwargs): t0 = time.perf_counter() try: return fn(*args, **kwargs) finally: print(fn.__name__, time.perf_counter() - t0) return wrapper wraps copies __name__ , __doc__ and other metadata onto the wrapper so debugging, docs and introspection still show the original function. How do you write a decorator that takes arguments? Add one more layer: a decorator factory that receives the arguments and returns the actual decorator, which returns the wrapper. @retry(times=3) means retry(3) is evaluated first and its result is applied to the function. What is the late-binding closure gotcha? Closures capture variables, not values. fs = [lambda: i for i in range(3)] makes all three return 2 , because i is looked up when called. Bind the current value with a default argument: lambda i=i: i , or use functools.partial . lambda equivalent return print repeat decorator factory returns func wrapper args kwargs range greet prints three times make_counter count increment nonlocal without this below raises unboundlocalerror counter persists between calls private instance import time from functools wraps timer preserves name docstring introspection start perf_counter result elapsed __name__ took slow_sum _000_000","python/iterators-generators":"Python's iterator protocol underlies every for loop, and generators (via yield) let you write lazy, memory-efficient sequences without building a custom Iterator class. Context managers (with) are Python's answer to Java's try-with-resources. Java: implementing Iterable/Iterator requires a class with hasNext()/next() methods. Python: the iterator protocol is __iter__/__next__, but yield inside a function auto-generates a full iterator with zero boilerplate. Java: Stream.generate() / IntStream.iterate() approximate lazy sequences. Python: a generator function is lazy by default — values are computed one at a time, on demand, never all held in memory at once. Java: try-with-resources — try (var conn = openConnection()) { ... } — requires the resource to implement AutoCloseable. Python: with open(\"file\") as f: — requires __enter__/__exit__; with is the direct equivalent. Java: a custom AutoCloseable class implements close(). Python: a custom context manager implements __enter__ and __exit__, or uses @contextlib.contextmanager to write it as a generator function instead. Java: a for-each loop works because collections implement Iterable directly. Python: a for loop works over ANYTHING implementing __iter__ — a generator, a file object (line by line), or a custom class — one uniform mechanism. A generator function, and a context manager built from one. Generator vs. list — memory behavior Contrast building a full list in memory with lazily generating values. squares_list must fully materialize every element before returning — for 10 million elements, that's real memory pressure. squares_gen is a generator function: calling it doesn't run the body at all, it returns a generator object that computes and yields exactly one value per next() call (which a for loop or sum() drives   Explain iterable, iterator and generator. An iterable can produce an iterator ( __iter__ ), such as a list. An iterator has __next__ and raises StopIteration when exhausted. A generator is an iterator created by a function using yield (or a generator expression); Python saves its local state between values. All generators are iterators, but not all iterators are generators. When would you use a generator instead of returning a list? When the sequence is large, unbounded, or expensive, and you only need to process items one at a time: reading a big file line by line, paginating an API, or streaming records. It keeps memory O(1), but a generator can be consumed only once and does not support indexing. How do you implement a context manager, and what does returning True from __exit__ do? Either a class with __enter__ / __exit__ , or a generator function decorated with @contextlib.contextmanager (code before yield is setup, after is cleanup). __exit__ runs even if the block raised; returning True suppresses that exception, so do it only deliberately. What does yield from do? It delegates to a sub-iterator, yielding each of its values and propagating send / throw /return values. yield from items is the tidy replacement for for x in items: yield x , and is what made composing generators (and early coroutines) practical. countdown while yield pauses here resumes next call computed time upfront print from contextlib import contextmanager timer_block start perf_counter code inside with block runs took range _000_000 squares_list return builds entire list memory squares_gen produces value demand allocates million ints once almost nothing until iterate total never holds more than class managedresource __enter__ self acquiring resource __exit__ exc_type exc_value traceback releasing false means suppress exception usi","python/modules-packages":"Python's import system replaces Java's package/classpath model: files are modules, folders (with an __init__.py) are packages, and pip plus a virtual environment replace Maven/Gradle's dependency resolution. Java: a package declaration must match its folder path exactly; the classpath tells the JVM where to find classes. Python: a file is automatically a module named after its filename; a folder becomes a package once it contains __init__.py. Java: import com.company.util.StringHelper; imports one class from a fully-qualified path. Python: import package.module imports a module; from package.module import name imports a specific name directly into scope. Java: Maven/Gradle resolve dependencies from Maven Central via pom.xml/build.gradle. Python: pip installs from PyPI using requirements.txt — conceptually the same job, a much simpler format. Java: a .jar bundles compiled classes for distribution. Python: a package is published to PyPI as a source/wheel distribution, installed straight into a venv's site-packages. Java: no concept of 'running a package directly' — you call a class's main(). Python: if __name__ == \"__main__\": is the idiom for 'only run this when the file is executed directly, not when it's imported.' Module imports, package imports, and the main-guard idiom. The __name__ == \"__main__\" idiom Show why this guard matters, contrasted with Java always needing an explicit main() call. When calculator.py is run directly (python calculator.py), Python sets the special variable __name__ to \"__main__\", so main() executes. When another file does import calculator instead, __name__ is set to \"calculator\", so the guard is False and main() does NOT run automatically — only the function/class definitions become available. Java has no equivalent ambiguity: a class either   What does if __name__ == \"__main__\": do? When a file is run directly, __name__ is \"__main__\" ; when it is imported, it is the module name. The guard lets one file be both an importable library and a runnable script, without running the script code on import. How does the import system find and cache modules? It checks sys.modules first (imports are cached, so a module body runs once per process), then searches sys.path (script directory, PYTHONPATH , site-packages). This caching is also why module-level objects act like singletons. What are circular imports and how do you fix them? Module A imports B while B imports A, so one sees a half-initialised module. Fixes: move the shared code to a third module, import inside the function that needs it, or restructure so dependencies point one way. Frequent circular imports usually signal a design smell. What is the difference between absolute and relative imports? Absolute imports name the full path from the top-level package ( from myapp.models import User ); relative imports are anchored to the current package ( from .models import User ). Relative imports only work inside a package, so running the file directly breaks them; use python -m . import math standard library module from collections counter name directly my_package __init__ helpers local package function __name__ __main__ only runs when this file executed print running script calculator return main project structure myapp models services class user self relative same create_user outside","python/stdlib-dsa":"A handful of standard library tools show up in nearly every DSA pattern in this roadmap. Knowing them cold is the difference between writing ten lines of boilerplate and one import. Java: ArrayDeque for a double-ended queue. Python: collections.deque — O(1) appends/pops from both ends, unlike list's O(n) pop(0). Java: manually check-then-increment a HashMap K,Integer for counting. Python: collections.Counter — purpose-built, with .most_common() included. Java: map.computeIfAbsent(key, k - new ArrayList ()) for the 'group into buckets' pattern. Python: collections.defaultdict(list) — the default factory eliminates the check entirely. Java: PriorityQueue Integer for a min-heap. Python: the heapq module — functions operating on a plain list, not a class (min-heap only; negate values for a max-heap). Java: Stream utilities and manual nested loops for combinations/permutations. Python: itertools — permutations(), combinations(), product(), chain() as ready-made generators. Java: no direct built-in memoization annotation. Python: functools.lru_cache — a decorator that adds memoization to any function in one line. The imports you'll reach for constantly once you start the DSA roadmap. Counter + defaultdict in one pass Group words by length using defaultdict, then find the most common length with Counter. defaultdict(list) removes the classic 'if key not in d: d[key] = []' boilerplate — accessing a missing key auto-creates the default value (here, an empty list) instead of raising KeyError. Counter accepts any iterable, including a generator expression, and tallies it directly; .most_common(n) returns the n most frequent items sorted by count, descending. heapq for a min-heap, lru_cache for memoization Find the 3 smallest numbers with heapq, and memoize a recursive Fibonacci wi   Why use collections.deque instead of a list for a queue? list.pop(0) is O(n) because every element shifts. deque supports O(1) append , appendleft , pop and popleft . The trade-off is O(n) random indexing in the middle. When would you use Counter and defaultdict ? Counter(iterable) tallies frequencies and offers .most_common(k) . defaultdict(list) (or int , set ) removes the \"if key not in d\" boilerplate when grouping or accumulating. Beware that merely reading a missing key inserts it. Python's heapq is a min-heap. How do you get a max-heap and what are the complexities? Push negated values (or tuples with a negated priority) and negate on pop. heappush / heappop are O(log n), heapify is O(n), and nlargest(k, xs) is roughly O(n log k). How does functools.lru_cache work and what are its limits? It memoises results in a dict keyed by the call arguments, with least-recently-used eviction if maxsize is set. Arguments must be hashable, and the cache lives for the process lifetime unless cache_clear() is called. It turns exponential recursive Fibonacci into O(n). from collections import counter defaultdict deque heapq itertools permutations combinations functools lru_cache freq mississippi groups list missing keys auto create empty appendleft popleft heappush heap operations plain words bird fish by_length append need check exists first print dict length_counts most_common length appears times nums smallest_three nsmallest maxsize none return fast each computed exactly once then cached","python/concurrency-memory":"Python's concurrency model has a fundamental difference from Java's that surprises every Java developer: the Global Interpreter Lock (GIL) means only one thread executes Python bytecode at a time, changing when threads, processes, and async are each the right tool. Memory management (reference counting + cyclic GC) also differs from the JVM's generational GC. Java: threads run truly in parallel across CPU cores (subject to synchronization). Python: CPython's Global Interpreter Lock (GIL) means only ONE thread executes Python bytecode at a time, even on a multi-core machine — threading gives concurrency, not parallelism, for CPU-bound code. Java: threads handle both I/O-bound and CPU-bound parallel work. Python: threading for I/O-bound work (the GIL releases during I/O waits, so threads genuinely help), multiprocessing for CPU-bound work (separate processes, separate GILs, true parallelism), and asyncio for high-volume I/O-bound work without thread overhead. Java: the JVM uses generational garbage collection; objects usually aren't reclaimed the instant they're unreachable. Python (CPython): primarily reference counting — an object is freed the moment its reference count hits zero, plus a periodic cyclic GC to catch reference cycles counting alone can't resolve. Java: == on objects compares references by default ( .equals() for value equality). Python: == compares values by default (delegating to __eq__); is compares identity — the operators are effectively swapped from Java muscle memory. Java: small integers aren't cached identically. Python (CPython detail): small integers (-5 to 256) and some short strings are cached/interned, so is can appear to work for equality by accident on small values — a trap, not a guarantee. Three concurrency tools, three different jobs. Th   What is the GIL, and when do threads still help? The Global Interpreter Lock is a mutex in CPython that lets only one thread execute Python bytecode at a time, which simplifies memory management but prevents CPU-bound threads from running in parallel. It is released during blocking I/O, so threads help I/O-bound work (network, disk). CPython 3.13 added an experimental free-threaded build, so check the current status for your version. When do you choose threading, multiprocessing or asyncio? threading : moderate I/O-bound concurrency, or blocking libraries. multiprocessing (or ProcessPoolExecutor ): CPU-bound work; separate processes have separate GILs, at the cost of pickling and memory. asyncio : very many concurrent I/O tasks on one thread with cooperative await ; needs async-aware libraries. How does CPython manage memory? Primarily reference counting: an object is freed the moment its count reaches zero. A cyclic garbage collector periodically finds and frees reference cycles that counting alone cannot. Weak references ( weakref ) let you refer to an object without keeping it alive. Why can x is y be True for 200 but False for 1000 ? CPython caches small integers (-5 to 256) as singletons, and may intern some strings. That is an implementation detail, not a language guarantee, so it makes is look like value equality by accident. Always use == for values. import threading multiprocessing asyncio good bound work network calls file limits parallelism thread target fetch_url args separate processes gils true process cpu_heavy_task data high volume single cooperative multitasking async fetch await releases control during waits needed task time cpu_bound count range return start perf_counter threads _000_000 join print threaded running four times sequentially takes roughly same serializes python bytecode execution across compares values like java equa","dsa/complexity":"Becoming FDE / DSA / Big O: Time Space Complexity Foundations Big O: Time Space Complexity Every pattern in this roadmap gets judged the same way: how does its runtime and memory usage grow as the input grows? Big-O notation is the language for answering that. Get this down cold before the patterns — every \"Time / Space\" tag you'll see from here on assumes you can read and derive it yourself. What Big-O actually measures Big-O describes the upper bound on an algorithm's growth rate as input size (n) approaches infinity — not the exact runtime in seconds, and not the runtime on any one specific input. It answers: \"if I double the input size, roughly how much more work does this do?\" Why constants and lower-order terms get dropped An algorithm that does exactly 3n + 5 operations is still O(n). As n grows large, the 3 and the +5 become irrelevant next to the fact that work scales linearly with n — that shape is what Big-O captures. This is also why O(2n) and O(n) are considered the same complexity class, even though one is literally twice as slow in practice. Common complexity classes Ordered best to worst. Everything in this roadmap lives somewhere on this ladder. Notation Name Typical operation O(1) Constant Array index access, hashmap get/set O(log n) Logarithmic Binary search, balanced BST insert/lookup O(n) Linear Single pass over the input O(n log n) Linearithmic Efficient sorting (merge sort, heapsort) O(n&sup2;) Quadratic Nested loop comparing pairs O(2 ) Exponential Generating every subset O(n!) Factorial Generating every permutation How to analyze time complexity from code Sequential statements: costs add, but only the dominant (largest) term survives — O(n) + O(1) simplifies to O(n). Loops: multiply the cost of the loop body by the number of iterations. Nested l   What is the difference between Big-O, Big-Omega and Big-Theta? What do people usually mean in interviews? Big-O is an upper bound, Big-Omega a lower bound, and Big-Theta a tight bound (both). In interviews \"O(n)\" is normally used loosely for the tight worst-case bound. State the case you mean (worst, average, best) and be ready to separate time from space. What is amortised complexity? Give an example. The average cost per operation over a worst-case sequence of operations. Appending to a dynamic array is O(1) amortised: most appends are cheap, and the occasional O(n) resize is spread across many appends. A single append can still cost O(n). What is the time complexity of for i in range(n): j = 1 while j O(n log n). The outer loop runs n times and the inner loop doubles j each step, so it runs about log (n) times. What space does a recursive DFS use? O(h) for the call stack, where h is the recursion depth: O(log n) for a balanced tree, O(n) for a skewed tree or a path-like graph. In Python the default recursion limit (about 1000) can also be hit, so deep recursion may need an explicit stack.","dsa/playbook":"Becoming FDE / Data Structures Algorithms / Foundations / Pattern Recognition Playbook Foundations Pattern Recognition Playbook Most interview problems are a familiar technique in disguise. The skill is not memorising solutions, it is noticing the keywords, data shape and constraints that give the technique away. This page is the cheat sheet for the whole DSA topic: a process to follow, \"if you see X, think Y\" rules , two decision guides (by input shape and by what the question asks), the pattern catalog, a constraints-to-complexity table, an interactive pattern finder, a Python toolbox, and a drill to train your eye. 1. A process for any problem 2. If you see X, think Y Quick rules. Click a pattern to open its lesson. Type to filter. If you see... Think 3. Decision guide: by the shape of the input Start from what you are given. These are the patterns worth considering for each shape. Input shape Consider 4. Decision guide: by what the question asks Then look at the last sentence of the problem. What are you asked to produce? The question asks Consider 5. The pattern catalog One row per technique: the question to ask yourself, and the trigger words. Each lesson has a template, worked examples, disguised problems, a Python example, and a numbered LeetCode list. Pattern Ask yourself Trigger words Problems that need two patterns at once are in Combining Patterns . 6. Constraints tell you the target complexity Look at the input size n before you pick an approach. It rules whole families of solutions in or out. Input size Aim for Typical techniques 7. Try it: pattern finder Paste or type a problem statement. The finder highlights trigger words and suggests patterns. It is a heuristic to train your instincts, not an oracle. 8. Python toolbox These standard-library pieces show  Tips and tricks: pattern recognition, keywords, cheat sheet, pattern finder, drill. Restate the problem and ask about edge cases: empty input, duplicates, negatives, very large values. Confirm input and output types, and whether you may modify the input. Read the constraints. They tell you the complexity you are aiming for (table below). Look for keywords and structure, name the pattern out loud, and say why. Start with the brute force and state its complexity. Then say which pattern removes which cost. Write the simplest correct version, then trace it on a small example and on an edge case. State time and space complexity, and mention trade-offs and follow-ups. have I seen this before / duplicate count / frequency / most common pair that sums to a target group by / anagram first non-repeating / unique longest consecutive run range sum queries subarray sum equals k / divisible by k running total / cumulative product of array except self equal number of 0s and 1s balance / pivot index sorted array pair or triplet with a given sum palindrome from both ends in place with O(1) extra space remove duplicates / partition closest to a target contiguous subarray or substring longest / shortest ... such that at most k distinct or k replacements subarray of size k maximum sum of k consecutive elements permutation or anagram inside a string intervals / ranges / [start, end] pairs merge overlapping meeting rooms / schedule conflicts insert into sorted intervals minimum removals so none ov You get a problem you have not seen before. Walk me through how you choose an approach. Restate the problem and clarify edge cases (empty input, duplicates, negatives, size). Read the constraints to learn the target complexity. Look for structural keywords (sorted, contiguous, prefix, graph-like, two sequences, \"number of ways\") and name a candidate pattern out loud. State the brute force and its cost, explain which pattern removes which cost, then code the simplest correct version and test it on a small case and an edge case. The input size is n up to 10 5 . What does that tell you? Roughly 10 8 simple operations are feasible, so O(n&sup2;) (10 10 ) is too slow and you should aim for O(n log n) or O(n): sorting, heaps, binary search, hashing, sliding window, or two pointers. By contrast, n around 20 suggests O(2 n ) subsets or bitmask, n around 10 allows factorial backtracking, and n up to 10 9 suggests O(log n) or math. Python is slower than compiled languages, so leave extra margin. A problem asks for the \"minimum number of steps/minutes\" on a grid, starting from several cells at once. What do you use? Multi-source BFS: put all starting cells in the queue at distance 0 and expand level by level, so each level is one step or minute. BFS gives shortest paths only when every move costs the same; with different costs use Dijkstra, and with 0/1 costs use a deque-based 0-1 BFS. \"Longest subarray\" versus \"longest subsequence\": how does the wording change your approach? A subarray","dsa/arrays-hashing":"Arrays and hash maps/sets are the substrate of almost every coding interview problem. Hashing trades space for time, turning O(n) or O(n^2) lookups into O(1) average — the single highest-leverage trick in this whole roadmap. Need to check whether something has been seen before use a set Need to count frequencies of elements use a dict / Counter Need to find a complement (e.g. target - x) use a value index hashmap Problem mentions an array of size n and expects an O(n) or O(n log n) solution Most array/hashing problems are either one pass with a running dict, or two passes: build a map, then query it. Two Sum Given an array of integers nums and an integer target, return the indices of the two numbers that add up to target. Assume exactly one solution exists. Instead of checking every pair (O(n^2)), store each number's index as you scan. Before inserting the current number, check whether its complement already exists in the map — if so, you've found the pair in a single pass. The array is scanned once (n iterations), and each iteration does O(1) work: one hash lookup and one hash insert. The seen map can hold up to n entries by the time the loop ends, which is where the O(n) space comes from. Group Anagrams Given an array of strings, group the anagrams together. Anagrams share the same sorted character sequence, so the sorted string makes a natural hashmap key. Every original string gets bucketed under its signature in a single pass. There are n strings, and sorting each one costs O(k log k) where k is that string's length. Multiplying the per-string sort cost by n strings gives O(n * k log k) overall. Space is O(n * k) because the output holds every input character exactly once, grouped into buckets. Contains Duplicate leetcode 217 Valid Anagram leetcode 242 Two Sum leetcode 1 First Unique Character in a String leetcode 387 Group Anagrams leetcode 49 Top K Frequent Elements leetcode 347 Longest Consecutive Sequence leetcode 128 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. Do I keep needing to know 'have I seen this?', 'how many times?', or 'what is the complement?' have I seen this before / duplicate count / frequency / most common pair that sums to a target group by / anagram first non-repeating / unique longest consecutive run Given a string, return the index of the first non-repeating character. 'First non-repeating' means you need each character's frequency, then one more scan in original order. Count with a dict, then scan. Given two arrays, return their intersection, where each result element must be unique. Set membership answers 'is it in the other array?' in O(1), so turn one array into a set and de-duplicate the result. Order or sortedness is the point (use two pointers or binary search), or you need the k-th smallest (heap or quickselect). Longest Consecutive Sequence Counter turns frequency and anagram questions into one-liners. dict.fromkeys(items) de-duplicates and keeps first-seen order; defaultdict(list) groups without key checks. Any hashable value can be a key. Use a tuple for coordinates or a sorted-letters key, because lists are not hashable. Two Sum: how do you solve it in O(n), and what are the trade-offs against sorting? One pass with a hash map from value to index: for each x , check whether target - x is already stored, then store x . That is O(n) time and O(n) space. Sorting plus two pointers is O(n log n) time and O(1) extra space, but loses the original indices unless you keep them. How do you detect duplicates in an array, and what are the options? Set of seen values: O(n) time, O(n) space. Sort then compare neighbours: O(n log n) time, O(1) extra space (if sorting in place). Brute force pairs: O(n&sup2;) time, O(1) space. State the time/space trade-off and ask whether the input may be modified. Group Anagrams: what makes a good hash key, and what is the complexity? Either the sorted string (O(k log k) per word) or a 26-length count tuple (O(k) per word). With n words of length k that is O(n k log k) or O(n k) respectively, with O(n k) space for the groups. Top K Frequent Elements: describe two approaches beyond sorting everything. (1) Count with a hash map, then keep a min-heap of size k: O(n log k). (2) Bucket sort by frequency (frequencies are at most n): O(n) time and space. Quickselect on the counts averages O(n). build_frequency_map nums freq return two_sum target seen value index enumerate complement group_anagrams strs groups join sorted setdefault append list values","dsa/prefix-sum":"A prefix sum stores running totals, so the sum of any range is one subtraction. Combined with a hash map of earlier prefix values, it also answers 'how many subarrays add up to k?' in a single pass, even when the array has negative numbers. Many range-sum queries on a fixed array 'Subarray sum equals k' or 'count subarrays with a property' (works with negatives, where a sliding window does not) 'Product of all elements except self', running totals, pivot/balance problems A property that can be turned into a running sum: equal 0s and 1s (treat 0 as -1), divisible by k (prefix modulo k) Grids with many rectangle-sum queries (2-D prefix sums) Build once in O(n); answer every range query in O(1). Subarray Sum Equals K Count the contiguous subarrays whose sum equals k . The array may contain negative numbers. A subarray between two prefixes sums to k when prefix[j] - prefix[i] == k , so for each running total look up how many earlier prefixes equal running - k . Seeding the map with {0: 1} counts subarrays that start at index 0. No window logic means negatives are fine. One pass, with an O(1) average dictionary lookup and update per element. The map holds at most n + 1 distinct prefix sums. Product of Array Except Self Return an array where each element is the product of all the others , without using division, in O(n). This is a prefix- product pass followed by a suffix-product pass. Storing the left products in the output array and multiplying in the right products on the way back gives O(1) extra space besides the output. Two linear passes with constant work per element. The only allocation is the output array itself. Off-by-one when converting an inclusive range to prefix indexes. Use prefix[right + 1] - prefix[left] . Forgetting to seed the hash map with the empty prefi Running Sum of 1d Array leetcode 1480 Range Sum Query - Immutable leetcode 303 Find Pivot Index leetcode 724 Product of Array Except Self leetcode 238 Subarray Sum Equals K leetcode 560 Contiguous Array leetcode 525 Subarray Sums Divisible by K leetcode 974 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. Will I be asked for the sum of many ranges, or for how many ranges add up to a given total? range sum queries subarray sum equals k / divisible by k running total / cumulative product of array except self equal number of 0s and 1s balance / pivot index Given an array of 0s and 1s, find the longest subarray with an equal number of 0s and 1s. Treat each 0 as -1. Equal counts means the subarray sums to 0, so two equal prefix sums mark its ends. Store the first index of each prefix. Design a class that answers many queries for the sum of elements between two indexes of a fixed array. 'Many range sum queries' on a static array: precompute the prefix sums once, answer each query with one subtraction. You need range minimum/maximum (use a sparse table, segment tree or monotonic deque), or the array changes between queries (use a Fenwick or segment tree). Contiguous Array itertools.accumulate builds prefix sums in one call; initial=0 (3.8+) adds the leading zero. Counting prefixes: seed a dict with {0: 1} (count) or {0: -1} (earliest index) so subarrays starting at index 0 are included. For 'divisible by k', use the prefix sum modulo k . Python's % is already non-negative for positive k, unlike Java. How does a prefix sum turn range-sum queries into O(1) operations? Precompute prefix[i] = sum of the first i elements in O(n). The sum of any range [l, r] is then prefix[r + 1] - prefix[l] , one subtraction. It pays off when there are many queries on a fixed array; for a single query a plain loop is fine. Count the subarrays whose sum equals k, when the array may contain negatives. What is the approach and complexity? One pass keeping a running sum and a hash map from prefix sum to the number of times it has occurred (seeded with {0: 1} ). For each element, add map[running - k] to the answer, then record the running sum. O(n) time, O(n) space. Why does a sliding window fail for \"subarray sum equals k\" when the array has negative numbers? A sliding window assumes that extending the window increases the sum and shrinking it decreases the sum, so it knows which end to move. Negative numbers break that monotonic behaviour: adding an element can lower the sum. Prefix sums compare two totals directly and make no such assumption. How would you handle \"longest subarray with equal 0s and 1s\" or \"subarrays divisible by k\"? Transform the property into a prefix-sum equality. Equal 0s and 1s: map 0 to -1, so a balanced subarray sums to 0, and two equal prefix sums mark its ends (store the earliest index of each). Divisible by k: two prefixes with the same remainder modulo k bound a divisible subarray, so count pairs of equal remainders. from itertools import accumulate build_prefix nums return list prefix range_sum left right inclusive indexes print subarray_sum count running seen many times occurred earlier that leaves exactly product_except_self range product everything multiply","dsa/two-pointers":"Two pointers move toward each other (or in the same direction at different speeds) across a sorted array or string. It collapses an O(n^2) brute-force pair search into a single O(n) sweep by exploiting order. Array or string is sorted, or can cheaply be sorted, and you need pairs/triplets meeting a condition Need to compare from both ends inward (palindrome check, container problems) Need to remove/partition/compact elements in place without extra space Fast/slow pointer variant: cycle detection, finding the middle of a linked list The classic converging form on a sorted array or string. Valid Palindrome Given a string, determine if it's a palindrome, considering only alphanumeric characters and ignoring case. Pointers converge from the outside in, skipping non-alphanumeric characters and comparing case-insensitively, until they meet in the middle. left and right each move inward at most n/2 times combined, so the total number of character comparisons is bounded by n regardless of how many get skipped for being non-alphanumeric. No extra data structure grows with input size, so space stays constant. 3Sum Given an array of integers, return all unique triplets that sum to zero. Sort first so the two-pointer sweep can decide direction from comparisons. Fix one element with an outer loop, then two-pointer the rest. Skip duplicates at both the outer loop and inner sweep to keep triplets unique. Sorting costs O(n log n), but the outer loop (n iterations) combined with an inner two-pointer sweep (up to n iterations each) dominates at O(n^2), since n^2 grows faster than n log n. No extra array is allocated for the search itself — only the output triplets use extra space. Valid Palindrome leetcode 125 Move Zeroes leetcode 283 Two Sum II - Input Array Is Sorted leetcode 167 3Sum leetcode 15 Container With Most Water leetcode 11 Sort Colors leetcode 75 Trapping Rain Water leetcode 42 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. Is the data sorted (or can I sort it), and can I decide which end to move by comparing two values? sorted array pair or triplet with a given sum palindrome from both ends in place with O(1) extra space remove duplicates / partition closest to a target Given a sorted array, return the indices of two numbers that add up to a target, using only constant extra space. Sorted plus 'pair that sums' plus O(1) space is the textbook converging-pointers signal. Sum too small: move left up. Too large: move right down. Given a string, decide if it can become a palindrome by deleting at most one character. Compare from both ends inward. On the first mismatch, try skipping the left character or the right one and check the remainder is a palindrome. The data is unsorted and you must keep the original order or indices (use a hash map), or the condition is not monotonic in the pointers. Container With Most Water Swap without a temporary variable using tuple assignment. If you must sort but keep original indices, sort the indices instead of the values. s == s[::-1] is a quick palindrome check, but it copies the string (O(n) space). Use pointers when space is constrained. How do you check for a palindrome ignoring non-alphanumeric characters in O(1) space? Two pointers from both ends: skip characters that are not alphanumeric, compare lowercased characters, and move inward until the pointers meet. O(n) time, O(1) space. Cleaning the string first and comparing to its reverse is simpler but uses O(n) extra space. 3Sum: why sort first, and how do you avoid duplicate triplets? Sorting enables the two-pointer sweep (move left up if the sum is too small, right down if too large) and groups duplicates together. Fix element i , skip it if it equals the previous one, run two pointers on the rest, and skip repeated values after finding a match. O(n&sup2;) time, O(1) extra space besides output. Container With Most Water: why is it correct to move the shorter line inward? The area is limited by the shorter line times the width. Moving the taller line inward can only shrink the width without raising the limiting height, so it cannot help; moving the shorter one might. This argument shows no better pair is skipped. O(n). When does two pointers not apply? When the data is unsorted and order cannot be changed, or when the condition is not monotonic in the pointers. Then use a hash map or set (for pairs) or a different technique. Also, if you need original indices after sorting, remember to carry them along. two_pointer_template target left right while curr return elif else is_palindrome isalnum lower false true three_sum nums sort range continue total append","dsa/sliding-window":"A window [left, right] expands and contracts over a contiguous subarray or substring, updating state incrementally instead of recomputing it. Turns O(n^2) or O(n^3) brute force into O(n). Problem language mentions a 'contiguous subarray' or 'substring' Need the max/min/count over windows satisfying some condition Fixed-size window ('subarray of size k') vs variable-size ('longest/shortest satisfying...') A running sum/count/frequency can be updated incrementally as the window slides The general variable-size shrinking window: expand right always, shrink left only while the window is invalid. Longest Substring Without Repeating Characters Given a string, find the length of the longest substring without repeating characters. Expand right one character at a time. Whenever the incoming character is already in the window, shrink from the left until it's gone. Every character enters and leaves the set at most once. The right pointer advances n times total, and the left pointer also advances at most n times total across the whole run (it never moves backward), so combined pointer movement is bounded by 2n, giving O(n). The set holds at most one entry per distinct character in the window, bounded by the size of the character set or n, whichever is smaller. Minimum Window Substring Given strings s and t, return the minimum window in s that contains every character of t (with multiplicity). Track how many of t's required character-counts are still unmet. Expand right until nothing is missing (window is valid), then greedily shrink from the left to find the tightest valid window before continuing to expand. Building the need counter costs O(m) for t's length. The main scan advances right n times and left at most n times total, since left never resets backward, giving O(n) for the  Maximum Average Subarray I leetcode 643 Best Time to Buy and Sell Stock leetcode 121 Longest Substring Without Repeating Characters leetcode 3 Longest Repeating Character Replacement leetcode 424 Permutation in String leetcode 567 Minimum Window Substring leetcode 76 Sliding Window Maximum leetcode 239 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. Is the answer defined over a contiguous range that I can extend on the right and shrink on the left as I scan? contiguous subarray or substring longest / shortest ... such that at most k distinct or k replacements subarray of size k maximum sum of k consecutive elements permutation or anagram inside a string Find the length of the longest substring that contains at most k distinct characters. 'Longest substring' + 'at most k' is a variable window: grow right, and shrink from the left while there are more than k distinct characters. Given a binary array, find the longest run of ones if you may flip at most k zeros. Contiguous run + a budget of at most k bad elements: keep a window with at most k zeros inside and shrink when you exceed it. Elements do not have to be adjacent (a subsequence: use DP), or the array has negatives and the rule is 'sum at least target' (the shrink logic breaks; use prefix sums with a hash map or deque). Sliding Window Maximum Use deque for windows and monotonic queues. list.pop(0) shifts every element (O(n)). For 'k distinct' windows, keep a Counter and delete keys that hit zero so len(counter) is the distinct count. Avoid slicing inside the loop: s[l:r] copies O(k) characters each time. Track indexes and slice once at the end. Longest substring without repeating characters: describe the approach. Expand the right pointer, adding characters to a set (or a last-seen index map). When a duplicate appears, shrink from the left until it is removed, tracking the maximum window length. O(n) time, O(min(n, alphabet)) space. With a last-index map you can jump the left pointer directly. How do you tell a fixed-size window from a variable-size one, and when do you shrink? \"Subarray of size k\" means fixed: slide by adding one element and removing one. \"Longest/shortest subarray satisfying X\" means variable: expand the right end always, and shrink the left end while the window is invalid (for longest) or while it is valid (for shortest). Minimum Window Substring: what state do you track and what is the complexity? A need-count map for the target and a counter of how many required characters are still missing. Expand until nothing is missing, then shrink from the left to find the smallest valid window, recording the best. O(n + m) time, O(alphabet) space. Sliding Window Maximum: how do you get O(n)? Keep a deque of indices whose values are in decreasing order. Before pushing index i , pop smaller values from the back; pop the front if it has left the window. The front is always the current maximum. Each index enters and leaves the deque once. longest_valid_window is_valid window left best right enumerate while return length_of_longest_substring seen remove from collections import counter min_window need missing best_left best_len float else","dsa/intervals":"Sort intervals by start (or end) time, then sweep once to merge or detect overlap. The vast majority of interval problems reduce to 'sort, then a single greedy pass.' Problem gives a list of [start, end] pairs Need to merge overlapping intervals, find free time, or detect conflicts Meeting room / scheduling style problems Sorting by start or end time unlocks a linear sweep The base merge-intervals sweep. Insert Interval Given a set of non-overlapping intervals sorted by start time, insert a new interval into the list, merging if necessary. Three phases in one linear pass: copy intervals ending before the new one starts, merge every interval that overlaps the new one by expanding its bounds, then copy whatever remains untouched. The input is already sorted, so a single linear scan through all n intervals — split into three phases (before, overlapping, after) — is enough; no additional sort is needed. The output list holds at most n+1 intervals, giving O(n) space. Meeting Rooms II Given an array of meeting time intervals, return the minimum number of conference rooms required. Sort by start time. The heap tracks end times of rooms currently in use. If the earliest-ending room has already freed up by the current meeting's start, reuse that room (replace its end time); otherwise open a new room (push). The final heap size is the number of rooms needed at peak. Sorting the n intervals by start time costs O(n log n), which dominates the subsequent scan (each of the n meetings triggers one O(log k) heap push or replace, where k <= n). The heap holds at most one end-time per room in use, bounded by n in the worst case. Meeting Rooms leetcode 252 Merge Intervals leetcode 56 Insert Interval leetcode 57 Non-overlapping Intervals leetcode 435 Minimum Number of Arrows to Burst Balloons leetcode 452 Interval List Intersections leetcode 986 Meeting Rooms II leetcode 253 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. Are there ranges [start, end] where overlap matters? If so, sort them first. intervals / ranges / [start, end] pairs merge overlapping meeting rooms / schedule conflicts insert into sorted intervals minimum removals so none overlap how many at the same time Given an array of meeting time intervals, determine if a person can attend all meetings. Sort by start, then any meeting that starts before the previous one ends is a conflict. One pass over adjacent pairs. Balloons are ranges on the x-axis. Find the minimum number of arrows to burst them all. Overlapping ranges can share one arrow. Sort by end and shoot at the earliest end; skip everything it already covers. Ranges carry weights or profits (weighted job scheduling needs DP plus binary search), or the ranges live on a graph. Non-overlapping Intervals sorted(intervals) sorts by start, then end. Use key=lambda x: x[1] to sort by end. Merging mutates the last result in place: merged[-1][1] = max(merged[-1][1], end) . Copy the input first if the caller's list must stay unchanged. For 'how many at once' you can sort start and end times separately and sweep, or push end times into a min-heap. Merge Intervals: outline the algorithm. Sort by start. Keep a result list; for each interval, if it starts at or before the last merged end, extend that end to the max of the two ends, else append it. O(n log n) for the sort, O(n) for the sweep. Meeting Rooms II: how do you compute the minimum number of rooms? Either sort by start and use a min-heap of end times (reuse a room if the earliest end is at or before the next start), or sort starts and ends separately and sweep with two pointers counting concurrent meetings. Both are O(n log n). Insert Interval into a sorted, non-overlapping list. One linear pass in three phases: copy intervals that end before the new one starts; merge all intervals that overlap it by taking min start and max end; copy the rest. O(n), no extra sort needed. Non-overlapping Intervals: minimum removals so none overlap? Greedy: sort by end time and keep an interval if it starts at or after the last kept end; otherwise it is removed. Choosing the earliest end leaves the most room for the rest. O(n log n). merge_intervals intervals sort lambda merged start last_end else append return insert new_interval result while import heapq min_meeting_rooms heap times ongoing meetings heapreplace heappush","dsa/binary-search":"Binary search halves the search space each step by exploiting monotonicity — sorted order, or any condition that flips from false to true. O(log n) instead of O(n), and it generalizes far beyond sorted arrays. Data is sorted, or you can define a monotonic predicate over a range 'Find the minimum/maximum value satisfying condition X' binary search on the answer space Problem naturally wants O(n) but the constraints demand O(log n) Rotated sorted array problems The textbook search-for-a-value form. Search in Rotated Sorted Array Search for a target in a rotated sorted array (no duplicates) in O(log n). At every step, one half of the current range is guaranteed sorted. Figure out which half that is, then check whether the target falls inside that sorted half's value range to decide which side to discard. Each iteration eliminates half of the remaining search range, exactly as in standard binary search, so the number of iterations is bounded by log2(n). Only a constant number of pointer variables are tracked, so space stays O(1). Koko Eating Bananas (search on the answer) Koko has piles of bananas and h hours. Find the minimum eating speed k such that she can finish all piles within h hours. The feasibility check 'can Koko finish within h hours at speed k' is monotonic in k — true for k implies true for every larger k. So binary search over the speed itself, not the array, shrinking toward the smallest feasible speed. The binary search over possible speeds takes O(log(max(piles))) iterations, and each iteration computes the total hours by scanning all n piles, giving O(n) work per iteration. Multiplying gives O(n log(max(piles))) overall; no extra structures grow with input, so space is O(1). Binary Search leetcode 704 Search Insert Position leetcode 35 First Bad Version leetcode 278 Find First and Last Position of Element in Sorted Array leetcode 34 Search in Rotated Sorted Array leetcode 33 Find Minimum in Rotated Sorted Array leetcode 153 Find Peak Element leetcode 162 Koko Eating Bananas leetcode 875 Capacity To Ship Packages Within D Days leetcode 1011 Median of Two Sorted Arrays leetcode 4 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. Is there a monotonic yes/no question ('is x big enough?') so I can discard half the possibilities each step? sorted or rotated sorted array O(log n) required minimum/maximum value such that it is feasible least capacity / smallest speed / fewest days first or last occurrence, insert position n up to 10⁹ or 10¹⁸ A conveyor belt carries packages with given weights. Find the least ship capacity that delivers everything within D days. 'Least ... such that it works' with a feasibility check that only gets easier as the number grows is binary search on the answer. Search capacity between max(weight) and sum(weight). Given a sorted array and a target, return the index where it is, or where it would be inserted. Sorted plus 'position' is a lower-bound search. That is exactly bisect_left . The data is unsorted and there is no monotonic property to exploit. Find First and Last Position of an Element bisect_left is the first index where x could go keeping order (lower bound); bisect_right is one past the last equal element. insort inserts but is still O(n). Since Python 3.10, bisect accepts key= , so you can search a list of records without building a separate list. No overflow to worry about: (lo + hi) // 2 is safe in Python (in Java write lo + (hi - lo) / 2 ). Pick one loop invariant and stick to it. What are the preconditions for binary search, and how do you avoid off-by-one bugs? The search space must be monotonic: sorted, or a predicate that flips from false to true once. Pick one invariant (for example \"answer lies in [lo, hi] \") and keep the loop condition and the bound updates consistent with it. In Java you also write mid = lo + (hi - lo) / 2 to avoid integer overflow; Python ints do not overflow. How do you search a rotated sorted array in O(log n)? At each step one half around mid is sorted. Determine which half by comparing nums[lo] with nums[mid] , check whether the target lies inside that sorted half, and discard the other half. Duplicates can break the ordering test and degrade the worst case to O(n). What is \"binary search on the answer\"? Give an example. Instead of searching an array, search the range of possible answers using a monotonic feasibility check. Koko Eating Bananas: if speed k works, any larger speed works, so search the smallest feasible k . Cost is O(check log range). Same idea applies to shipping capacity and split-array problems. How do you find the first or last occurrence of a value? Do not stop when you find a match; keep searching the left half (for first) or right half (for last) while recording the candidate. Python's bisect_left and bisect_right implement exactly these lower and upper bounds. binary_search nums target left right while return elif else search_rotated half sorted import math min_eating_speed piles hours ceil","dsa/bit-manipulation":"Operate directly on the binary representation of integers for O(1) tricks that would otherwise need loops or extra memory — XOR for finding unique elements, shifts for power-of-two logic, AND/OR for masks and flags. 'Without using extra memory' combined with numeric input often a bit trick Finding a single/unique element among duplicates XOR Checking or counting set bits, or detecting powers of two Subset generation via bitmask, where each bit represents include/exclude A few bit tricks that show up constantly. Single Number Given a non-empty array where every element appears exactly twice except for one, find that single element in O(n) time and O(1) space. XOR is commutative and associative, and a ^ a = 0 while a ^ 0 = a. XOR-ing every element together cancels every pair, leaving only the unpaired element standing. Each of the n elements is XOR-ed into the accumulator exactly once, giving O(n) time. A single integer accumulator is used regardless of array size, giving O(1) space. Counting Bits Given an integer n, return an array ans of length n+1 where ans[i] is the number of 1's in the binary representation of i. Dropping the lowest bit (i >> 1) gives a smaller number whose bit count is already computed earlier in the same pass; adding back whether that dropped bit was 1 (i & 1) completes the count. It's DP layered on top of a bit trick. Each ans[i] for i from 1 to n is computed with O(1) work (one shift, one AND, one array lookup, one addition), giving O(n) total. The output array itself holds n+1 entries, giving O(n) space. Single Number leetcode 136 Number of 1 Bits leetcode 191 Power of Two leetcode 231 Counting Bits leetcode 338 Missing Number leetcode 268 Reverse Bits leetcode 190 Sum of Two Integers leetcode 371 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. Is this about parity, uniqueness, powers of two, or subsets, and does it demand O(1) extra space? every element appears twice except one without extra space power of two count set bits / Hamming weight enumerate subsets with a mask add or swap without using + or a temp variable An array holds the numbers 0..n with one missing. Find it using O(1) extra space. XOR all indices and all values. Everything cancels except the missing number (or use the sum formula). Return the number of 1 bits in the binary representation of an integer (its Hamming weight). Repeatedly clear the lowest set bit with n &= n - 1, counting iterations. Or use n.bit_count(). Readability matters more than a micro-optimisation. A set or counter is fine. Sum of Two Integers (no + or -) Python ints have unlimited width. To emulate fixed-width behaviour, mask with 0xFFFFFFFF , then convert back if the sign bit is set. Handy built-ins: bin(n) , n.bit_count() (3.10+), n (n - 1) clears the lowest set bit, n -n isolates it. There is no unsigned shift . Right shift is arithmetic (sign-extending) for negatives. Single Number: find the element that appears once when the rest appear twice. XOR everything: a ^ a = 0 and a ^ 0 = a , and XOR is commutative, so pairs cancel and the unique value remains. O(n) time, O(1) space. How do you check for a power of two and count set bits? Power of two: n 0 and n (n - 1) == 0 . Count set bits with Brian Kernighan's loop: while n: n = n - 1; count += 1 , which runs once per set bit. Python also has int.bit_count() (3.10+). What is different about bit manipulation in Python versus Java? Python ints are arbitrary precision with infinite sign extension, so there is no 32-bit wraparound. To emulate fixed-width behaviour, mask with 0xFFFFFFFF and convert back for negatives. Right shift is arithmetic, and there is no operator. How do you enumerate all subsets with a bitmask? For mask in range(1 n) , include element i when mask (1 i) is non-zero. It produces 2 n subsets in O(n 2 n ) and is also the basis of bitmask DP for small n (roughly n 20). bit_tricks is_power_of_two count_set_bits count lowest_set_bit return single_number nums result count_bits range","dsa/fast-slow-pointers":"Two pointers move along the same sequence at different speeds. If there is a cycle, the fast pointer eventually laps the slow one; if there is not, fast reaches the end exactly when slow is at the middle. It needs O(1) space where a visited set would need O(n). Detect a cycle in a linked list, an array, or a repeating sequence of states Find the middle of a linked list without knowing its length Find where a cycle starts 'n-th node from the end' (a fixed gap between two runners) 'Find the duplicate number without extra space' or 'does it reach 1 or loop forever' (happy number) Floyd's tortoise and hare. Slow moves one step, fast moves two. Linked List Cycle II: find where the cycle starts Return the node where a cycle begins, or None if the list has no cycle. Phase 1 detects the cycle. Phase 2 works because the distance from the head to the cycle start equals the distance from the meeting point to the cycle start, going around. Advance both one step at a time and they meet exactly there. Fast catches slow within at most one lap of the cycle once both are inside it, and the second phase walks at most n steps. Only two pointers are kept. Remove Nth Node From End of List Remove the n-th node from the end of a linked list in one pass . This is the runner variant: keep two pointers a fixed gap apart. When the lead reaches the last node, the trailing pointer is right before the node to delete. The dummy head removes the special case of deleting the first node. The lead pointer walks the list once, and the trailing pointer follows at most the same number of steps. No extra structures are allocated. Always check fast and fast.next in that order, so fast.next.next is never evaluated on None . Compare nodes with is , not values: two different nodes can hold equal values. The patt Linked List Cycle leetcode 141 Middle of the Linked List leetcode 876 Happy Number leetcode 202 Palindrome Linked List leetcode 234 Linked List Cycle II leetcode 142 Remove Nth Node From End of List leetcode 19 Find the Duplicate Number leetcode 287 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. Is there a cycle or repeated state, or do I need the middle or the end position of a sequence without knowing its length? cycle in a linked list / array / sequence middle of the list start of the cycle n-th node from the end find the duplicate without extra space loops forever or reaches 1 Repeatedly replace a number with the sum of the squares of its digits. Return true if it reaches 1, false if it loops forever. A repeating sequence of states is a cycle. Floyd's two speeds detect it in O(1) space instead of a set of seen numbers. An array of n+1 integers in [1, n] holds one repeated number. Find it without modifying the array and in O(1) extra space. Treat i → nums[i] as a pointer. A duplicate means two indexes point to the same place, which creates a cycle. You need every repeated element or the number of repeats (use a hash map or set). Happy Number Compare nodes with is , not == , when detecting a cycle. Equal values do not mean the same node. divmod(x, 10) gives quotient and remainder in one call, handy for digit loops. The same idea works on an array: treat i - nums[i] as a next pointer (Find the Duplicate Number). Detect a cycle and find where it starts. Floyd's tortoise and hare: a slow pointer (1 step) and fast pointer (2 steps) meet if there is a cycle. Then reset one pointer to the head and advance both one step at a time; they meet at the cycle start. O(n) time, O(1) space. How do you remove the n-th node from the end in one pass? Use two pointers separated by n nodes: advance the lead pointer n steps, then move both until the lead reaches the end; the trailing pointer is just before the node to delete. A dummy head handles removing the first node. Find the duplicate number in an array of n + 1 integers from 1 to n, in O(1) extra space and without modifying the array. Treat i - nums[i] as a linked list. Because a value repeats, two indexes point to the same place, which forms a cycle, and the cycle's entrance is the duplicate. Use Floyd's algorithm: find the meeting point, reset one pointer to 0, and advance both one step at a time until they meet. O(n) time, O(1) space. When is a hash set of visited nodes acceptable instead of Floyd's algorithm? When O(n) extra space is allowed and simplicity matters, a set is easier to write and to explain, and it also gives you the first repeated node directly. Floyd's algorithm is preferred when the problem demands O(1) space or when nodes are not hashable. has_cycle head slow fast while next return true false detect_cycle phase they meet inside cycle break else none restart pointer start remove_nth_from_end dummy listnode handles removing itself lead trail range open nodes sits just before target","dsa/linked-list-reversal":"Reversing a linked list in place means re-pointing each node's next backwards while remembering the rest of the list. Partial reversals, pair swaps, palindrome checks and list reordering are all variations of the same three-pointer loop. 'Reverse a linked list', or a portion of it between two positions Reverse in groups of k, swap nodes in pairs, rotate the list 'Palindrome linked list' or 'reorder list' in O(1) extra space 'In place' with a linked list as input Iterative reversal with three pointers. Save the next node before you overwrite next . Reverse Linked List II (a sublist) Reverse the nodes from position left to position right (1-indexed) and leave the rest unchanged. Walk to the node before the sublist, reverse exactly right - left + 1 nodes with the standard loop, then reconnect both ends. The dummy head handles the case where the sublist starts at the head. At most one pass over the list: up to n steps to reach the sublist and reverse it. Only a fixed number of pointers are used. Swap Nodes in Pairs Swap every two adjacent nodes by changing links , not values. Each iteration reverses one pair locally with three link updates and then moves prev to the tail of the pair. It is the same re-pointing idea as full reversal, applied to windows of two. Each pair is processed once with a constant number of pointer updates, and no extra memory is allocated. Overwriting curr.next before saving it loses the rest of the list. Always save nxt first. For partial reversals, forgetting to reconnect the node before the sublist and the node after it. Returning head instead of prev (the new head) or dummy.next . Recursive reversal uses O(n) stack space and can hit Python's recursion limit on long lists. Reverse Linked List leetcode 206 Merge Two Sorted Lists leetcode 21 Palindrome Linked List leetcode 234 Reverse Linked List II leetcode 92 Swap Nodes in Pairs leetcode 24 Reorder List leetcode 143 Reverse Nodes in k-Group leetcode 25 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. Do I have to rearrange the links in place (reverse all, part, or pairs), possibly after finding the middle? reverse a linked list or a portion of it reverse in groups of k palindrome linked list reorder list (L0, Ln, L1, Ln-1, ...) swap nodes in pairs rotate the list Given a linked list, determine whether it is a palindrome using O(1) extra space. Find the middle (fast/slow), reverse the second half in place, then compare the two halves. Given the head of a linked list, reverse the nodes k at a time and return the modified list. Reverse in groups of k: apply the standard reversal to each block of k nodes and reconnect the blocks. O(n) extra space is allowed and you only need to read the values: copy them into an array, which is simpler. Palindrome Linked List Reversing pointers with one chained tuple assignment is a trap: the targets are assigned left to right , so curr.next can be overwritten before it is read. Save next first. A dummy head ( ListNode(0, head) ) removes special cases when the head itself changes. Recursive reversal is elegant but uses O(n) stack and hits the recursion limit on long lists. Prefer the iterative loop in interviews unless asked. Reverse a linked list iteratively and recursively. What are the complexities? Iterative: keep prev , curr , and next ; point curr.next at prev and advance: O(n) time, O(1) space. Recursive: reverse the rest, then point the next node back at the current one: O(n) time, O(n) stack space. Why use a dummy head node? It removes special cases for operations at the head (insert, delete, merge), since the real head is always dummy.next . It keeps the code uniform and avoids null checks. Check whether a linked list is a palindrome in O(n) time and O(1) space. Use fast and slow pointers to find the middle, reverse the second half in place, then compare the first half with the reversed half node by node. Optionally reverse the second half back to restore the list. Reverse nodes in k-groups: describe the approach. Use a dummy head. For each block, first check that k nodes remain; if so, reverse exactly k nodes with the standard three-pointer loop and reconnect the block's start to the previous group's tail and its new tail to the next group's head. Leave a final block with fewer than k nodes unchanged. O(n) time, O(1) space. reverse_list head prev curr none while next remember rest flip link step forward return reverse_between left right dummy listnode before range node just sublist first points past swap_pairs second skips over back previous front tail this pair","dsa/stack":"A stack is last-in, first-out. Reach for it whenever the most recent unmatched thing decides what happens next: bracket matching, undo, nested structures, evaluating expressions, and 'remember the state before this'. Matching or balancing pairs: parentheses, brackets, tags 'Undo', 'most recent', or a nested structure (decode a string, simplify a path) Evaluating expressions, especially postfix (reverse Polish) notation Turning recursion into iteration with an explicit stack (iterative DFS) A running summary you need in O(1): minimum so far (Min Stack) The bracket matcher: push openers, and each closer must match the most recent opener. Min Stack Design a stack that supports push , pop , top and get_min , each in O(1) . Store the running minimum beside every value. When you pop, the previous minimum is already sitting underneath, so no search is ever needed. Every operation touches only the top of the list. Each stored entry adds a constant amount of extra data (the running minimum). Evaluate Reverse Polish Notation Evaluate an expression in postfix notation, such as [\"2\", \"1\", \"+\", \"3\", \"*\"] (which is (2 + 1) * 3). Push numbers. An operator consumes the two most recent numbers and pushes the result, so nested sub-expressions resolve themselves. Note that a // b would floor for negatives, so use int(a / b) to truncate toward zero. Each token is handled once with O(1) stack work. In the worst case (all operands first) the stack holds about half of the tokens. Popping from an empty stack. Check if not stack first (Valid Parentheses fails on an input like \")\" ). Using a list as a queue by calling pop(0) . That is O(n). Use collections.deque for FIFO. Forgetting the final emptiness check: leftover openers mean the string is invalid. Operand order for non-commutative operator Valid Parentheses leetcode 20 Min Stack leetcode 155 Evaluate Reverse Polish Notation leetcode 150 Simplify Path leetcode 71 Decode String leetcode 394 Generate Parentheses leetcode 22 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. Do I need the most recent unmatched thing, or the next greater/smaller element? valid or balanced brackets undo / most recent nested structure or expression postfix (reverse Polish) evaluation minimum or maximum so far in O(1) remove adjacent duplicates / simplify a path Given a string, repeatedly remove adjacent duplicate letters (for example abbaca becomes ca). Each new letter is compared with the most recent kept letter. A match cancels it, which is a pop; otherwise push. Simplify a Unix absolute path: handle ., .., and repeated slashes. '..' means undo the most recent directory, which is a pop. Split on '/', push names, pop on '..'. You need oldest-first processing (use a queue or deque) or random access to elements. Decode String A plain list is your stack: append and pop are O(1), stack[-1] peeks, and if not stack tests empty. Match brackets with a dict of closer to opener. Do not use a list as a queue: pop(0) is O(n). Use collections.deque when you need FIFO. Valid Parentheses: outline the solution and its complexity. Push opening brackets. On a closing bracket, the stack must be non-empty and its top must be the matching opener; pop it. At the end the stack must be empty. O(n) time, O(n) space. Design a stack that supports push, pop, top and getMin in O(1). Keep a second stack of running minimums (or store (value, current_min) pairs). On push, record min(value, current_min) ; on pop, pop both. getMin reads the top of the min stack. O(1) time per operation, O(n) extra space. When do you replace recursion with an explicit stack? When depth may exceed the recursion limit (deep trees or graphs), or when you need to control traversal state. An explicit stack turns recursive DFS into iterative DFS with the same O(V+E) complexity but heap-allocated depth. How do you evaluate a postfix (Reverse Polish) expression, and what should you watch for with division? Scan the tokens: push numbers; for an operator, pop the right operand first and then the left, apply the operator, and push the result. The final stack top is the answer. Watch division and modulo on negatives: Java truncates toward zero, but Python's // floors, so use int(a / b) when the problem expects truncation. is_balanced pairs stack return false else append class minstack __init__ self value minimum push current_min get_min eval_rpn tokens right operand comes first elif truncate toward zero like java","dsa/monotonic-stack":"A monotonic stack keeps its elements sorted by popping anything that breaks the order. The moment an element is popped, you have found its 'next greater' (or next smaller) element. Every index is pushed once and popped once, so the whole scan is O(n). 'Next greater / next smaller element' for every position 'How many days until a warmer temperature', stock span, previous smaller element Histogram and water-trapping problems (largest rectangle, trapping rain water) Each element is answered by the nearest element to its left or right that beats it 'Remove k digits to make the smallest number' (greedy with a stack) Keep values decreasing from bottom to top. Anything smaller than the current value has just found its next greater element. Daily Temperatures For each day, return how many days you must wait for a warmer temperature (0 if there is none). Store indices , not values, so you can compute the distance. When today is warmer than the day on top of the stack, that day just found its answer: pop it and record the gap. Each index is pushed once and popped at most once across the whole run. That amortised accounting keeps the nested while loop linear. Largest Rectangle in Histogram Given bar heights, return the area of the largest rectangle that fits inside the histogram. When a shorter bar arrives, every taller bar on the stack can no longer extend right. Popping one gives its height; the new stack top is the nearest smaller bar on the left and index i is the nearest smaller on the right, so the width is i - left - 1 . Each bar is pushed and popped exactly once. The sentinel 0 guarantees the stack is emptied without a second loop. Storing values instead of indices, which makes distances and widths impossible to compute. Strict versus non-strict comparison ( vs = ) change Next Greater Element I leetcode 496 Daily Temperatures leetcode 739 Next Greater Element II leetcode 503 Online Stock Span leetcode 901 Car Fleet leetcode 853 Largest Rectangle in Histogram leetcode 84 Trapping Rain Water leetcode 42 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. For each element, do I need the nearest element to its left or right that is bigger (or smaller)? next greater / next smaller element days until a warmer temperature stock span / previous smaller largest rectangle in a histogram visible buildings / taller to the right remove k digits to make the smallest number Given a circular array, return the next greater element for every element, wrapping around the end. 'Next greater' is the monotonic-stack signal. For a circular array, walk the array twice. Given a number as a string, remove k digits so the remaining number is the smallest possible. Keep the digits increasing on a stack. When a smaller digit arrives, pop larger ones while you still have removals left. You need the k-th greater element or every greater element (not just the nearest), or you only need a value lookup (use a hash map). Next Greater Element II (circular) Store indices on the stack, not values, so you can compute distances and widths. nums[stack[-1]] reads the value. A sentinel simplifies flushing the stack: heights + [0] guarantees everything is popped by the end. The invariant flips with the question: pop while top x for next greater, pop while top x for next smaller. What is a monotonic stack and what problems does it solve? A stack kept in increasing or decreasing order by popping elements that break the order. It finds the next greater/smaller element, daily temperatures, and largest rectangle in a histogram. Each element is pushed and popped at most once, so the total work is O(n) even with a nested while loop. Largest Rectangle in Histogram: outline the monotonic-stack solution. Keep bar indexes on a stack with increasing heights. When a shorter bar arrives, pop taller bars: for each popped bar, its height applies over the width between the new stack top (nearest smaller on the left) and the current index (nearest smaller on the right). Append a sentinel height 0 to flush the stack. O(n) time and space. How do you adapt next-greater-element to a circular array? Iterate over indexes 0 .. 2n - 1 using i % n , pushing indexes only during the first pass. Elements near the end can then see the elements at the start. Still O(n). Monotonic stack versus monotonic deque: when do you use each? A stack works when you only need the nearest greater/smaller element on one side and elements never expire. A deque adds removal from the front, which you need for sliding windows (for example Sliding Window Maximum), where old indexes fall out of the window. next_greater nums result stack indices whose values decreasing enumerate while next greater that index append return daily_temperatures temps with temperatures largest_rectangle_area heights increasing best final flushes height left else nearest smaller","dsa/heap":"A binary heap gives O(log n) insert and extract-min/max, with O(1) peek. Reach for it whenever you need the 'top k' of something, or repeatedly need the current min/max as data streams in. 'Kth largest/smallest' or 'top k' language in the problem Need to repeatedly extract the min or max as data arrives Merging k sorted lists or arrays Scheduling / greedy problems where you always want the next-best available option Python's heapq is a min-heap. Capping it at size k and popping the smallest gives you the k largest elements seen so far. Top K Frequent Elements Given an array of integers, return the k most frequent elements. Build a frequency map, then use nlargest, which internally maintains a heap of size k rather than sorting the entire frequency map — O(n log k) instead of O(n log n). Building the frequency map costs O(n). Maintaining the heap in nlargest at size k means each of the n elements triggers at most an O(log k) heap operation, giving O(n log k) total — cheaper than fully sorting all n elements when k is small. The frequency map itself holds up to n distinct keys, giving O(n) space. Merge k Sorted Lists Merge k sorted linked lists into one sorted linked list. Keep exactly one candidate node per list in the heap. Always pop the globally smallest, attach it to the result, and push its successor from the same list. The list index i is included purely as a tiebreaker, since ListNode objects aren't directly comparable when values are equal. Every one of the N total nodes across all lists is pushed and popped from the heap exactly once, and the heap never holds more than k elements (one per list) at a time, so each push/pop costs O(log k). Multiplying N operations by O(log k) each gives O(N log k); the heap's O(k) size is the space bound. Kth Largest Element in a Stream leetcode 703 Last Stone Weight leetcode 1046 Kth Largest Element in an Array leetcode 215 Top K Frequent Elements leetcode 347 K Closest Points to Origin leetcode 973 Task Scheduler leetcode 621 Merge k Sorted Lists leetcode 23 Find Median from Data Stream leetcode 295 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. Do I repeatedly need the current smallest or largest (or the top k) while the data changes? k-th largest / smallest top k / k most frequent / k closest merge k sorted lists median of a stream always process the highest priority next repeatedly take the two largest Given points on a plane, return the k closest to the origin. Keep a max-heap of size k (negate distances). Each new point either replaces the farthest of the k or is ignored: O(n log k). Repeatedly smash the two heaviest stones; if unequal, the difference stays. Return the last stone's weight. 'Repeatedly take the largest' while the collection changes is a priority queue. Negate values for a max-heap. You need everything sorted once (just sort), or arbitrary rank queries (an order-statistics tree). Find Median from Data Stream heapq is a min-heap only. Negate values on the way in and out to get a max-heap. Push tuples (priority, counter, item) . If priorities tie, Python compares the next field, and un-orderable items would raise an error. A running counter breaks ties safely. heapq.heapify(list) is O(n), better than n pushes. nlargest(k, it, key=...) and nsmallest are concise for one-off top-k. Kth largest element: compare heap and quickselect. Min-heap of size k: O(n log k) time, O(k) space, and works on streams. Quickselect: O(n) average, O(n&sup2;) worst case, O(1) extra space, but needs the whole array in memory. Sorting is O(n log n). Merge k sorted lists: approach and complexity. Keep a min-heap with the current head of each list. Pop the smallest, append it to the output, and push its successor. With N total elements and k lists it is O(N log k) time and O(k) space. Divide and conquer merging gives the same bound. Find the median of a data stream. Two heaps: a max-heap for the lower half and a min-heap for the upper half, kept balanced within one element. The median is the top of the larger heap or the average of both tops. Insert is O(log n), median lookup O(1). What is the complexity of building a heap, and how does a heap compare with a BST? heapify builds a heap from n items in O(n). A heap gives O(1) peek and O(log n) push/pop of the min or max but no ordered traversal or fast arbitrary search. A balanced BST gives O(log n) search, delete and ordered iteration, at higher constants. import heapq kth_largest nums heap heappush heappop return from collections counter top_k_frequent freq nlargest keys merge_k_lists lists node enumerate dummy listnode tail while next","dsa/trees":"Recursive DFS (preorder/inorder/postorder) and level-order BFS are the two lenses for tree problems. Almost every tree problem reduces to 'combine information returned from the left and right subtrees.' Problem gives a TreeNode with .left / .right Need something computed bottom-up (height, sum, balanced check) post-order DFS returning values Need level-by-level processing BFS with a queue Need root-to-leaf path logic pre-order DFS with a path/accumulator The generic post-order DFS shape most tree solutions follow. Maximum Depth of Binary Tree Given the root of a binary tree, return its maximum depth. Classic post-order recursion: the depth of a node is 1 plus the deeper of its two subtrees. A null node contributes 0, acting as the correct base case for the max() combination. Every node in the tree is visited exactly once by the recursion, giving O(n) total calls. The space cost comes from the recursion call stack, which grows as deep as the tree is tall — O(h), which is O(log n) for a balanced tree but can degrade to O(n) for a skewed one. Binary Tree Level Order Traversal Given the root of a binary tree, return the level order traversal of its nodes' values, grouped by level. Snapshotting len(queue) at the top of each iteration captures exactly the nodes belonging to the current level, so children appended during the loop don't bleed into the current level's count. Every node is enqueued and dequeued exactly once, giving O(n) time. In the worst case (a wide tree, like a complete binary tree's last level) the queue can hold up to roughly n/2 nodes at once, which rounds to O(n) space. Binary Tree Inorder Traversal leetcode 94 Maximum Depth of Binary Tree leetcode 104 Invert Binary Tree leetcode 226 Binary Tree Level Order Traversal leetcode 102 Validate Binary Search Tree leetcode 98 Kth Smallest Element in a BST leetcode 230 Lowest Common Ancestor of a Binary Tree leetcode 236 Construct Binary Tree from Preorder and Inorder Traversal leetcode 105 Serialize and Deserialize Binary Tree leetcode 297 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. Can I define the answer for a node from the answers of its children (or from a bound passed down to them)? binary tree or BST depth, height or diameter path sum / root-to-leaf level by level / right side view lowest common ancestor validate BST or balanced serialize / construct from traversals Return the diameter of a binary tree: the number of edges on the longest path between any two nodes. Post-order: each node returns its height, and while doing so updates a global best with left_height + right_height. Return the right side view of a binary tree: the values you would see from the right, top to bottom. Level by level means BFS. Keep the last node of each level. It is really a graph with cycles or shared children (use graph traversal with a visited set). Validate Binary Search Tree float('-inf') and float('inf') make clean bounds. Immutable default arguments are fine (mutable ones are not). The recursion limit is about 1000, so a skewed tree with 10⁵ nodes can crash recursive DFS. Use an explicit stack for a safe iterative traversal. Return a tuple from recursion when one pass needs two facts (for example (is_balanced, height) ) instead of calling the function twice. Explain preorder, inorder and postorder traversals, and what an inorder traversal of a BST gives. Preorder: node, left, right. Inorder: left, node, right. Postorder: left, right, node. Inorder on a BST yields values in sorted order, which is the basis for validating a BST and finding the k-th smallest. Validate a BST: what is the common mistake? Comparing each node only with its direct children. A node deep in the left subtree can still be greater than an ancestor. Pass down allowed bounds (low, high) (or check that inorder output is strictly increasing). O(n) time. Compare BFS and DFS on trees in time and space. Both visit every node in O(n). DFS uses O(h) stack space (O(log n) balanced, O(n) skewed). BFS uses O(w) space for the widest level, up to about n/2 for a complete tree. Use BFS for level-based questions and shortest path; DFS for path/subtree questions. Lowest Common Ancestor: how does it differ for a BST and a general binary tree? BST: walk down from the root; if both values are smaller go left, if both larger go right, otherwise the current node is the LCA: O(h). General tree: recurse both sides; if left and right each return a node, the current node is the LCA: O(n). node return base_case left right combine max_depth root from collections import deque level_order result queue while level range popleft append","dsa/tries":"A trie (prefix tree) is a nested structure of nodes keyed by character, purpose-built for prefix queries — autocomplete, dictionary lookups, and word search — in time proportional to word length, not dictionary size. Need prefix matching or 'starts with' queries Autocomplete, spell-check, or word validity against a dictionary Many words share common prefixes and you want O(L) lookup independent of dictionary size Wildcard-style word search ('a.c' matches 'abc', 'axc', ...) The base trie: insert, exact search, and prefix search. Implement Trie (Prefix Tree) Implement insert, search, and startsWith for a trie, as shown in the template above. Insertion walks down the tree, creating a child node per character. is_end distinguishes 'apple was inserted' from 'app is merely a prefix of something inserted' — the same path can represent both a prefix and a complete word depending on where is_end flags are set. Each operation walks exactly one character-length path down the tree, so it costs O(L) regardless of how many other words are stored. Space is proportional to the total number of unique character-paths created across every inserted word, bounded by the sum of all word lengths. Add and Search Word (with '.' wildcard) Design a data structure that supports adding words and searching for a word where '.' can match any single character. Insertion is unchanged. Search becomes a DFS: on a normal character it follows one child deterministically; on '.' it branches into every child at that node and returns True if any branch succeeds. With no wildcards, search behaves like the base trie — O(L). Each '.' forces the search to branch into every child at that node, and in the worst case (a word of all wildcards) that branching compounds multiplicatively across all L positions, giving u Longest Common Prefix leetcode 14 Implement Trie (Prefix Tree) leetcode 208 Design Add and Search Words Data Structure leetcode 211 Replace Words leetcode 648 Search Suggestions System leetcode 1268 Longest Word in Dictionary leetcode 720 Word Search II leetcode 212 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. Am I repeatedly asking 'does any word start with this prefix?' across many words? prefix / starts with autocomplete or suggestions dictionary of words word search on a grid with many words longest common prefix wildcard '.' matching Design a search box: after each typed character, return the top 3 product names that share that prefix. Prefix queries over many strings is the reason tries exist. Walk down one node per typed character. Given a dictionary of roots and a sentence, replace every word with the shortest root that is its prefix. Walk each word down the trie; the first end-of-word marker you hit is the shortest prefix root. You only need exact membership. A set is simpler and faster. Replace Words A trie can be nested dicts: node = node.setdefault(ch, {}) , with a key like '#' as the end marker. One-line recursive trie with defaultdict , a well-known trick. Use a TrieNode class with __slots__ when memory matters. Slots remove the per-instance dict. What is a trie and when is it better than a hash set of strings? A tree where each edge is a character and each path from the root spells a prefix. It supports prefix queries (startsWith, autocomplete) and enumeration by prefix in O(L), which a hash set cannot do efficiently. For exact membership only, a hash set is simpler and often faster. What are the time and space complexities? Insert, search and prefix-search are O(L) for a word of length L, independent of how many words are stored. Space is O(total characters) in the worst case, times the per-node overhead of the children structure. Word Search II: why does a trie help? Building a trie of all target words lets DFS on the grid follow only paths that are a prefix of some word and prune immediately otherwise, instead of running a separate search per word. It also finds all words in a single traversal. How would you reduce a trie's memory use? Use dicts (sparse) instead of fixed 26-slot arrays for large alphabets, compress single-child chains into one edge (radix/compressed trie), or store the words in a sorted array and binary search if the set is static. class trienode __init__ self children is_end false trie root insert word node setdefault true search _find return none starts_with prefix apple marked full exists worddictionary add_word child values","dsa/dfs":"Depth-first search follows one branch as far as it goes before backing up. Use it to visit everything reachable, count connected components, detect cycles, copy a graph, or enumerate paths in trees and graphs. It runs in O(V + E) with recursion or an explicit stack. 'Explore all paths' or 'is B reachable from A' Connected components , provinces, groups of friends Cycle detection , or 'can all tasks finish' Clone or copy a graph; path sums from root to leaf You need to visit everything , not the shortest route Recursive DFS with a visited set, and the same thing with an explicit stack for very deep graphs. Number of Provinces (connected components) Given an adjacency matrix where is_connected[i][j] == 1 means cities i and j are directly connected, count the groups of connected cities . The outer loop makes sure disconnected parts are all found. Each time it meets an unvisited city, that city begins a new component, and one DFS marks the whole component so it is never counted twice. Each city is visited once, and visiting scans a full matrix row of length n, giving n × n work. The visited set and recursion stack are at most n deep. Course Schedule (cycle detection) Given num_courses and prerequisite pairs [a, b] ('to take a you must first take b'), decide whether all courses can be finished. That is, the graph has no cycle . A plain visited set is not enough for directed graphs: reaching a visited node is fine if it was finished earlier, but reaching one that is still on the current path means a cycle. Three states (unvisited, on path, done) capture that. Each vertex is expanded once and each prerequisite edge examined once. The state map and the recursion stack are each at most V in size. Recursion depth is about 1000 in Python. For large or path-like graphs use an expli Flood Fill leetcode 733 Number of Provinces leetcode 547 Clone Graph leetcode 133 All Paths From Source to Target leetcode 797 Path Sum II leetcode 113 Course Schedule leetcode 207 Graph Valid Tree leetcode 261 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. Do I need to visit everything reachable, find groups, detect a cycle, or list paths? connected components / groups / provinces explore all paths from A to B detect a cycle clone or copy a graph root-to-leaf path sum is it possible to reach Given a connected undirected graph, return a deep copy (clone) of it. Visit every node once, and keep a dict from original to copy so each node is cloned exactly once even with cycles. Given a directed acyclic graph, return all possible paths from node 0 to node n − 1. 'All paths' means exhaustive exploration: DFS, carrying the current path and recording it when the target is reached. You need the SHORTEST path in an unweighted graph (use BFS) or the cheapest weighted path (Dijkstra). Clone Graph Enumerating all paths: pass the path down as a new list ( path + [nxt] ) instead of appending and popping, which avoids copy mistakes. Python's default recursion limit (about 1000) makes deep DFS risky. Convert to an explicit stack for graphs that can be path-like. Objects are hashable by identity by default, so a node can be a dict key with no extra code (unless you define __eq__ ). Adjacency list vs adjacency matrix? List: O(V + E) space, fast iteration over neighbours, best for sparse graphs. Matrix: O(V&sup2;) space, O(1) edge lookup, better for dense graphs or small V. Most interview graphs are sparse, so use an adjacency list. How does cycle detection differ in directed and undirected graphs? Undirected: DFS with a visited set; reaching a visited node that is not the current node's parent means a cycle (or use Union-Find). Directed: track three states (unvisited, on the current path, done); reaching a node that is still on the current path is a back edge and means a cycle. Kahn's algorithm is an alternative. Recursive versus iterative DFS: what are the trade-offs? Recursion is shorter and mirrors the problem, but its depth is limited (about 1000 frames by default in Python) and it costs stack space. An explicit stack avoids the limit and gives control over visit order, at the price of more code. Prefer iterative for graphs that can be long paths. How do you clone a graph that may contain cycles? Keep a dictionary from original node to its copy. When you visit a node, create its copy and register it before cloning its neighbours, then recursively clone (or reuse) each neighbour. The map prevents infinite recursion and preserves shared structure. O(V + E). graph start seen visit node return dfs_iterative stack while append find_circle_num is_connected range provinces unvisited city starts group mark everything connected can_finish num_courses prerequisites course state current path fully explored has_cycle true back edge looped onto false already proven safe","dsa/bfs":"Breadth-first search explores a graph in rings, using a queue, so the first time it reaches a node is along a path with the fewest edges. That makes it the tool for shortest paths in unweighted graphs, level-order traversal, 'minimum steps' puzzles, and spreading from several sources at once. Minimum number of steps / moves / minutes when every move costs the same Shortest path in an unweighted graph or grid; nearest gate/zero/exit Level by level processing Spreading from several starting points at the same time (multi-source BFS) An implicit graph of states: one letter changed, one lock wheel turned Mark nodes when you enqueue them, not when you dequeue them. Rotting Oranges (multi-source BFS) In a grid, rotten oranges (2) infect fresh neighbours (1) every minute. Return the minimum minutes until none are fresh, or -1 . Start the BFS from every rotten orange at once. Processing the queue one level at a time counts minutes. If fresh oranges remain when the queue empties, they were unreachable. Each cell is enqueued at most once and examined a constant number of times. The queue can hold a large part of the grid at once. Word Ladder (an implicit graph) Transform begin into end by changing one letter at a time , where every intermediate word must be in a word list. Return the length of the shortest sequence, or 0. Words are nodes and two words are adjacent if they differ by one letter. You never build the graph: generate the neighbours on demand. Because every edge costs one step, BFS finds the shortest ladder. For each of N words and each of L positions, 26 candidate letters are tried, and building each candidate word (slicing and hashing) costs O(L). The seen set and queue hold up to N words of length L. Marking visited when you dequeue lets the same node be queued many Minimum Depth of Binary Tree leetcode 111 Binary Tree Level Order Traversal leetcode 102 Open the Lock leetcode 752 Shortest Path in Binary Matrix leetcode 1091 Rotting Oranges leetcode 994 01 Matrix leetcode 542 Walls and Gates leetcode 286 Word Ladder leetcode 127 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. Is the question 'fewest steps / moves / minutes / edges', or 'level by level', from one or many starting points? minimum number of steps / moves / minutes shortest path in an unweighted graph or grid level order / by distance spreads from several sources at once transform one word/state into another one step at a time nearest zero / nearest gate A combination lock has 4 wheels (0-9) and some dead-end states. Return the minimum number of turns to open it. Each state is a node; one turn is an edge. 'Minimum number of turns' with equal-cost moves is BFS over an implicit graph. Given a binary matrix, return the length of the shortest clear path from top-left to bottom-right, moving in 8 directions. Shortest path on a grid where every step costs the same is BFS. The only twist is the 8 directions. Edges have different costs (use Dijkstra) or you need every path rather than the shortest (DFS or backtracking). Shortest Path in Binary Matrix Use collections.deque for the queue. list.pop(0) is O(n) and turns BFS quadratic. To process one level at a time, loop for _ in range(len(queue)) and count levels outside the loop. Generate neighbours lazily for implicit graphs (word ladder, lock states) instead of building the whole graph. BFS vs DFS: when do you use each? BFS for shortest path in an unweighted graph and level-order problems (it explores by distance). DFS for reachability, connected components, cycle detection, topological sort and exhaustive search. Both are O(V + E). Why does BFS find the shortest path in an unweighted graph, and when does it stop being correct? BFS processes all nodes at distance d before any at distance d + 1, so the first time it reaches a node uses the fewest edges. It stops being correct when edges have different costs: it counts edges, not total weight. Use Dijkstra for non-negative weights, or 0-1 BFS with a deque when weights are only 0 or 1. Why should you mark nodes visited when they are enqueued, not when dequeued? If you mark on dequeue, the same node can be added to the queue several times by different neighbours before it is processed, wasting time and memory (and making the state space blow up in puzzles like Word Ladder). Marking on enqueue guarantees each node enters the queue once. What is multi-source BFS? Give an example. Start the queue with several sources at distance 0 so they expand simultaneously. Rotting Oranges seeds every rotten orange, so each BFS level is one minute for all of them; \"01 Matrix\" and \"Walls and Gates\" find the distance to the nearest source the same way. It is cheaper than running one BFS per source. How could you speed up Word Ladder? Bidirectional BFS: search from both the start and the end word and always expand the smaller fron from collections import deque bfs_distance graph start target queue seen while node dist popleft return mark enqueue append oranges_rotting grid rows cols fresh range rotten oranges together elif minutes level minute else ladder_length begin word_list words word steps abcdefghijklmnopqrstuvwxyz","dsa/matrix-traversal":"A grid is a graph: each cell is a node and its up/down/left/right neighbours are the edges. Learn the direction loop, the bounds check and the visited marking once, and you can solve islands, flood fill, enclosed regions, mazes and boundary tricks. Some matrix problems are about walking the grid in a special order (spiral, rotate) instead. Input is a grid, matrix or board of cells Islands, regions, flood fill, connected cells of the same kind 'Surrounded by', border cells, 'reach the edge' Neighbours are adjacent cells (4 or 8 directions) Spiral order, rotate or transpose in place, word on a board The four-direction neighbour generator with a bounds check. Reuse it in every grid problem. Number of Islands Given a grid of '1' (land) and '0' (water), count the islands : groups of land cells connected horizontally or vertically. Scan every cell. An unvisited land cell starts a new island, and one flood fill marks all of it, so each island is counted once. Every cell is visited at most once by the flood fills and once by the outer scan. The visited set and, for an all-land grid, the recursion stack can each reach rows × cols. Surrounded Regions (start from the border) Capture every region of 'O' that is completely surrounded by 'X' by flipping it to 'X' . Regions touching the border are safe. Searching for enclosed regions directly is awkward. Flip the question: which 'O' cells can escape ? Start a DFS from every border 'O', mark what it reaches as safe, then capture everything else. Working from the boundary inward is a very common grid trick. Each cell is marked at most once and scanned twice. The recursion can be as deep as the number of connected 'O' cells. Check the bounds before indexing the grid. A negative index silently wraps around in Python. Building a grid with  Flood Fill leetcode 733 Number of Islands leetcode 200 Max Area of Island leetcode 695 Surrounded Regions leetcode 130 Pacific Atlantic Water Flow leetcode 417 Spiral Matrix leetcode 54 Rotate Image leetcode 48 Word Search leetcode 79 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. Is the input a grid where each cell relates to its neighbours, or must I walk the grid in a special order? grid / matrix / board islands, regions, flood fill surrounded by / border cells adjacent cells (up, down, left, right) spiral / rotate / transpose word on a board Given an m x n matrix, return all elements in spiral order. This is a traversal order problem, not a search: keep four shrinking boundaries (top, bottom, left, right) and walk the outer ring each round. Rain flows to neighbouring cells with lower or equal height. Find the cells from which water can reach both the Pacific and Atlantic borders. Reverse the flow: start from the border cells and search inward to higher cells. The cells reached from both oceans are the answer. Cells carry weights and you need the cheapest route (Dijkstra), or the grid is only used for repeated rectangle sums (2-D prefix sums). Spiral Matrix Keep the four directions in a tuple and use chained comparison for the bounds check. zip(*matrix) transposes a matrix, and rotating 90 degrees clockwise is a one-liner. Mark visited cells by overwriting them (for example with '#' ) and restore afterwards if the caller needs the grid intact (Word Search). Number of Islands: how do you model and solve it? The grid is an implicit graph with 4-neighbour edges. Scan cells; on an unvisited land cell, flood fill with DFS or BFS, marking cells visited, and increment the count. O(rows cols) time and space. Marking visited by mutating the grid saves the extra set. How do you return the elements of a matrix in spiral order? Maintain four boundaries (top, bottom, left, right). Walk the top row, right column, bottom row and left column in turn, shrinking the corresponding boundary after each side, and stop when the boundaries cross. Guard the bottom and left passes so a single remaining row or column is not visited twice. O(m n) time. How do you rotate an n x n matrix 90 degrees clockwise in place? Transpose the matrix (swap m[i][j] with m[j][i] ), then reverse each row. Both steps are in place, giving O(n&sup2;) time and O(1) extra space. Alternatively rotate four cells at a time layer by layer. In Pacific Atlantic Water Flow, why do you search from the borders instead of from every cell? Searching from every cell repeats work. Reversing the direction, start a DFS/BFS from all Pacific-border cells moving to neighbours with greater or equal height, and do the same from the Atlantic border. The cells reached by both searches are the answer, and each search is O(m n). dirs neighbors rows cols yield num_islands grid return seen islands range flood fill whole island solve board mark_safe safe connected border begin every cell capture elif restore cells","dsa/topological-sort":"A topological order lists the nodes of a directed acyclic graph so that every edge points forward: each task appears after the tasks it depends on. Kahn's algorithm repeatedly removes nodes with no remaining prerequisites; if it cannot remove them all, the graph has a cycle and no valid order exists. Prerequisites, dependencies, build order 'Return a valid order' or 'is it possible to finish everything' Deriving an order from constraints (alien dictionary: letters from sorted words) A directed graph where you need the order, not just reachability Peeling a tree layer by layer from its leaves (minimum height trees) Kahn's algorithm with in-degrees. If the result is shorter than n, there was a cycle. Course Schedule II Return an order in which to take all courses given prerequisite pairs [course, prerequisite] , or an empty list if impossible. This is the template with the edge direction fixed: a prerequisite must come before its course. Start with courses that have no prerequisites, and unlock others as their prerequisite count reaches zero. Each course is enqueued once and each prerequisite edge is relaxed once. The adjacency list stores all E edges. Alien Dictionary (Premium on LeetCode) Given words sorted in an alien language , derive the order of its letters, or return an empty string if the input is inconsistent. Comparing neighbouring words: the first differing letters give one edge. Then it is a topological sort over letters. Two traps: a longer word before its own prefix is invalid, and a cycle means the ordering is contradictory. Building edges reads each adjacent word pair once (bounded by the total characters). Kahn's algorithm is linear in the number of distinct letters U and edges E. Getting the edge direction backwards. Decide whether your edge means 'a bef Course Schedule leetcode 207 Course Schedule II leetcode 210 Find Eventual Safe States leetcode 802 Minimum Height Trees leetcode 310 Parallel Courses leetcode 1136 Alien Dictionary leetcode 269 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. Are there 'A must happen before B' rules, and do I need a valid order or to know whether one exists? prerequisites / dependencies build order / task ordering order of letters (alien dictionary) is it possible to finish all in-degree / directed acyclic graph trimming leaves layer by layer You must build projects where some projects depend on others. Return a valid build order, or report that it is impossible. Dependencies are directed edges. A valid order is a topological sort, and 'impossible' means there is a cycle. Given a sorted list of words in an alien language, derive the order of its letters. Each pair of neighbouring words yields one 'x before y' rule. Then sort the letters topologically. The graph is undirected, or you only need connectivity or components (DFS, BFS or Union-Find). Minimum Height Trees defaultdict(list) for the adjacency list plus an indegree list is the whole setup for Kahn's algorithm. Detect a cycle by comparing the size of the produced order with the node count: shorter means a cycle. Direction matters: for 'to take a you need b ', the edge is b - a . Write the direction in a comment. Compare Kahn's algorithm with the DFS-based topological sort. Kahn's (BFS) repeatedly removes nodes with in-degree 0 and detects cycles when fewer than n nodes are removed. The DFS version emits nodes in post-order and reverses the list, detecting cycles with the on-current-path state. Both are O(V + E); Kahn's is iterative and easy to extend to \"process in layers\". How do you detect that no valid ordering exists? In Kahn's algorithm the produced order has fewer nodes than the graph: the remaining nodes lie on or depend on a cycle. In DFS you meet a node that is still on the current recursion path. Can a graph have more than one valid topological order? How does that affect testing? Yes, whenever two available nodes are independent. Tests should validate the ordering (every edge goes forward) instead of comparing to one specific list, unless the problem guarantees uniqueness. Derive the letter order of an alien language from a sorted word list. For each pair of adjacent words, find the first differing letters x != y and add the edge x - y . If a longer word precedes its own prefix, the input is invalid. Then topologically sort the letters; a cycle means the ordering is contradictory. Time is linear in the total characters. from collections import defaultdict deque topo_order edges edge must come before graph list indegree append queue range order while node popleft prerequisite satisfied return else shorter means cycle find_order num_courses prerequisites course alien_order words comes break cannot join","dsa/union-find":"Union-Find (disjoint set union) tracks which elements belong to the same group. It merges two groups and answers 'are these two connected?' in near-constant time using path compression and union by size. It shines when connections arrive one at a time. 'Are X and Y connected / in the same group?', asked repeatedly Merge accounts, friends, sets, equivalent items Counting components as edges are added; 'redundant connection' (the edge that closes a cycle) Minimum spanning tree (Kruskal's algorithm) Equality constraints such as a == b and b != c The standard DSU with path compression and union by size. Redundant Connection A tree had one extra edge added. Given the edges in order, return the edge that creates a cycle . Add edges one by one. An edge whose endpoints are already in the same group would close a cycle, which is exactly the redundant one. union returns False in that case. Each edge costs one union, which is near-constant amortised (α is the inverse Ackermann function, below 5 for any real input). The parent and size arrays hold one entry per vertex. Satisfiability of Equality Equations Given equations like \"a==b\" and \"b!=c\" , decide whether all can be true at once. Equality is transitive, so the == equations form groups. A != equation is violated exactly when its two letters ended up in the same group. Do all unions first, then all checks. Each equation triggers a constant number of find operations on a 26-element structure. The parent array has a fixed size of 26. Forgetting path compression or union by size, which can make find O(n) on a long chain. Comparing parent[a] == parent[b] instead of find(a) == find(b) . Parents are not roots until compressed. Using Union-Find when a single DFS/BFS is enough. If the graph is static and you only need components once, D Number of Provinces leetcode 547 Redundant Connection leetcode 684 Satisfiability of Equality Equations leetcode 990 Accounts Merge leetcode 721 Min Cost to Connect All Points leetcode 1584 Number of Connected Components in an Undirected Graph leetcode 323 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. Are things being merged into groups, and do I keep asking whether two things are already in the same group? are X and Y connected merge accounts / friends / sets number of components as edges are added redundant connection / cycle in an undirected graph minimum spanning tree equality constraints (a == b) Given a list of accounts (a name and emails), merge the accounts that share any email. 'Merge things that share something' is grouping. Union every email of an account together, then collect emails by group root. You add edges one at a time to a graph that started as a tree. Return the edge that creates a cycle. If both endpoints of a new edge are already in the same group, that edge closes a cycle. Union-Find answers it in near-constant time. The graph is fixed and you only need its components once (a single DFS or BFS is simpler), or you need shortest paths. Min Cost to Connect All Points (Kruskal + Union-Find) Iterative find with path halving avoids recursion limits: parent[x] = parent[parent[x]] . To gather group members, bucket by root: groups[find(x)].append(x) with a defaultdict(list) . Sorting tuples (cost, u, v) orders edges by cost first, which is what Kruskal needs. What is Union-Find and why are path compression and union by rank used? A disjoint-set structure with find (representative of a set) and union (merge sets). Path compression flattens trees during find ; union by rank/size keeps them shallow. Together they give near-constant amortised time, O( (n)) per operation. Kruskal vs Prim for a minimum spanning tree? Kruskal sorts edges by weight and adds each edge that does not create a cycle (checked with Union-Find): O(E log E). Prim grows one tree from a start node by repeatedly adding the cheapest crossing edge using a heap: O((V + E) log V). Kruskal suits sparse graphs given as edge lists. Accounts Merge: describe the approach. Give each email an id and union all emails that appear in the same account (for example union each email with the account's first email). Then group emails by their root, sort each group, and attach the account name. Near-linear time using path compression and union by size. Detect a cycle in an undirected graph: Union-Find versus DFS? Both are near-linear. With Union-Find, process edges one at a time; an edge whose endpoints already share a root closes a cycle, which also works when edges arrive online. DFS needs the whole graph and must ignore the edge back to the parent. class __init__ self parent list range size components find while path compression halving return union false already connected attach smaller tree true find_redundant_connection edges were equations_possible equations first everything equal then check every inequality","dsa/shortest-path":"When edges have different costs, BFS is no longer enough. Dijkstra's algorithm keeps a min-heap of (distance, node), repeatedly finalises the closest unvisited node and relaxes its neighbours. It needs non-negative weights; Bellman-Ford handles negative weights and 'at most k edges' limits. Cheapest / minimum cost / fastest route with weighted edges 'Network delay time', travel time, price, distance 'At most k stops' (Bellman-Ford or Dijkstra over a state) Variants: minimise the largest step (minimum effort) or maximise a probability A grid where cells have different costs Dijkstra with a min-heap. Skip stale heap entries whose distance is already worse. Network Delay Time A signal is sent from node k . Given directed weighted edges (u, v, w) , return the time for all n nodes to receive it, or -1 if impossible. Pop the closest unvisited node, lock in its distance, and push its neighbours with updated distances. The signal reaches everyone when the farthest node is reached, so the answer is the maximum of the shortest distances. Each edge can add at most one heap entry, and each heap operation costs O(log V). The adjacency list stores E edges and the distance map up to V entries. Path With Minimum Effort (a Dijkstra variant) In a grid of heights, the effort of a route is the largest absolute height difference between consecutive cells. Return the smallest possible effort from top-left to bottom-right. The structure is identical to Dijkstra, but the cost of a path is the maximum edge instead of the sum. That still works because extending a path never lowers its cost, so the smallest-cost-first order stays valid. Each cell can be pushed a small constant number of times (once per improving neighbour), and each heap operation costs a logarithm of the heap size. Dijkstra requ Network Delay Time leetcode 743 Path With Minimum Effort leetcode 1631 Path with Maximum Probability leetcode 1514 Cheapest Flights Within K Stops leetcode 787 The Maze II leetcode 505 Swim in Rising Water leetcode 778 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. Do edges have different costs (time, price, distance, effort) and do I need the cheapest total, or the smallest worst step? cheapest / minimum cost / minimum time weighted edges network delay / signal at most k stops minimum effort or maximum probability variants a grid where cells have costs Flights have prices. Find the cheapest price from src to dst with at most k stops. Weighted shortest path with a hop limit: Bellman-Ford for k + 1 rounds, or Dijkstra over the state (city, stops used). Each edge has a success probability. Find the path from start to end with the maximum probability of success. Path value is a product that only shrinks as you extend it, so Dijkstra with a max-heap (negated probabilities) applies. All edges cost the same (plain BFS is enough), or weights can be negative (Bellman-Ford). Cheapest Flights Within K Stops (Bellman-Ford) Push (distance, node) tuples into heapq . When you pop, skip entries that are already worse than the best known distance. Use float('inf') (or math.inf ) for 'unreachable'. Remember inf + 1 is still inf . For 'maximum probability', push negated values into the min-heap and multiply along the path. How does Dijkstra's algorithm work, and what are its limits? Repeatedly pop the unvisited node with the smallest known distance from a min-heap, finalise it, and relax its outgoing edges. With a binary heap it is O((V + E) log V). It requires non-negative edge weights; with negative weights use Bellman-Ford (O(VE)). Pick the shortest-path algorithm: BFS, Dijkstra or Bellman-Ford? Unweighted (or all weights equal): BFS, O(V + E). Non-negative weights: Dijkstra. Negative weights or need negative-cycle detection: Bellman-Ford. Cheapest Flights Within K Stops: how do you solve it? Bellman-Ford limited to k + 1 rounds: keep a distance array and, in each round, relax every edge using a copy of the previous round's distances so a round adds at most one flight. O(k E). Alternatively run Dijkstra over states (city, stops used), pruning states that exceed the stop limit. Why does Dijkstra fail with negative edge weights? Give an example. Dijkstra finalises a node the first time it is popped, assuming no later path can be shorter. A negative edge can make a longer-looking path cheaper: with edges A-B 2, A-C 3 and C-B -2, B is finalised at 2 but the true shortest distance is 1. Use Bellman-Ford instead. import heapq dijkstra graph start dist heap while node heappop float continue stale entry shorter path found already neighbor weight heappush return from collections defaultdict network_delay_time times list append first time popped shortest distance values else minimum_effort_path heights rows cols best range effort step cost worst","dsa/backtracking":"Systematically explore all candidate solutions by building them incrementally, abandoning a partial candidate the moment it can't possibly lead to a valid solution. It's DFS over a decision tree with an explicit undo step. 'Generate all...' / 'find all possible...' language: subsets, permutations, combinations Constraint satisfaction problems: N-Queens, Sudoku Need to explore a decision tree and prune invalid branches early The choose explore unchoose shape The generic choose/explore/unchoose skeleton. Subsets Given an array of unique integers, return all possible subsets (the power set). Every recursive call first records the current path as a valid subset (including the empty one), then tries extending it with each remaining element. Passing start forward prevents reusing earlier elements, which avoids generating duplicate subsets. There are exactly 2^n subsets of an n-element set, and the algorithm generates all of them; copying each subset into the result costs up to O(n), giving O(n * 2^n) total. The recursion depth and the path array are both bounded by n, giving O(n) auxiliary space excluding the output. Combination Sum Given distinct candidates and a target, return all unique combinations that sum to target. A candidate may be reused an unlimited number of times. Because a number can be reused, the recursive call passes i (not i+1) so the same element stays eligible. The 'remaining < 0' check is the prune — it cuts off a branch the moment it overshoots, instead of exploring further down a doomed path. In the worst case (e.g. candidates = [1] and a large target), the number of valid combinations — and therefore recursive branches explored — grows exponentially with target. The recursion depth is bounded by target divided by the smallest candidate, which is the O( Subsets leetcode 78 Subsets II leetcode 90 Permutations leetcode 46 Combination Sum leetcode 39 Combination Sum II leetcode 40 Letter Combinations of a Phone Number leetcode 17 Palindrome Partitioning leetcode 131 Word Search leetcode 79 N-Queens leetcode 51 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. Must I generate (or count) every arrangement or choice sequence that satisfies some rules? all possible / all valid / generate every subsets, permutations, combinations partition a string or array into parts place N queens / solve sudoku find all paths n is tiny (about 15 to 20) Generate all combinations of well-formed parentheses for n pairs. 'Generate all' with a validity rule is backtracking. Add '(' while open n and ')' while close open; the rule prunes invalid branches early. Partition a string so every substring is a palindrome; return all partitions. At each position choose the next cut, keep it only if the piece is a palindrome, and recurse on the rest (choose, explore, unchoose). You only need a count or an optimum and the same state is reached repeatedly (memoise or use DP). Permutations Append a copy of the path: result.append(path[:]) . Appending path itself stores a reference that ends up empty. itertools.permutations/combinations/product give you a quick brute force to compare against your backtracking output. Nested helper functions can read the outer lists, but to rebind a counter you need nonlocal (or keep it in a list). Describe the backtracking template. Choose, explore, unchoose: append a choice to the current path, recurse, then remove it. Record a solution at the base case. Add pruning checks before recursing so invalid branches are abandoned early. Compare the sizes of subsets, permutations and combinations output. Subsets of n items: 2 n . Permutations: n!. Combinations of k from n: C(n, k). Because the output itself is that large, the running time is exponential or factorial no matter how clever the code is; pruning only improves constants. How do you avoid duplicate results when the input has duplicates? Sort the input, then at each recursion level skip a value equal to the previous one that was already tried at that level ( if i start and nums[i] == nums[i-1]: continue ). For permutations, use a used-array and the same skip rule. Backtracking vs dynamic programming: how do you choose? If you must enumerate all solutions, use backtracking. If you only need a count or an optimum and subproblems overlap (the same state is reached by different paths), memoise or use DP. Backtracking with memoisation on the state is effectively top-down DP. backtrack path choices is_solution result append return choice is_valid continue next_choices undo subsets nums start range combination_sum candidates target remaining reuse allowed","dsa/greedy":"Make the locally optimal choice at each step and never look back, trusting that local optimality composes into a global optimum. Only valid when the problem has the greedy-choice property and optimal substructure — otherwise it silently gives a wrong answer, unlike DP, which always considers every option. 'Maximum/minimum number of X' with a simple, provable local rule Interval scheduling (earliest finish time first) Sorting by some criterion, then a single linear scan, gives the answer Warning sign: if you can't prove the local choice is always safe, it's probably not actually a greedy problem Jump Game: can you reach the last index, greedily tracking the farthest reachable index. Gas Station Given gas[i] and cost[i] around a circular route, return the starting station index from which you can complete the whole circuit, or -1 if impossible. Guaranteed unique if it exists. If total gas is at least total cost overall, a valid start is guaranteed to exist. Greedily: whenever the running tank goes negative starting from the current candidate, none of the stations between the old start and i could have been valid starts either — so jump the candidate straight to i+1 and reset. The array is scanned exactly once, accumulating the running total and candidate start in the same pass, so total work is O(n). Only a few running variables are tracked, none of which grow with n, giving O(1) space. Jump Game II Given an array where nums[i] is the max jump length from index i, return the minimum number of jumps to reach the last index. This is a greedy BFS-by-levels in disguise. curr_end marks the boundary of the current jump's reach; when i reaches that boundary, another jump is forced, and the next boundary becomes the farthest index reachable from anywhere within the current range. Assign Cookies leetcode 455 Maximum Subarray leetcode 53 Jump Game leetcode 55 Jump Game II leetcode 45 Gas Station leetcode 134 Partition Labels leetcode 763 Hand of Straights leetcode 846 Task Scheduler leetcode 621 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. Is there a rule I can justify (earliest end, largest first, closest deadline) that never needs to be undone? minimum number of ... to cover / remove / schedule maximum number of non-overlapping can you reach the end / minimum jumps earliest deadline / best ratio first always take the best local option split so each letter appears in one part Each child has a greed factor and each cookie a size. Maximise the number of content children. Sort both. Give the smallest cookie that satisfies the least greedy child. The local choice never hurts later matches. Split a string into as many parts as possible so that each letter appears in at most one part. Return the part sizes. For each letter, know its last occurrence and extend the current part to it. When the scan reaches that boundary, cut. A locally best choice can be wrong later (coin change with awkward denominations). Use DP. Partition Labels A dict comprehension over enumerate keeps the last index of each key automatically. Sort by several keys with a tuple. Negate a number to sort that part descending. sorted is stable. Track a running best with max(end, x) . Off-by-one errors are the usual bug, so write end - start + 1 for inclusive sizes. How do you know a greedy strategy is correct? Prove the greedy-choice property, typically with an exchange argument (any optimal solution can be transformed to use the greedy choice without getting worse) plus optimal substructure. Try small counterexamples: greedy fails for coin change with coins [1, 3, 4] and amount 6 (greedy gives 4+1+1, optimal is 3+3). Which greedy rule solves interval scheduling (max non-overlapping intervals)? Sort by earliest finish time and repeatedly take the interval that ends first and does not overlap the last chosen one. Sorting by start time or by shortest duration is not correct. Jump Game: greedy solution? Track the farthest reachable index while scanning; if the current index exceeds it, return false. O(n) time, O(1) space. For the minimum number of jumps, track the end of the current jump range and increment a counter when you reach it. Gas Station: why can you skip ahead after a failure? If starting at s the tank first goes negative at i , no start between s and i can work either (they would arrive at each point with no more fuel than starting at s ). So restart at i + 1 . A solution exists only if total gas total cost. O(n). can_jump nums max_reach enumerate return false true can_complete_circuit cost total start range jump jumps curr_end farthest","dsa/dp-1d":"Break a problem into overlapping subproblems indexed by a single variable (position, amount, or length), and cache results to avoid recomputation. Recognize it by noticing brute-force recursion would re-solve the same subproblem over and over. Optimal substructure: the answer at position i depends on answers at smaller positions Brute-force recursion would re-solve identical subproblems (overlapping subproblems) Keywords: 'number of ways', 'minimum/maximum cost to reach', 'can you reach...' State can be described by a single index: day, amount, or position in a string Top-down memoization. Bottom-up (tabulation) does the same thing iteratively, and often gets O(1) space if only the last few states matter. House Robber Given nums representing money in houses arranged in a row, you cannot rob two adjacent houses. Return the maximum money you can rob. curr represents the best result including house i; prev is the best result up through house i-1. At each house you either skip it (keep curr) or rob it (prev + n). Rolling two variables instead of a full array gives O(1) space. The array is scanned once, with O(1) work updating the two rolling variables per house, giving O(n) time. Because only the previous two states are ever needed, no array proportional to n is allocated, giving O(1) space. Coin Change Given coin denominations and a target amount, return the fewest number of coins needed to make that amount, or -1 if it can't be made. dp[a] holds the minimum coins to make amount a. For each amount, try every coin as 'the last coin used' and build on the best result for the smaller amount already solved. For each of the `amount` possible sub-amounts, the algorithm tries every one of the `len(coins)` denominations, giving amount * len(coins) total operations. The dp array ho Climbing Stairs leetcode 70 Min Cost Climbing Stairs leetcode 746 House Robber leetcode 198 House Robber II leetcode 213 Coin Change leetcode 322 Word Break leetcode 139 Decode Ways leetcode 91 Longest Increasing Subsequence leetcode 300 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. Is the answer at position i built from answers at earlier positions (count the ways, minimise, maximise, or 'is it possible')? number of ways / how many ways minimum or maximum cost to reach can you reach / is it possible to form longest increasing subsequence no two adjacent (rob, pick) break a string into words / decode ways You can climb 1 or 2 steps at a time. In how many distinct ways can you reach step n? 'Number of ways' where each answer depends on the previous two is 1-D DP: ways[i] = ways[i-1] + ways[i-2]. A digit string maps 1 → A … 26 → Z. Return the number of ways to decode it. 'Number of ways' along a string, where each position depends on the last one or two digits, is 1-D DP over the index. A locally optimal choice is provably safe (greedy), or you must list every solution (backtracking). Word Break @lru_cache(maxsize=None) turns a plain recursive solution into memoised top-down DP. Arguments must be hashable. Initialise tables with the right sentinel: [0] * (n + 1) for counts, [float('inf')] * (n + 1) for minimums, [False] * (n + 1) for feasibility. When only the last one or two states matter, roll them in variables: a, b = b, a + b . How do you recognise a dynamic programming problem, and what is the difference between top-down and bottom-up? Look for overlapping subproblems and optimal substructure, with keywords like \"number of ways\", \"minimum/maximum cost\", \"can you reach\". Top-down is recursion plus memoisation (easy to write, computes only needed states). Bottom-up fills a table in dependency order (no recursion, easier space optimisation). House Robber: give the recurrence and the space optimisation. dp[i] = max(dp[i-1], dp[i-2] + nums[i]) . Only the last two values are needed, so keep two variables: O(n) time, O(1) space. Coin Change: how does \"minimum coins\" differ from \"number of ways\"? Minimum coins: dp[a] = min(dp[a - c] + 1) over coins, initialised to infinity. Number of ways (combinations): iterate coins in the outer loop and amounts inner loop so each combination is counted once, dp[a] += dp[a - c] . Swapping loop order counts permutations. Both are O(amount coins). Longest Increasing Subsequence: what are the two standard solutions? DP in O(n&sup2;): dp[i] = 1 + max(dp[j]) for j i with nums[j] nums[i] . Patience-sorting with binary search in O(n log n): maintain the smallest tail of an increasing subsequence for each length and use bisect_left to place each element. fib_memo memo return nums prev curr coin_change coins amount float range else","dsa/dp-2d":"State depends on two indices — two positions in a string, a grid cell, or an item index plus remaining capacity. Same overlapping-subproblems idea as 1-D DP, but the table is a 2D grid. Two strings being compared (edit distance, longest common subsequence) Grid path problems (unique paths, minimum path sum) Knapsack-style problems: item index + remaining capacity dp[i][j] depends on dp[i-1][j], dp[i][j-1], and/or dp[i-1][j-1] The classic grid-path DP shape. Longest Common Subsequence Given two strings text1 and text2, return the length of their longest common subsequence. dp[i][j] is the LCS length of text1[:i] and text2[:j]. A matching character extends the diagonal's LCS by one; a mismatch carries forward the best result from dropping one character off either string. Every cell (i, j) of the m x n table is filled exactly once with O(1) work, giving O(m * n) time. The table itself has (m+1) * (n+1) cells, giving O(m * n) space (reducible to O(min(m, n)) by only keeping the previous row alive). Edit Distance Given two words, find the minimum number of operations (insert, delete, replace) to convert word1 into word2. The base row/column represent converting a string to or from empty (i or j operations). Otherwise take the cheapest of delete, insert, or replace, each expressed as one step from an already-solved smaller subproblem. Same structure as LCS: the (m+1) x (n+1) table is filled cell by cell with O(1) work per cell (one comparison and a min of three lookups), giving O(m * n) time and space. Unique Paths leetcode 62 Unique Paths II leetcode 63 Minimum Path Sum leetcode 64 Longest Common Subsequence leetcode 1143 Edit Distance leetcode 72 Partition Equal Subset Sum leetcode 416 Coin Change II leetcode 518 Target Sum leetcode 494 Interleaving String leetcode 97 Tips and tricks: spot it, then apply it. Trigger words, disguised problems, Python tips, keywords for this pattern. Do I have two sequences or two indices, a grid, or a choice of item combined with a remaining capacity? two strings or sequences edit distance / longest common subsequence grid paths, with or without obstacles knapsack / subset sum / partition capacity or budget interleaving / matching patterns Given an m x n grid with obstacles, count the unique paths from top-left to bottom-right moving only right or down. A grid with moves that depend on the cell above and the cell to the left is 2-D DP: paths[r][c] = paths[r-1][c] + paths[r][c-1], and 0 on obstacles. Can you partition an array into two subsets with equal sum? It reduces to 'subset sum equals total/2', a 0/1 knapsack: item index × reachable sum. The state needs only one index (1-D DP) or the objective is locally decidable (greedy). Partition Equal Subset Sum (bitset trick) Build 2-D tables with a comprehension. [[0] * n] * m repeats one row m times. For 0/1 knapsack in one dimension, loop the capacity downwards so each item is used at most once. Big integers are bitsets: bits |= bits x and bits t 1 . It is a Python-specific speed-up that would overflow in Java. Longest Common Subsequence: state the recurrence. dp[i][j] is the LCS of the first i and j characters. If a[i-1] == b[j-1] then dp[i-1][j-1] + 1 , else max(dp[i-1][j], dp[i][j-1]) . O(mn) time; O(min(m, n)) space by keeping one row. Edit Distance: what are the transitions and base cases? If characters match, take dp[i-1][j-1] ; otherwise 1 + min(insert, delete, replace) = 1 + min(dp[i][j-1], dp[i-1][j], dp[i-1][j-1]) . Base cases: dp[i][0] = i and dp[0][j] = j . O(mn). 0/1 Knapsack: how do you reduce space to one dimension? Use a 1-D array over capacity and iterate capacity downwards for each item so each item is used at most once: dp[w] = max(dp[w], dp[w - wt] + val) . Iterating upwards would allow reuse (unbounded knapsack). O(nW) time, O(W) space. Unique Paths with obstacles: how does it change? Set dp to 0 at obstacle cells; otherwise dp[r][c] = dp[r-1][c] + dp[r][c-1] , with the first row and column limited by the first obstacle. It can be rolled into one row for O(cols) space. min_path_sum grid rows cols range elif else return longest_common_subsequence text1 text2 min_distance word1 word2","dsa/combining-patterns":"Real problems rarely use one pattern in isolation. A common shape is a 'wrapper' pattern that organises the search (binary search, DFS, sliding window) around a smaller 'engine' (a greedy check, memoisation, a hash map, a trie). This lesson shows how to spot when one pattern leaves a sub-problem that another one solves. The first pattern gives you a search space, but you still need a feasibility check (binary search + greedy) A DFS revisits the same states over and over (DFS + memoisation) Intervals or ranges combined with running counts (intervals + prefix sums / a difference array) A window whose validity depends on counts (sliding window + frequency counting) Many words to find in a grid (trie + backtracking) Binary search over the answer, with a problem-specific feasibility check. Many hard problems reduce to writing feasible . Binary Search + Greedy: Capacity To Ship Packages Within D Days Packages must ship in order . Find the least ship capacity that delivers everything within days . If capacity c works, any larger capacity works too, so the answer space is monotonic: binary search it. Each probe needs a check, and the check is a simple greedy loop. Two patterns, each trivial on its own. The binary search takes about log(sum − max) probes, and each probe is one linear pass over the packages. DFS + Memoisation: Longest Increasing Path in a Matrix Find the length of the longest strictly increasing path in a grid, moving in four directions. A path only moves to larger values, so it can never cycle. That means no visited set is needed, and the answer from each cell depends only on that cell. Memoising it turns exponential DFS into one visit per cell. Each cell's result is computed once and cached, with a constant amount of work (four neighbours) per cell. The cache a Capacity To Ship Packages Within D Days leetcode 1011 Car Pooling leetcode 1094 Longest Repeating Character Replacement leetcode 424 Split Array Largest Sum leetcode 410 Longest Increasing Path in a Matrix leetcode 329 Word Search II leetcode 212  What are the signs of a \"binary search on the answer\" problem? The question asks for the minimum or maximum value of something (capacity, speed, days, largest sum) subject to a constraint, the answer lies in a numeric range, and you can write a function feasible(x) that is monotonic: if x works, then every larger (or smaller) x works. Then search the range and use the check as the comparator. How do you convince an interviewer that your feasibility function is monotonic? State the argument directly: for example, if a ship of capacity c can deliver everything in D days, a larger capacity can too, because any packing valid for c is still valid. Then state the search bounds (lower bound the heaviest single item, upper bound the total) and the cost, O(log range times the check). Split Array Largest Sum: outline the solution. Binary search the largest allowed part sum between max(nums) and sum(nums). The greedy check walks the array, starting a new part whenever adding the next element would exceed the limit, and returns whether the number of parts is at most m. The smallest feasible limit is the answer. O(n log sum). When combining patterns, how do you reason about total complexity? Multiply the cost of the outer pattern by the cost of its inner engine: binary search iterations times a linear check, DFS states times work per state (with memoisation, distinct states times work each), trie-pruned backtracking bounded by word lengths. State the pieces separately, then combine them. min_feasible feasible smallest value which true monotonic while works smaller else fails need larger return ship_within_days weights days capacity greedy check pack much fits each used load answer lies between heaviest package everything once from functools import lru_cache longest_increasing_path matrix rows cols maxsize none longest path starting best range car_pooling trips delta difference array over road positions passengers start they here counted change running gives position false collec","patterns/patterns-overview":"Design patterns are named, reusable solutions to recurring design problems. Many were shaped by Java/C++ constraints (no first-class functions, rigid class hierarchies). In Python, some patterns collapse into language features and others stay useful. Knowing which is which is the skill. Singleton : Java needs a private constructor and a static getter. Python: a module is already a singleton; a class-based one is rarely needed. Strategy / Command : Java needs an interface plus classes. Python: pass a function (functions are objects). Iterator : Java needs Iterator classes. Python: generators and the for protocol are built in. Decorator : Java wraps objects with the same interface. Python has the pattern and the @decorator syntax for functions (two different things, covered later). Factory, Builder, Adapter, Facade, Proxy, State, Observer, DI : still useful in Python, usually lighter (dicts of callables, dataclasses, duck typing). Interfaces: Python has no interface keyword. Duck typing, typing.Protocol , or abc.ABC fill the role. Patterns are grouped by the problem they solve. This is the vocabulary you'll use when discussing design with a customer's engineers. Strategy collapses into a function In Java, swapping a discount algorithm needs an interface and one class per algorithm. In Python you can pass the algorithm itself. The Strategy pattern's whole purpose is 'make the algorithm swappable'. Since functions are first-class objects, the parameter is the strategy. No interface, no class hierarchy. Duck typing replaces a shared interface Two unrelated classes work with the same function because both have a .send() method. Many pattern diagrams contain an abstract base class that exists only to satisfy a static type system. Python doesn't require it. When you do want a c   What are design patterns, and what are the three main categories? Named, reusable solutions to recurring design problems. Creational patterns handle object creation (Singleton, Factory, Builder); structural patterns handle composition (Adapter, Decorator, Facade, Proxy); behavioural patterns handle interaction and responsibility (Strategy, Observer, Command, State). Which classic patterns are less necessary in Python, and why? Strategy and Command shrink to passing a callable, because functions are first-class. Singleton is usually a module-level object, since modules are cached. Iterator is built in via generators. Interfaces are replaced by duck typing or typing.Protocol . The problems remain; the ceremony does not. How do you decide whether to apply a pattern? Start from a concrete pain: duplicated code, growing if/elif chains, tight coupling to a vendor, or hard-to-test code. Apply the simplest pattern that removes it, and avoid patterns \"just in case\", which adds indirection without benefit. Be ready to explain the trade-off. What does \"favour composition over inheritance\" mean? Build behaviour by holding collaborator objects (has-a) rather than inheriting from a base class (is-a). Composition is more flexible (swap parts at runtime), avoids deep hierarchies and the fragile-base-class problem, and is the mechanism behind Strategy, Decorator, Adapter and Proxy. creational objects made singleton factory builder structural composed adapter decorator facade proxy behavioral communicate share responsibility strategy observer command state architecture level idea that leans above dependency injection no_discount price return black_friday checkout discount just callable round print class emailnotifier send self email slacknotifier slack alert notifier declared interface needed deploy done","patterns/singleton":"Singleton guarantees a class has exactly one instance and gives global access to it. Typical uses: configuration, a connection pool, a logger. It is also the most overused pattern, because it is global state with a nicer name. Use when exactly one shared instance is genuinely required: app config, a shared client/pool, a registry. Java: private constructor + static getInstance() + thread-safety concerns. Python: the simplest singleton is a module-level object . A class-based singleton is possible via __new__ , a decorator, or a metaclass. Reach for these only if you need lazy creation or subclassing. Singletons make testing hard (hidden shared state). Prefer passing the instance in (see Dependency Injection). Create the object once at module level; every importer gets the same one. Class-based Singleton with __new__ When you need the class form (for example, lazy creation), override __new__ to return the same instance each time. __new__ runs before __init__ and controls object creation. Returning the cached instance means every Registry() call yields the same object. Note that __init__ would run again on each call, which is why the state is set inside __new__ here. Thread-safe lazy singleton Two threads calling the constructor at the same moment could each create an instance. Guard creation with a lock. This is double-checked locking: the cheap check avoids taking the lock on every call, and the second check inside the lock prevents two threads that both passed the first check from each creating an instance. Hidden global state: tests that mutate a singleton leak into other tests. Provide a way to reset it, or inject it instead. With the __new__ form, __init__ re-runs on every Registry() call and can wipe state. Initialize in __new__ or guard __init__ . Multiprocessing:   How do you implement a Singleton in Python? Simplest: a module-level instance that everyone imports. Class-based options: override __new__ to cache the instance, a class decorator, or a metaclass. Choose the class forms only if you need lazy creation or a class-based API. How do you make a lazy Singleton thread-safe? Guard creation with a lock and use double-checked locking: check for the instance, acquire the lock, check again, then create. The first check keeps the fast path lock-free; the second stops two threads from both creating one. Why is Singleton often considered an anti-pattern? It is global mutable state: it hides dependencies, couples code to a concrete class, makes unit tests interfere with each other, and complicates configuration. Prefer passing the shared object in (dependency injection) and letting the composition root create a single instance. With multiprocessing, how many Singleton instances exist? One per process, since each process has its own memory. A true cross-process single instance needs an external coordination point such as a database, a lock file, or a service. config class _config __init__ self settings created once when module first imported elsewhere from import registry _instance none __new__ super items return print true same object threading client _lock lock fast path with check inside","patterns/factory":"A factory centralizes object creation so calling code asks for 'a thing of kind X' without knowing which concrete class to build. It removes big if/elif chains from callers and gives you one place to add new kinds. Use when the concrete class depends on runtime input (a config value, a file type, a customer's system) and callers shouldn't care. Java: Factory Method needs subclasses, Abstract Factory needs families of interfaces. Python: a function returning an instance , or a dict mapping names to classes , is usually enough. Classes are first-class objects in Python: you can store them in a dict and call them. A @classmethod alternative constructor ( from_json , from_env ) is the Pythonic 'named factory'. A dict from name to class. Adding a new kind means adding one entry. Factory choosing a connector by customer config An FDE often supports several source systems behind one interface. The caller only knows the config string. Calling code depends only on the fetch() contract. Supporting a third system means adding one class and one dict entry; no caller changes. This is the Factory Method idea without a class hierarchy. Alternative constructors with @classmethod Python has one __init__ (no overloading), so named class methods act as extra 'constructors'. This replaces Java's overloaded constructors and static factory methods. Because cls is passed in, subclasses that inherit these methods get instances of themselves. Unknown keys: a bare PARSERS[kind] raises KeyError. Convert to a clear error listing valid options. Over-engineering: a factory for a single concrete class adds indirection with no benefit. Don't build the factory as a class with one method; a function is enough. Add an xml parser to the registry and confirm no calling code changes. Use a decorator to auto   Compare Simple Factory, Factory Method and Abstract Factory. Simple factory : one function/class that returns an object by type; not a GoF pattern. Factory Method : a base class defines a creation method that subclasses override. Abstract Factory : an interface for creating families of related objects (for example UI widgets for one platform). What problem does a factory solve? It centralises the decision of which concrete class to create, so callers depend on an abstraction and adding a new kind does not require editing callers (open/closed principle). It removes scattered if/elif chains that choose classes. Show an idiomatic Python factory. PARSERS = {\"csv\": CsvParser, \"json\": JsonParser} def make_parser(kind): try: return PARSERS[kind]() except KeyError: raise ValueError(f\"unknown parser: {kind}\") Classes are objects, so a dict of name to class is the whole factory. Python has one __init__ . How do you provide alternative ways to construct an object? Use @classmethod constructors such as Job.from_json(text) or Job.from_env() . Because they receive cls , subclasses that inherit them return instances of themselves. class csvparser parse self text return line split splitlines pipeparser parsers pipe make_parser kind except keyerror raise valueerror unknown parser postgressource fetch salesforcesource record sources postgres salesforce make_source config type source print import json __init__ name retries classmethod from_json data loads default nightly sync export","patterns/builder":"Builder constructs a complex object step by step, so callers don't need giant constructors or telescoping overloads. Requests, queries, reports, and configuration objects with many optional parts are typical candidates. Use when an object has many optional parts, or must be validated before it's considered complete. Java: nested Builder class, fluent setters, build() . Python: keyword arguments with defaults cover most cases already. A frozen @dataclass plus dataclasses.replace() gives immutable 'builder-like' updates. Use a real builder when construction is multi-step, order-dependent, or needs validation at build() time (for example, assembling an HTTP request or SQL query). Before writing a Builder class, check whether this is enough. Fluent builder with validation Assemble a query step by step and validate it when build() is called. Each method mutates the builder and returns self , enabling chaining. build() is the single place to validate and produce the final value. (Note: this demo interpolates raw strings. Real code must use parameterized queries to avoid SQL injection.) Building an immutable result Keep the builder mutable but have build() return a frozen object so the product can't be changed afterwards. Converting the list to a tuple at build() means later changes to the builder can't corrupt an already-built report. Using a builder where keyword arguments would do. Extra classes with no extra value are noise. Reusing one builder for two products: state from the first leaks into the second. Create a new builder or reset. String-built SQL is injection-prone. Builders that emit SQL should produce parameterized queries. Change QueryBuilder to return (sql, params) with placeholders instead of interpolated values. Add an order_by() step and validate that the colu   What problem does Builder solve, and do you need it in Python? It avoids telescoping constructors and lets you assemble a complex object step by step, validating at the end. In Python, keyword arguments with defaults and a (frozen) dataclass cover most cases; use a real builder for multi-step, order-dependent, or validated construction such as building a request or query. What is a fluent interface? Methods that return self so calls chain: QueryBuilder(\"t\").where(...).limit(10).build() . It reads well but hides state changes, so keep the builder's scope small and make build() return an immutable result. How do frozen dataclasses give you builder-like behaviour? dataclasses.replace(obj, timeout=60) returns a modified copy of an immutable instance, so you can derive variants from a base configuration without mutating shared state. Builder vs Factory? A factory decides which object to create and usually returns it in one call. A builder constructs one complex object in stages. They can be combined: a factory returns a pre-configured builder. from dataclasses import dataclass replace frozen true class request method timeout float retries base https example items slow object untouched print querybuilder __init__ self table _table _where _limit none where cond append return calls chain limit build raise valueerror must positive select join orders status open total report title sections tuple reportbuilder text immutable snapshot intro numbers","patterns/adapter":"Adapter wraps an existing class so it exposes the interface your code expects. It is the daily bread of integration work: a vendor SDK, a legacy client, or a third-party API almost never matches your internal interface. Use when you must use a class you can't change (vendor SDK, legacy module) but it has the wrong method names or shapes. Java: adapter class implementing the target interface. Python: a small wrapper class (composition) or even a function; duck typing means it just needs the right method names. Prefer composition (hold the adaptee) over inheriting from it, so you control the surface area. Keeping adapters at the edge of your system isolates vendor quirks from your business logic. Your code expects .get_customer(id) ; the vendor client offers .fetch(kind, key) . Swapping vendors behind one interface Two payment providers with different APIs, one internal charge(amount_cents) . checkout depends only on charge(cents) . Each adapter absorbs its provider's units (cents vs dollars), names, and success signals, so a customer changing provider touches one adapter. Adapter as a function When only one method is needed, a function is a perfectly good adapter. This is the seed of the anti-corruption layer covered in Enterprise Integration → Data Mapping: convert vendor-shaped data into your own model at the boundary. Leaky adapters that return vendor objects. Translate to your own types at the boundary. Inheriting from the vendor class exposes every vendor method to your callers. Prefer holding it. Putting business logic in the adapter. Keep it to translation only. Add a third provider adapter and confirm checkout is unchanged. Make adapters raise your own PaymentError instead of vendor exceptions. Write a test using a fake adaptee to test an adapter without the netw   What is the difference between Adapter, Facade, Decorator and Proxy? Adapter : converts one interface into another that a client expects. Facade : offers a simpler interface to a complex subsystem. Decorator : same interface, adds behaviour. Proxy : same interface, controls access (lazy load, cache, permissions). Object adapter vs class adapter? An object adapter holds the adaptee (composition) and delegates; a class adapter inherits from the adaptee (often via multiple inheritance). Prefer object adapters: they are looser, work with subclasses of the adaptee, and expose only what you choose. Give an integration example. Wrapping two payment providers behind a single charge(amount_cents) interface: one adapter converts to a vendor's create_charge(cents, currency) , another converts cents to dollars for a legacy bank API. The rest of the code depends only on charge . Where do adapters belong in an architecture? At the system boundary (ports and adapters / hexagonal architecture), so vendor-specific formats, units and errors stay outside the core domain. They also make it easy to fake in tests. class vendorclient third party change fetch self kind return customergateway adapter interface code wants __init__ client _client get_customer customer_id customer name translate shape print stripelike create_charge cents currency true ch_1 legacybank dollars approved stripeadapter charge amount_cents bankadapter checkout gateway to_internal vendor_row email email_addr strip lower rows example","patterns/decorator-pattern":"The Decorator pattern adds behavior to an object by wrapping it in another object with the same interface. Logging, caching, retry, metrics, and auth checks can be layered on without touching the wrapped class. Python also has @decorator syntax for functions, which is a related but separate tool. Use when you want to add responsibilities to individual objects dynamically, and stack them (cache around retry around client). Object decorator (GoF) : a wrapper class exposing the same methods, delegating to the wrapped object. Function decorator ( @syntax ) : wraps a function at definition time. Covered in Python → Functional Python. Use __getattr__ to delegate everything you don't override, instead of re-declaring every method. The wrapper looks like the client, does extra work, then delegates. Stacking decorators: cache around logging Each wrapper adds one concern. Order matters: the outer layer sees calls first. Because caching is outermost, the second call never reaches the logger or the real client. Swap the order and you'd log every call, cached or not. Both wrappers are unaware of each other. Delegating with __getattr__ Only override what you change; forward everything else automatically. One wrapper now instruments every method of any object, without listing them. It's powerful, but it hides the real interface from IDEs and type checkers, so use it sparingly. Confusing this with @decorator syntax. Same name, different mechanism and use case. Wrapper order changes behavior (cache vs log; retry vs circuit breaker). Document it. __getattr__ delegation hides methods from type checkers and doesn't forward special methods like __len__ . Add a RetryingClient wrapper and test where it sits in the stack. Swap the order of caching and logging and predict the output first. Reim   How does the Decorator pattern differ from Python's @decorator syntax? The pattern wraps an object with another that has the same interface to add behaviour dynamically. The syntax wraps a function (or class) at definition time via func = decorator(func) . They are related uses of wrapping but are separate mechanisms. Why use Decorator instead of subclassing? Inheritance is static and can lead to a combinatorial explosion (LoggingCachingClient, RetryingLoggingClient...). Decorators compose at runtime: Caching(Logging(Client())) lets you stack any combination in any order. Does the order of decorators matter? Yes: the outermost layer sees the call first. Caching outside logging skips logging on a cache hit; logging outside caching logs every call. Retry outside a circuit breaker behaves very differently from the reverse. Decorator vs Proxy? Structurally near-identical. Decorator's intent is to add responsibilities and it is often stacked; Proxy's intent is to control access to the same object (lazy initialisation, permissions, remote access). class client self path return data from loggingclient __init__ inner _inner print orders network call cachingclient _cache served cache line printed timed __getattr__ name called only attributes found attr getattr callable wrapper args kwargs result finished service ping pong echo","patterns/facade":"Facade provides one simple entry point in front of a complicated subsystem. Onboarding a customer might touch billing, identity, storage, and email systems. A facade exposes onboard(customer) and hides the choreography. Use when callers need a few common operations but the underlying subsystem has many classes, ordering rules, and setup steps. Java: a facade class. Python: a class or a plain function/module . A well-named module of functions is often the facade. A facade does not forbid access to the subsystem; it just offers an easy path for the common case. Great for FDE work: wrap a customer's messy systems behind one stable, documented interface for your own code. The facade owns the sequence and error handling; callers make one call. A module as a facade No class needed: expose two functions and keep the rest private. Leading underscores mark internals. Callers get one stable function, so the internals can be reorganized or replaced without breaking anyone. Facade with partial-failure handling Multi-step operations can fail midway. A good facade decides what to undo. The facade is the right place to own ordering and compensation (undo), because callers of a simple onboard() can't be expected to know which sub-steps to roll back. A facade that grows into a god-class with all the logic. It should coordinate, not contain the business rules. Hiding failures: return clear errors that say which step failed. Forcing all callers through it. Leave the subsystem reachable for power users. Add a fourth subsystem call to onboard() and handle its failure with a compensation step. Turn the Onboarding class into a module of functions and compare. Write a fake for each subsystem and test the facade without the network.   What is a Facade and how is it different from an Adapter? A Facade provides one simple entry point in front of a complex subsystem (for example onboard(customer) orchestrating identity, billing and email). An Adapter changes an interface to match one a client expects. Facade simplifies; Adapter translates. What are the risks of a Facade? It can grow into a god-class that owns business logic instead of just coordinating, and it can hide failures. Keep it thin, surface which step failed, and leave the subsystem accessible for callers who need finer control. How does a Facade help with multi-step operations that can fail midway? It is the natural place to own ordering and compensation: if step 3 fails, it undoes steps 1 and 2 (or records what needs cleanup), so callers of the simple method do not need to know the internals. Where do you see Facades in real systems? An SDK's high-level client, a service layer over several repositories, and a backend-for-frontend or API gateway endpoint that aggregates several downstream calls into one. class identity create_user self email return billing create_account acct mailer welcome sent onboarding __init__ onboard user note account print example reports imagine this file _fetch_rows source amount _summarize rows monthly_total public entry point callers never touch fetch summarize orders_db steps done append raise runtimeerror failed undo_a remove except exception compensate step that succeeded error state","patterns/proxy":"A proxy stands in for another object and controls access to it: lazy loading an expensive resource, caching results, checking permissions, or rate limiting. Callers use the proxy exactly like the real object. Virtual proxy : delay creating something expensive until first use. Protection proxy : check permissions before delegating. Caching / rate-limiting proxy : reduce calls to a slow or metered API. Structurally similar to Decorator; the intent differs. Decorator adds features, Proxy controls access to the same object. Python extras: functools.cached_property for lazy attributes, __getattr__ for transparent delegation. The expensive object is only built on first real use. Protection proxy Only admins may call destructive methods; everything else passes through. The caller holds what looks like a Database. The proxy enforces policy in one place instead of every call site trusting itself. Lazy attribute with cached_property Python has built-in lazy evaluation for attributes. The first access computes and stores the value on the instance; later accesses read it directly. For many 'lazy load' needs this is simpler than writing a proxy class. Caching proxies serve stale data. Decide on expiry or invalidation. A proxy that swallows or reshapes errors makes debugging hard. Preserve exceptions. Lazy creation and threads: two threads may both create the object. Guard with a lock if that matters. Add a TTL to a caching proxy so entries expire after 60 seconds. Make the lazy proxy thread-safe with a lock. Build a rate-limiting proxy that allows at most N calls per second.   Name the common types of proxy. Virtual : defer creating something expensive until needed. Protection : check permissions before delegating. Caching : return stored results for repeated calls. Remote : represent an object in another process or machine (RPC stubs). Rate-limiting/logging : control or observe calls. How do you implement lazy loading in Python? A wrapper that creates the real object on first use, or the built-in functools.cached_property for expensive attributes that should be computed once per instance. For thread safety, guard creation with a lock. What are the risks of a caching proxy? Stale data, unbounded memory growth, and cache stampedes. Add expiry (TTL) or invalidation, bound the size, and consider request coalescing for hot keys. Which Python features help build transparent proxies? __getattr__ delegates unknown attributes to the wrapped object, and __call__ , __getitem__ and other dunder methods can be forwarded explicitly (special methods are looked up on the type, so __getattr__ does not cover them). class expensivemodel __init__ self print loading model imagine seconds here predict return lazymodel _real none instant nothing loaded loads then predicts database read value delete deleted guardeddatabase role _role admin raise permissionerror only viewer except blocked from functools import cached_property customer history querying order runs query cached second","patterns/strategy":"Strategy defines a family of interchangeable algorithms and lets the caller pick one at runtime. Pricing rules, retry policies, sorting orders, and data-cleaning steps are all strategies. Use when you have if/elif chains choosing between algorithms, or when behavior must vary per customer or per config. Java: interface + one class per algorithm + a context holding a reference. Python: a callable . Functions, lambdas, and callable objects all work. Use a class only when a strategy needs its own state or several methods. Strategies pair well with a dict registry (name → function) for config-driven selection. The context accepts any callable with the agreed signature. Retry policies as strategies Different customer APIs need different waits between retries. Encode each as a function returning the delay for attempt n. The retry loop doesn't care how the delay is computed. Choosing a policy from config is one dict lookup, and adding a new policy is one function. Stateful strategy as a callable class When a strategy needs configuration, use a class with __call__ so it still looks like a function to the caller. Because the caller only needs discount(price) , a lambda, a function, and an object with __call__ are interchangeable. That is Strategy without any base class. Don't create a class hierarchy for strategies that are one line of logic each. Agree on one signature. Mixing strategies with different parameters breaks the swap-ability. Watch late binding in lambdas created in a loop; capture with a default argument. Replace an if/elif on a 'mode' string in your own code with a dict of functions. Add a 'jitter' retry policy that adds a random offset to exponential delays. Write a strategy that needs state (a counter) as a callable class.   What is the difference between Strategy and State? Structurally similar, but different intent. Strategy: the client chooses an interchangeable algorithm, and strategies do not usually know each other. State: an object changes behaviour as its own state changes, and the states often trigger transitions to one another. How would you implement Strategy idiomatically in Python? Pass a callable: def checkout(price, discount): return discount(price) . Use a plain function or lambda for simple cases, and a class with __call__ when the strategy needs its own state or configuration. A dict from name to function supports config-driven selection. What smell tells you to introduce Strategy? A growing if/elif chain choosing between algorithms, often keyed on a mode string, that must be edited every time a variant is added. Strategy moves each variant to its own unit and lets new ones be added without touching the caller. Give real examples of Strategy in an integration context. Retry/backoff policies (fixed, exponential, jittered), pricing or discount rules per customer, pagination styles for different vendor APIs, and data-cleaning or matching rules that vary by customer. by_price item return price by_name name sort_items items key_strategy sorted print fixed attempt linear exponential policies delays policy_name attempts policy range class percentdiscount __init__ self percent __call__ checkout discount round lambda plain works","patterns/observer":"Observer lets one object (the subject) notify many others (observers) when something happens, without knowing who they are. It's in-process publish/subscribe: UI events, plugin hooks, audit logging, cache invalidation. Use when one event should trigger several independent reactions and you want to add reactions without editing the emitter. Java: Observer interface + subject with a list. Python: a list of callables is enough. This is in-process only. Across services you want a message broker (see Enterprise Integration → Queues & Events). Beware memory leaks: a subject holding strong references keeps observers alive. Provide unsubscribe, or use weakref . Subscribe callables, then emit an event to all of them. Order events with independent listeners When an order is placed, log it, update inventory, and notify billing. The order code knows none of them. The emitter loops over handlers. Adding a fourth reaction means one more subscribe ; the order code stays untouched. That's the open/closed principle in practice. Isolating a failing observer One broken listener shouldn't stop the others from being notified. Without the try/except, bad would abort the loop and good would never run. Decide deliberately whether a failing observer should be swallowed and logged, retried, or propagated. Mutating the handler list while iterating (unsubscribing inside a handler). Iterate over a copy. Order dependence: don't rely on observer order unless you document and test it. Strong references keep observers alive after you think they are gone. Unsubscribe explicitly. Add an unsubscribe and prove a removed handler stops receiving events. Make the emitter use weakref.WeakMethod for bound methods. Add event names so one emitter supports 'created' and 'deleted' handlers.   Explain the Observer pattern and push vs pull notification. A subject keeps a list of observers and notifies them when its state changes, so it does not need to know who they are. Push sends the data with the notification; pull sends a lightweight signal and observers query the subject for what they need. What are common pitfalls of Observer? Memory leaks: the subject's strong references keep observers alive (the lapsed-listener problem); provide unsubscribe or use weak references. Modifying the listener list during notification. One failing observer breaking the loop; decide how errors are isolated. Hidden ordering assumptions and cascading updates. How is an in-process Observer different from a message broker? Observers are callbacks in one process: synchronous, no durability, no delivery guarantee across restarts. A broker (Kafka, RabbitMQ, SQS) decouples producers and consumers across processes and time, persists messages, and supports retries and replay, at the cost of complexity and eventual consistency. How do you implement it in Python? Keep a list of callables; subscribe(fn) appends and returns an unsubscribe function; emit(*args) iterates over a copy of the list and calls each. No Observer interface is needed. class emitter __init__ self _handlers subscribe append return lambda remove returns unsubscribe function emit args kwargs list copy handlers order_placed audit inventory billing print emit_safely event errors except exception real system would this __name__ good raise runtimeerror boom","patterns/command":"Command turns an action into an object (or callable) that can be stored, queued, logged, retried, or undone. Job queues, undo stacks, audit trails, and scheduled tasks all rely on it. Use when you need to defer execution, queue work, record what happened, or support undo/redo. Java: Command interface with execute() . Python: a callable , functools.partial , or a small class with execute() / undo() . Commands should carry everything needed to run (their arguments), so they can be executed later, elsewhere, or twice. Serializable commands (name + JSON args) are the basis of job queues and audit logs. Each command knows how to do and undo one thing. Queued commands with functools.partial Bind arguments now, run later. No class required. partial packages the function with its arguments into a zero-argument callable. That's the Command pattern: the request is now an object you can put in a list, delay, or retry. Serializable command log Store commands as data (name + args) so they survive restarts and can be replayed. Because each command is plain data plus a registry of handlers, you can write it to a queue or file and replay it after a crash. This is how job systems and event-sourced audit logs work. Commands that capture live objects (connections, open files) can't be serialized or run elsewhere. Capture IDs and re-fetch. Undo is only valid if execute stored what it needs (previous value). Design for it up front. Non-idempotent commands replayed after a failure can double-apply. See Enterprise Integration → Resilience. Add a RemoveItem command with undo that restores the item at its original index. Add a redo stack to the cart example. Make the JSON command log reject unknown command names with a clear error.   What is the Command pattern used for? Turning an action into an object so it can be queued, scheduled, logged, retried, or undone. Typical uses: undo/redo, job queues, transactional scripts, and audit logs of user actions. How would you implement undo? Give each command execute() and undo() , storing whatever state undo needs (for example the previous value) when it executes. Keep a history stack; undo pops and calls undo() , with a second stack for redo. Command vs Strategy? Strategy picks how to do something (an algorithm). Command packages a request to do something with its arguments so it can be deferred or recorded. In Python both are often just callables, and functools.partial is a lightweight command. What must you consider when commands are serialised into a queue? Store data (name plus arguments), not live objects, so commands survive restarts and can be replayed. Because delivery is often at-least-once, commands should be idempotent or carry an idempotency key. class additem __init__ self cart item execute append undo remove history book print from functools import partial send_email subject return mail queue welcome invoice results later order json handlers set_status lambda status __setitem__ name args shipped pending saved dumps persist this loads replay","patterns/state":"State lets an object change its behavior when its internal state changes, replacing sprawling if/elif checks on a status field with explicit states and legal transitions. Orders, tickets, jobs, and connection lifecycles are classic examples. Use when the same action means different things depending on status, or when only some transitions are legal (you can't ship a cancelled order). Java: State interface + one class per state. Python: for small machines, an Enum plus a transitions dict is clearer than classes. Use state classes when each state carries substantial behavior of its own. Making illegal transitions raise errors protects data integrity, which matters when your code syncs status between systems. Legal moves live in one data structure, not scattered across if statements. Using the state machine Legal transitions succeed; illegal ones fail loudly. A shipped order can't be cancelled, and that rule is one line of data. When a customer's system sends an out-of-order status update, you get an explicit error instead of silent corruption. State classes with behavior When states have different behavior, not just different permissions, give each its own class. The same press() call does different things depending on the current state object. No conditionals: each state decides what happens and which state comes next. Allowing direct assignment to status from anywhere bypasses the rules. Route changes through one method. State machines mirrored in an external system can drift. Reconcile periodically. Don't build state classes when a small table would do. Add a REFUNDED state reachable only from SHIPPED and update the table. Log every transition with a timestamp to build an audit trail. Convert the player example to an Enum + table and compare readability.   Explain the State pattern and when to use it. An object delegates behaviour to a state object that changes as the object's status changes, replacing large if/elif chains on a status field. Use it when the same action means different things in different states or when only some transitions are legal (orders, tickets, connections, jobs). How would you model a small workflow in Python? An Enum for the states plus a dict of allowed transitions, with a single move_to(new) method that raises on an illegal transition. Use state classes only when each state carries substantial behaviour of its own. How do you handle illegal or out-of-order transitions from external systems? Reject or quarantine them with a clear error and log them, rather than silently applying them. When syncing status with another system, treat the transition table as the source of truth, use timestamps or version numbers to detect stale updates, and reconcile periodically. State vs Strategy? Strategy is chosen by the client and is normally stable for a call; State is driven internally and changes over the object's life, often deciding the next state itself. from enum import class status paid shipped cancelled allowed order __init__ self move_to raise valueerror value except print idle press player state playing return start paused pause resume behavior delegates current range","patterns/dependency-injection":"Dependency Injection (DI) means passing an object its collaborators (database, HTTP client, clock) instead of having it create them. It keeps code testable and lets you swap a customer's real system for a fake or a different vendor without editing business logic. Use whenever code reaches out to something external: network, database, filesystem, time, randomness. Java: often a DI container (Spring). Python: usually plain constructor or function arguments . No framework needed. Default arguments can supply the real dependency while tests pass a fake. DI is the antidote to Singleton: instead of importing a global, receive the instance. The service doesn't know or care which client it gets. Injecting the clock to test time-dependent logic Code that calls datetime.now() directly is hard to test. Inject a function returning the time. The test controls time, so expiry logic can be verified instantly without sleeping. The default argument means production code doesn't change at all. Wiring at the edge (composition root) Build real objects in one place, at program start; everything below just receives them. Only main() knows the concrete classes. To run against a customer's sandbox or a fake, change the wiring in one function. DI containers automate this wiring, but in most Python projects hand-wiring stays readable. Default-argument trap: def f(x, client=Client()) creates the client once at definition time. Use None and create inside, or pass a factory. Injecting too many dependencies suggests the class does too much. Mocking everything with unittest.mock.patch instead of injecting hides design problems. Refactor a function that calls requests.get directly so the HTTP function is a parameter, then test it without network. Write a fake client that raises on the second call and    What is Dependency Injection, and how does it relate to IoC and the Dependency Inversion Principle? DI passes an object its collaborators instead of it creating them. Inversion of Control is the general idea that a framework or caller controls object wiring. The Dependency Inversion Principle says high-level code should depend on abstractions, not concrete implementations. DI is a technique that realises the principle. How does DI improve testability? You can pass a fake or stub (an in-memory client, a controllable clock) instead of the real network, database or time, so tests are fast and deterministic without monkey-patching. Do you need a DI container in Python? Usually not. Constructor or function arguments, wired in one composition root (often main() ), are enough for most projects. Containers or frameworks help in very large applications with many layered dependencies. What is wrong with def f(client=Client()): ? The default is evaluated once at definition time and shared across calls (and constructing it may have side effects at import). Use None and create it inside, or accept a factory, or inject from the composition root. class invoiceservice __init__ self client injected total_due customer_id invoices list_invoices return amount paid fakeclient false true print from datetime import timedelta tokencache ttl_seconds default real clock seconds _issued none valid fake_time cache lambda httpclient inner data health main composition root","integration/integration-styles":"Almost every customer engagement is an integration problem: your product has to exchange data with systems you don't control. There are four classic styles. Knowing which fits a situation (and what each costs) is the first design decision you make on site. Request/response API (REST, GraphQL, gRPC, SOAP): synchronous, simple, needs the other side up right now. Best for lookups and user-driven actions. Events / messaging (queues, pub/sub, webhooks): asynchronous, decoupled, tolerates downtime, but harder to reason about ordering and duplicates. File / batch (CSV or XML drops, SFTP): universal and often the only option for legacy systems. High latency, needs validation and reconciliation. Shared database / direct read : fastest to start, most fragile. Couples you to the other team's schema. Prefer read-only access to a replica or view. Real projects mix styles: a nightly file for history, a webhook for changes, an API for lookups. Decide by: latency needed, who owns the system, volume, what happens when either side is down, and how you'll detect a silent failure. 'Get a customer to the other system' expressed as the shape of each integration (simulated). Choosing a style: a small decision helper Encode the trade-offs as questions. This isn't production logic; it's a way to make the reasoning explicit in a design discussion. Notice what dominates: reliability of the other side and whether an immediate answer is required. If you can't get an immediate answer from a system that is often down, a synchronous API will make your product as unreliable as their system. Reconciliation: how you know an integration is healthy Whatever the style, compare what should have arrived with what did. Silent data loss is the worst failure mode. A scheduled reconciliation job (count, ID diff,    How do you choose between synchronous APIs, messaging, file/batch and direct database integration? Decide by required latency, who owns each system, volume, and what happens when either side is down. Use a synchronous API when the user needs an immediate authoritative answer and the provider is reliable; use events/queues to decouple and absorb outages; use files when a legacy system only supports them; use direct database access sparingly (read-only, replica or view) because it couples you to their schema. Real projects mix styles. Polling vs webhooks: trade-offs? Polling is simple and under your control but wasteful and delayed. Webhooks are near-real-time and cheap but require a public, secure endpoint, signature verification, idempotent handling, and a reconciliation job because deliveries can be missed or reordered. A common approach is webhooks for speed plus periodic polling or reconciliation for safety. How do you prove an integration did not lose or duplicate data? Reconciliation: compare counts, ID sets, or checksums between source and target on a schedule and report missing and unexpected records. Combine it with idempotent writes, dead-letter queues, and monitoring for stuck items so silent failures become visible. A customer wants their CRM and billing system to stay in sync. How would you approach the design? Clarify requirements first: direction, latency, volume, conflict rules, and system-of-record for each field. Then propose: an adapter per system behind a c call answer sync_via_api post customer return customers blocks caller sees success failure event publish move consumer does work later sync_via_event updated returns immediately file append batch that scheduled ships sync_via_file rows shipped tonight database write straight into other system table fragile sync_via_db conn execute insert crm_customers values name suggest_style needs_immediate_answer other_side_reliable volume_per_day legacy_only_files request response _000 events messaging print","integration/rest-clients":"Calling someone else's API is easy in a demo and hard in production. Networks time out, servers return 429 and 503, and result sets span hundreds of pages. A reliable client sets timeouts, retries only what is safe, follows pagination, and respects rate limits. Always set a timeout. Python's requests has none by default; a hung server hangs your program forever. Retry selectively : network errors, 429, and 5xx. Not 400/401/403/404 (retrying won't fix them). Only retry safe or idempotent calls (GET, PUT, DELETE, or POST with an idempotency key). See Resilience. Respect 429 and the Retry-After header instead of hammering. Paginate : cursor/next-link pagination is more robust than page numbers when data changes underneath you. Reuse a Session (connection pooling), and log status codes and durations. Not run in this page (needs network); this is the shape to copy. Cursor pagination as a generator Hide pagination from callers: yield items across pages until there's no next cursor. (Uses a fake API so it runs anywhere.) A generator keeps memory flat (one page at a time) and callers just write for item in iter_items(...) . Injecting fetch makes it testable and reusable for any API. Honoring 429 and Retry-After When a server says slow down, wait the time it asks for. (Simulated responses; sleep is injected so the demo is instant.) The first 429 carries an explicit Retry-After: 3 , so we wait 3s. The second has none, so we fall back to exponential backoff (2 ** attempt = 4s). Ignoring these signals can get your integration blocked. No timeout: one stuck connection freezes a worker indefinitely. Retrying POST blindly can create duplicate records. Use idempotency keys. Offset pagination while data is being inserted skips or repeats records. Prefer cursors. Ignoring Retry-After and   How do you handle rate limiting (HTTP 429)? Respect the Retry-After header when present, otherwise use exponential backoff with jitter. Proactively throttle with a token bucket, queue work to smooth bursts, cap total retries, and log throttling so you can request higher limits if needed. Which errors do you retry, and what makes a request safe to retry? Retry network errors, timeouts, 429 and 5xx. Do not retry 400, 401, 403 or 404, since they will fail the same way. Retries are safe for idempotent operations (GET, PUT, DELETE) or POSTs sent with an idempotency key; otherwise a retry after a timeout can create duplicates. Offset vs cursor pagination? Offset ( page=3 ) is simple but skips or repeats records when data changes during traversal and gets slow on deep pages. Cursor (a token or last-seen ID) marks a stable position and stays correct under inserts and deletes. Prefer cursors, and wrap them in a generator so callers just iterate. What timeout settings should a production client have? Always set both a connect timeout and a read timeout (Python's requests has none by default, so a hung server blocks forever). Reuse a session for connection pooling, bound the total time across retries, and log status and duration for each call. import requests from adapters httpadapter urllib3 util retry make_session session total backoff_factor status_forcelist allowed_methods delete post default idempotent respect_retry_after_header true mount https max_retries return resp example orders timeout connect read raise_for_status data json fake_api cursor none pages items next iter_items fetch while page yield break print list class __init__ self status headers status_code get_with_backoff send max_attempts sleep lambda waits attempt rang","integration/authentication":"Before any data moves, you have to prove who you are. Customers will hand you API keys, OAuth2 credentials, or service accounts, and each has different lifetimes, scopes, and failure modes. Getting authentication right (and keeping secrets out of code) is usually the first thing a security review checks. API key : a static secret in a header ( X-API-Key or Authorization ). Simple; must be stored securely and rotated. Basic auth : base64 of user:password . Base64 is encoding, not encryption. Only use over HTTPS. Bearer token : Authorization: Bearer token . Usually short-lived and obtained from an auth server. OAuth2 client credentials : machine-to-machine. Exchange client id and secret for an access token, cache it until near expiry, then refresh. OAuth2 authorization code : a user grants your app access (acting on a user's behalf); involves redirects and refresh tokens. Least privilege: request only the scopes you need. Read secrets from the environment or a vault, never from source code. Secrets come from the environment; headers are built per request. Token manager for OAuth2 client credentials Fetch a token once, reuse it until shortly before it expires, then refresh. The token endpoint and clock are injected so this runs offline. Without caching you'd hit the auth server on every call (slow, and often rate limited). Without a skew, a token can expire mid-request. Production versions also lock around refresh so concurrent threads don't all refresh at once. Recovering from a 401 A token can be revoked before it expires. On 401, refresh once and retry, but don't loop forever. Retrying exactly once distinguishes 'my token went stale' from 'my credentials are wrong'. If the second attempt is still 401, surface the error instead of retrying forever. Committing secrets to    Compare API keys, OAuth2 and JWTs. An API key is a static shared secret: simple but coarse and needs rotation. OAuth2 is an authorisation framework for obtaining scoped, short-lived access tokens (client credentials for machine-to-machine, authorisation code for acting on behalf of a user). A JWT is a signed token format often used as an access token; it can be verified offline but is hard to revoke before expiry. Explain the OAuth2 client credentials flow and how you would manage tokens. The app authenticates with its client ID and secret to the token endpoint and receives a short-lived access token. Cache it in memory, refresh shortly before expires_in elapses (with a small skew), lock around refresh so concurrent threads do not stampede, and on a 401 refresh once and retry once. Where should credentials be stored, and how are they rotated? In a secrets manager or environment variables injected at deploy time, never in source control, logs or URLs. Support rotation by reading secrets at runtime, allowing two valid secrets during a switch-over, and scoping each credential narrowly with least privilege. What is the difference between a 401 and a 403? 401 Unauthorized means the request lacks valid authentication (missing, expired or wrong token). 403 Forbidden means the caller is authenticated but not allowed to do this. A 401 may be fixed by refreshing a token; a 403 is a permission problem and retrying will not help. import base64 api_key environ vendor_api_key demo never hard code real keys api_key_headers return basic_auth_headers user password encode authorization basic b64encode decode bearer_headers token bearer print class tokenmanager __init__ self fetch_token skew _fetch returns access_token expires_in seconds _now _skew refresh early avoid edge expiry failures _token none _expires_at data calls clock fake_fetch append lambda same fetch within refreshed fetches call_with_reauth send get_token refresh","integration/webhooks":"A webhook is the other system calling you when something happens (payment succeeded, ticket updated). Because it's an inbound HTTP request from the internet, you must verify it's genuine, respond quickly, and expect duplicates and retries. Verify the signature. Providers sign the raw body with a shared secret (HMAC-SHA256). Reject anything that doesn't match. Use a constant-time comparison. Check the timestamp and reject old requests, so captured requests can't be replayed later. Respond fast (2xx) , then process asynchronously. Slow handlers cause the sender to time out and retry. Be idempotent. Providers deliver at least once. De-duplicate on the event ID. Don't trust ordering. Events can arrive out of order. Use timestamps or fetch current state. Verify against the raw bytes. Re-serializing parsed JSON changes whitespace and breaks the signature. HMAC over the timestamp and body, compared in constant time. Accepting valid, rejecting tampered and stale requests Exercise the verifier with three cases. Any change to the body invalidates the signature; including the timestamp in the signed message stops an attacker from re-sending an old valid request with a fresh timestamp. compare_digest avoids timing side channels that == can leak. Idempotent processing by event ID Handle a duplicate delivery without double-applying it. Providers retry on any non-2xx or timeout, so the same event can arrive several times. Recording processed IDs (in a durable store with a uniqueness constraint) makes handling safe to repeat. Comparing signatures with == (timing attack) instead of hmac.compare_digest . Verifying a re-serialized JSON body instead of the raw request bytes. Doing slow work inside the handler, causing timeouts and duplicate deliveries. Storing processed IDs in memory only:   How do you build a secure webhook receiver? Verify an HMAC signature computed over the raw request body (and timestamp) with a shared secret, using a constant-time comparison; reject old timestamps to stop replays; serve over HTTPS; then respond 2xx quickly and process asynchronously. Do not trust the payload beyond the verified event, and consider fetching current state from the provider's API. How do you handle duplicate and out-of-order deliveries? Deliveries are at-least-once, so de-duplicate on the event ID by storing processed IDs durably (a database table with a unique constraint, with a TTL). For ordering, do not assume it: compare event timestamps or version numbers, or re-fetch the latest resource state instead of applying the event blindly. Why respond immediately and process later? Providers time out and retry when a handler is slow, creating duplicate deliveries and load. Acknowledge with a 2xx after verifying and persisting or enqueuing the event, and let a worker do the real processing with retries and a dead-letter queue. What if your receiver is down when events are sent? Most providers retry with backoff for a while, but not forever, so you need a recovery path: a reconciliation job or replay endpoint that pulls missed events from the provider's API by time range, and monitoring that alerts on gaps. import hmac hashlib time secret whsec_demo sign body bytes timestamp message encode return sha256 hexdigest verify signature tolerance none else float false from future expected compare_digest constant compare evt_1 type invoice paid print valid tampered replayed late processed production database table with unique index event_id balance total handle event duplicate ignored amount applied provider retried same","integration/messaging":"Queues and pub/sub decouple systems in time: the producer hands off a message and moves on, and the consumer processes it when it can. This absorbs spikes and outages, but introduces at-least-once delivery, duplicates, ordering questions, and poison messages you must handle deliberately. Queue (point-to-point) : each message is processed by one consumer. Good for work distribution. Topic (pub/sub) : each message is delivered to every subscriber. Good for broadcasting events. Ack / nack : a message is removed only after the consumer acknowledges success. If it crashes first, the message is redelivered. At-least-once is the common guarantee, so consumers must be idempotent. (True exactly-once end-to-end is very hard.) Dead-letter queue (DLQ) : messages that fail repeatedly go aside for inspection, instead of blocking the queue forever. Real brokers: RabbitMQ, Kafka, SQS/SNS, Pub/Sub, Azure Service Bus. The concepts transfer. In-memory simulation of the semantics real brokers give you. Retry then dead-letter a poison message One bad message must not block good ones. 'bad' fails three times and lands in the DLQ; 'a' and 'b' are processed normally. Someone (or an alert) then inspects the DLQ to fix the data or the parser and replay. Pub/sub fan-out with an idempotent consumer Every subscriber gets each event; a redelivered event must not be applied twice. Billing de-duplicates by event ID so the redelivery is harmless; the audit subscriber records both deliveries because it doesn't need idempotency. Each consumer owns its own duplicate handling. Acknowledging before doing the work: a crash loses the message. Non-idempotent consumers with at-least-once delivery: duplicates corrupt data. No DLQ: one poison message is retried forever and blocks everything behind it. Assuming gl   Explain at-most-once, at-least-once and exactly-once delivery. What do you use in practice? At-most-once may lose messages; at-least-once never loses but may duplicate; exactly-once is very hard end to end. In practice use at-least-once delivery with idempotent consumers, which yields effectively-once processing. What is a dead-letter queue and why is it important? A holding queue for messages that failed after the maximum number of attempts (poison messages). It stops one bad message from blocking the queue, preserves the data for inspection, and enables replay after a fix. Alert on its depth. Queue vs topic (pub/sub)? How do Kafka, RabbitMQ and SQS differ in broad terms? A queue delivers each message to one consumer (work distribution); a topic delivers each message to every subscriber (fan-out). Kafka is a partitioned, replayable log with consumer offsets and per-partition ordering; RabbitMQ is a broker with flexible routing and acknowledgements; SQS is a managed queue (with FIFO variants) that trades features for operational simplicity. How do you handle ordering and backpressure? Ordering is typically guaranteed only within a partition or message group, so key messages by entity ID to keep one customer's events ordered while scaling out. For backpressure, bound queue sizes, limit consumer concurrency, scale consumers on queue depth, and monitor the age of the oldest message. from collections import deque class queue __init__ self max_attempts items entries message attempts dead letter send append process handler while entry popleft success gone except exception give else nack redeliver later done raise valueerror cannot parse print processed lettered topic subs subscribe publish event billingconsumer seen total __call__ return duplicate delivery amount billing audit lambda broker redelivered","integration/batch-files":"Many enterprise systems still exchange data as CSV, XML, or fixed-width files dropped on SFTP or a shared folder. Batch integration is high-latency but predictable, and it's often the only interface a legacy system offers. The craft is in validating rows, isolating bad ones, and making reloads safe. Validate every row , and collect errors instead of failing the whole file on row 1. Write rejects to a dead-letter file with the row number and reason, so the customer can fix the source data. Idempotent loads : upsert on a natural key so re-running a file doesn't duplicate rows. Stream, don't slurp : use csv.DictReader and chunk inserts so large files don't exhaust memory. Beware encodings and dialects : BOMs, cp1252, semicolon delimiters, quoted newlines, leading zeros lost by spreadsheets. Track the file : name, checksum, row counts in and out, so you can prove what happened. The pattern: good rows go forward, bad rows are captured with a reason. Run it on a messy file One valid row, one bad email, one bad amount. The load doesn't abort on the first problem; it returns everything valid plus a precise list (line number and reason) the customer can act on. The reject report is often the most valuable deliverable of a batch integration. Idempotent load with an upsert Running the same file twice must not create duplicates. Use a unique key and ON CONFLICT . The primary key plus ON CONFLICT DO UPDATE makes the load safe to repeat and lets corrected files overwrite earlier values. Parameterized placeholders ( ? ) also protect against SQL injection. Failing the whole file on one bad row, or worse, silently skipping bad rows with no report. Reading a huge file into memory. Stream and batch. Wrong encoding: opening a cp1252 file as UTF-8 raises errors or garbles names. Detect or a   How would you design a robust CSV import? Stream the file, validate each row, load valid rows, and write rejects (line number, reason, original row) to a separate file instead of failing the whole batch. Track file name, checksum, and row counts in and out, and make the load idempotent with an upsert on a natural key so reruns are safe. How do you make a batch load idempotent and restartable? Upsert on a unique key ( INSERT ... ON CONFLICT DO UPDATE ), record a checkpoint or processed-file marker, commit in chunks, and keep each run's input immutable. A rerun then converges to the same final state. What problems come from encodings and spreadsheet exports? Wrong encodings (cp1252 read as UTF-8), BOMs, different delimiters and quoting, embedded newlines, dates in ambiguous formats, and identifiers with leading zeros lost when treated as numbers. Agree on the format with the customer, decode explicitly, and treat IDs as strings. How do you avoid reading a file that is still being written? Have the producer write to a temporary name then atomically rename, or drop a completion marker file, or verify a stable size/checksum before processing. Then move processed files to an archive folder. import load text good rejects reader dictreader stringio line_no enumerate start header line errors email append amount float except valueerror else strip lower return data nada example nnot nbob print sqlite3 conn connect memory execute create table customers primary real upsert rows executemany insert into values conflict update excluded commit file_rows same file corrected value arrives select from order fetchall","integration/legacy-systems":"Customers run on systems built long before REST: SOAP web services, XML feeds, fixed-width mainframe exports, and databases with cryptic column names. You rarely get to replace them. Your job is to talk to them safely and translate what they say. SOAP : XML envelopes over HTTP, described by a WSDL. Libraries like zeep generate clients from the WSDL; underneath it's just XML you can build and parse with xml.etree . XML namespaces trip everyone up: element names are qualified as {namespace-uri}tag in ElementTree. Fixed-width files (mainframe/COBOL exports): fields defined by column positions from a spec, not by delimiters. Direct database access : prefer a read-only account against a replica or a view. Use parameterized queries. Never write into their tables unless agreed. Character encodings : cp1252, latin-1, EBCDIC. Decode explicitly with the encoding from the spec. Wrap all of it in an adapter so the weirdness stays at the edge (see Data Mapping). SOAP is XML: construct an envelope, send it as the HTTP body, parse the XML reply. Parsing a namespaced SOAP response Find fields inside an envelope. Note the namespace mapping passed to find . Without the ns mapping, find('soap:Body') raises or returns None. Also handle SOAP Fault elements: a 200 or 500 response can still contain an error payload that you must detect. Fixed-width records and a safe database read Parse a mainframe-style record by column positions, then look it up with a parameterized query. Slicing by position is the whole parser; validate the line length first in real code. The query passes the ID as a bound parameter ( ? ), never interpolated into the SQL string, which prevents SQL injection and quoting bugs. Forgetting XML namespaces so lookups silently return None. Treating an HTTP 200 SOAP reply as succ   How would you integrate with a SOAP service? Obtain the WSDL and generate a client (for example with zeep ), or build the XML envelope directly. Handle XML namespaces correctly, check for Fault elements even on HTTP 200, apply timeouts and retries, and wrap the client in an adapter that exposes your own types and errors. What is an anti-corruption layer and the strangler pattern? An anti-corruption layer translates between a legacy model and your own so its quirks do not leak into your domain. The strangler pattern incrementally replaces a legacy system by routing functionality to new components one slice at a time until the old system can be retired. How do you handle fixed-width or mainframe-style files and odd encodings? Use the record layout spec to slice by column positions, validate record length, decode with the specified encoding (cp1252, EBCDIC variants), and convert types explicitly (for example zero-padded cents to decimals). Test with realistic data including accented characters. What are the risks of reading a customer's database directly? You couple to a schema they may change without notice, can affect their performance, and may breach their security policies. Prefer a read-only account on a replica or a view, parameterised queries, change data capture where available, and never write into their tables unless explicitly agreed. import etree elementtree soap_ns http schemas xmlsoap soap envelope register_namespace build_request customer_id element body subelement getcustomer text return tostring encoding unicode print xml_text xmlns getcustomerresponse name lovelace status active root fromstring resp find findtext sqlite3 spec cols account balance cents zero padded line record lstrip strip conn connect memory execute create table acct insert into values open select from where fetchone","integration/data-mapping":"Every system models the world differently: 'customer' means something else in the CRM, the billing system, and yours. Data mapping translates between models. An anti-corruption layer (ACL) is a dedicated boundary that keeps a partner's odd model from leaking into your core, and it's also where schema drift gets caught. Define a canonical (internal) model once. Map each external system to and from it, rather than mapping every system to every other one. An anti-corruption layer = adapters + mappers at the boundary. Business logic only ever sees your model. Be explicit about units, time zones, null vs empty, enums, and identifiers . These are where silent bugs live. Schema drift : vendors add, rename, or remove fields without telling you. Detect unknown or missing fields and alert, instead of crashing or silently dropping data. Preserve the original payload (raw) alongside the mapped record for debugging and re-mapping later. Version your mappers so you can support old and new formats while a customer migrates. The mapper is the only place that knows the vendor's field names and quirks. Two vendors, one model Different field names, units, and date formats map to the same canonical record. Money is normalized to integer cents (never floats in the core). Timestamps become timezone-aware UTC. Emails are normalized. Everything downstream sees a single, consistent shape. Detecting schema drift Compare an incoming record's keys with what the mapper expects and report differences instead of failing mysteriously. 'missing' should fail the record (and alert); 'unknown' should usually just be logged and counted, since new optional fields are common. Trending these counts tells you a vendor changed their API before customers notice. Using floats for money. Use integer minor units or   What is a canonical data model, and what are its pros and cons? A single internal representation that each external system is mapped to and from. It avoids N-by-N pairwise mappings and isolates vendor quirks, but it takes design effort, can become a lowest-common-denominator schema, and must be versioned as needs change. How do you detect and handle schema drift? Compare incoming records with the expected schema: missing required fields fail the record and alert; unknown new fields are logged and counted. Keep the raw payload for re-mapping, version your mappers, and track drift metrics so you notice vendor changes before customers do. Which data-mapping pitfalls do you watch for? Money as floats (use integer minor units or Decimal), naive or mixed time zones (normalise to UTC), null versus empty versus missing, enums and code lists that differ between systems, ID formats and leading zeros, and character encodings. Explain an anti-corruption layer. A boundary made of adapters and mappers that translates an external system's model into your domain model, so business logic never sees vendor field names or units. Replacing a vendor then means rewriting one layer instead of the whole codebase. from dataclasses import dataclass datetime timezone frozen true class customer canonical model email created_at always aware balance_cents from_vendor_a return cust_id strip lower fromtimestamp created_ts round float balance_dollars from_vendor_b created strptime signup replace tzinfo contact print year date expected check_drift keys missing sorted will break mapper unknown fields vendor added loyalty_tier gold","integration/resilience":"Integrations run over networks and other people's systems, so failures are normal: timeouts, duplicate deliveries, slow downstreams, partial outages. Resilience techniques (idempotency, backoff with jitter, circuit breakers) let your side stay correct and stable while the other side misbehaves. Idempotency : performing an operation twice has the same effect as once. Send an idempotency key with writes; the server (or you) remembers the result for that key. Retry with exponential backoff and jitter : wait longer after each failure, with randomness so many clients don't retry in lockstep (thundering herd). Circuit breaker : after repeated failures, stop calling the dependency for a cooldown period ('open'), then probe carefully ('half-open'). Set timeouts everywhere and a total retry budget . Unbounded retries turn a blip into an outage. Retry only transient errors; fail fast on permanent ones (validation, auth). Prefer graceful degradation : serve cached or partial data when a dependency is down. delay = random between 0 and min(cap, base * 2**attempt). Injected sleep and random keep it testable. Idempotency keys prevent double charges The client times out and retries a payment; the same key returns the original result instead of charging twice. Without the key, the retry would create a second charge. With it, the operation is safe to repeat. Choose keys that identify the intent (an order ID), not the attempt (a random UUID per try). A minimal circuit breaker After 3 consecutive failures, fail fast for a cooldown instead of piling more load on a sick service. The fourth call never reaches the failing service, which protects it and keeps your latency low. After the cooldown one probe call is allowed; if it succeeds the circuit closes again. In production, use a tested lib   How do you design an idempotency key? The key should identify the intended operation, such as an order or request ID, not each attempt, so retries reuse it. The server stores the result keyed by it (with an expiry) and returns the stored result on repeats. Combine it with a unique constraint so concurrent duplicates cannot both succeed. Why use exponential backoff with jitter? Backoff gives a struggling service time to recover; jitter (randomness) prevents many clients from retrying in lockstep and causing a thundering herd. Cap the delay and the number of attempts so a blip does not become an outage. Describe the circuit breaker pattern and its states. Closed: calls pass through and failures are counted. Open: after a failure threshold, calls fail fast for a cooldown. Half-open: after the cooldown, a limited probe is allowed; success closes the circuit, failure reopens it. It protects the dependency and keeps your own latency low. What other techniques keep an integration stable under partial failure? Timeouts on every call, bulkheads (separate pools so one slow dependency cannot exhaust all threads), retry budgets to avoid retry storms across layers, graceful degradation with cached or partial data, and dead-letter queues for work that cannot be completed. import random time retry attempts base sleep rand attempt range return except connectionerror raise budget surface delay full jitter charges results idempotency_key stored result create_charge amount replay original append real side effect first order network hiccup client retried print class circuitopen exception pass breaker __init__ self threshold cooldown lambda failures opened_at none call skipping fail fast half open allow probe success closes circuit clock flaky down passed recovered","integration/security-observability":"Enterprise customers will put your integration through a security review and expect you to diagnose problems fast. That means protecting secrets and personal data, granting least privilege, and emitting logs, metrics, and audit trails that let you answer 'what happened to record 42?' in minutes. Secrets : environment variables or a secrets manager; never in code, logs, or URLs. Support rotation. Least privilege : read-only where possible, narrow OAuth scopes, dedicated service accounts. Data protection : TLS in transit, encryption at rest, minimize what you store, redact PII in logs. Structured logging with a correlation ID that follows a request across systems. Metrics : success/failure counts, latency, queue depth, age of oldest unprocessed item. Audit trail : who or what changed which record and when. Compliance regimes (SOC 2, HIPAA) require it. Alerting on symptoms customers feel (data late, errors rising), not just CPU. contextvars carries the ID through code (and async tasks) without passing it everywhere. Redacting secrets and PII before logging A log filter scrubs sensitive keys so debugging output is safe to share with a customer. Redaction is recursive so nested structures are covered. Apply it centrally (a logging filter or wrapper) rather than remembering at every log call. Also avoid logging full request/response bodies by default. Simple metrics for an integration Count outcomes and track age of the oldest pending item. These are the numbers you alert on. 'Oldest pending age' catches stuck integrations that produce no errors at all (nothing fails, nothing moves). Real systems export such numbers to Prometheus, CloudWatch, or Datadog. Logging bodies or headers that contain tokens and personal data. Secrets that can't be rotated without a redeploy. Logs wit   How do you manage secrets in an integration? Use a secrets manager or injected environment variables, never source control, logs, or URLs. Grant the minimum scopes, use separate credentials per environment and per customer, support rotation, and audit access. Scan repositories and rotate anything that leaked. What should you log, and what must you not log? Log structured events with a correlation ID, the operation, the target system, outcome, latency, and record identifiers useful for support. Do not log secrets, tokens, full request bodies, or personal data; redact centrally with a logging filter so it cannot be forgotten. Which metrics and alerts matter for an integration? Success and failure counts, latency, retry and dead-letter counts, queue depth, and above all the age of the oldest unprocessed item, which catches stalled pipelines that produce no errors. Alert on symptoms customers feel (data late, error rate rising), not just CPU. What will a customer security review typically ask? How data is encrypted in transit and at rest, where secrets live, what permissions you need and why (least privilege), what personal data you store and for how long, audit logging, incident response, and compliance posture such as SOC 2 or HIPAA. Prepare clear, honest answers and diagrams. import json logging uuid from contextvars contextvar correlation_id default class jsonformatter formatter format self record return dumps level levelname getmessage getlogger integration handler streamhandler setformatter addhandler setlevel info uuid4 sync started sensitive password token authorization api_key redact isinstance dict lower else items list payload user bearer abc123 profile city london print collections counter metrics __init__ counts pending_since item_id timestamp received done"};
