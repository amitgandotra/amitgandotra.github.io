// Interview questions for the Enterprise Integration topic. Key = "topicId/pageId"; a = HTML.
window.INTERVIEW = window.INTERVIEW || {};
Object.assign(window.INTERVIEW, {
  "integration/integration-styles": [
    { q: `How do you choose between synchronous APIs, messaging, file/batch and direct database integration?`, a: `<p>Decide by required latency, who owns each system, volume, and what happens when either side is down. Use a synchronous API when the user needs an immediate authoritative answer and the provider is reliable; use events/queues to decouple and absorb outages; use files when a legacy system only supports them; use direct database access sparingly (read-only, replica or view) because it couples you to their schema. Real projects mix styles.</p>` },
    { q: `Polling vs webhooks: trade-offs?`, a: `<p>Polling is simple and under your control but wasteful and delayed. Webhooks are near-real-time and cheap but require a public, secure endpoint, signature verification, idempotent handling, and a reconciliation job because deliveries can be missed or reordered. A common approach is webhooks for speed plus periodic polling or reconciliation for safety.</p>` },
    { q: `How do you prove an integration did not lose or duplicate data?`, a: `<p>Reconciliation: compare counts, ID sets, or checksums between source and target on a schedule and report missing and unexpected records. Combine it with idempotent writes, dead-letter queues, and monitoring for stuck items so silent failures become visible.</p>` },
    { q: `A customer wants their CRM and billing system to stay in sync. How would you approach the design?`, a: `<p>Clarify requirements first: direction, latency, volume, conflict rules, and system-of-record for each field. Then propose: an adapter per system behind a canonical model; events/webhooks for changes with an idempotent consumer; periodic reconciliation; retries with backoff and a dead-letter queue; monitoring on lag and failure counts; and a plan for initial backfill.</p>` },
  ],

  "integration/rest-clients": [
    { q: `How do you handle rate limiting (HTTP 429)?`, a: `<p>Respect the <code>Retry-After</code> header when present, otherwise use exponential backoff with jitter. Proactively throttle with a token bucket, queue work to smooth bursts, cap total retries, and log throttling so you can request higher limits if needed.</p>` },
    { q: `Which errors do you retry, and what makes a request safe to retry?`, a: `<p>Retry network errors, timeouts, 429 and 5xx. Do not retry 400, 401, 403 or 404, since they will fail the same way. Retries are safe for idempotent operations (GET, PUT, DELETE) or POSTs sent with an idempotency key; otherwise a retry after a timeout can create duplicates.</p>` },
    { q: `Offset vs cursor pagination?`, a: `<p>Offset (<code>page=3</code>) is simple but skips or repeats records when data changes during traversal and gets slow on deep pages. Cursor (a token or last-seen ID) marks a stable position and stays correct under inserts and deletes. Prefer cursors, and wrap them in a generator so callers just iterate.</p>` },
    { q: `What timeout settings should a production client have?`, a: `<p>Always set both a connect timeout and a read timeout (Python's <code>requests</code> has none by default, so a hung server blocks forever). Reuse a session for connection pooling, bound the total time across retries, and log status and duration for each call.</p>` },
  ],

  "integration/authentication": [
    { q: `Compare API keys, OAuth2 and JWTs.`, a: `<p>An API key is a static shared secret: simple but coarse and needs rotation. OAuth2 is an authorisation framework for obtaining scoped, short-lived access tokens (client credentials for machine-to-machine, authorisation code for acting on behalf of a user). A JWT is a signed token format often used as an access token; it can be verified offline but is hard to revoke before expiry.</p>` },
    { q: `Explain the OAuth2 client credentials flow and how you would manage tokens.`, a: `<p>The app authenticates with its client ID and secret to the token endpoint and receives a short-lived access token. Cache it in memory, refresh shortly before <code>expires_in</code> elapses (with a small skew), lock around refresh so concurrent threads do not stampede, and on a 401 refresh once and retry once.</p>` },
    { q: `Where should credentials be stored, and how are they rotated?`, a: `<p>In a secrets manager or environment variables injected at deploy time, never in source control, logs or URLs. Support rotation by reading secrets at runtime, allowing two valid secrets during a switch-over, and scoping each credential narrowly with least privilege.</p>` },
    { q: `What is the difference between a 401 and a 403?`, a: `<p>401 Unauthorized means the request lacks valid authentication (missing, expired or wrong token). 403 Forbidden means the caller is authenticated but not allowed to do this. A 401 may be fixed by refreshing a token; a 403 is a permission problem and retrying will not help.</p>` },
  ],

  "integration/webhooks": [
    { q: `How do you build a secure webhook receiver?`, a: `<p>Verify an HMAC signature computed over the raw request body (and timestamp) with a shared secret, using a constant-time comparison; reject old timestamps to stop replays; serve over HTTPS; then respond 2xx quickly and process asynchronously. Do not trust the payload beyond the verified event, and consider fetching current state from the provider's API.</p>` },
    { q: `How do you handle duplicate and out-of-order deliveries?`, a: `<p>Deliveries are at-least-once, so de-duplicate on the event ID by storing processed IDs durably (a database table with a unique constraint, with a TTL). For ordering, do not assume it: compare event timestamps or version numbers, or re-fetch the latest resource state instead of applying the event blindly.</p>` },
    { q: `Why respond immediately and process later?`, a: `<p>Providers time out and retry when a handler is slow, creating duplicate deliveries and load. Acknowledge with a 2xx after verifying and persisting or enqueuing the event, and let a worker do the real processing with retries and a dead-letter queue.</p>` },
    { q: `What if your receiver is down when events are sent?`, a: `<p>Most providers retry with backoff for a while, but not forever, so you need a recovery path: a reconciliation job or replay endpoint that pulls missed events from the provider's API by time range, and monitoring that alerts on gaps.</p>` },
  ],

  "integration/messaging": [
    { q: `Explain at-most-once, at-least-once and exactly-once delivery. What do you use in practice?`, a: `<p>At-most-once may lose messages; at-least-once never loses but may duplicate; exactly-once is very hard end to end. In practice use at-least-once delivery with idempotent consumers, which yields effectively-once processing.</p>` },
    { q: `What is a dead-letter queue and why is it important?`, a: `<p>A holding queue for messages that failed after the maximum number of attempts (poison messages). It stops one bad message from blocking the queue, preserves the data for inspection, and enables replay after a fix. Alert on its depth.</p>` },
    { q: `Queue vs topic (pub/sub)? How do Kafka, RabbitMQ and SQS differ in broad terms?`, a: `<p>A queue delivers each message to one consumer (work distribution); a topic delivers each message to every subscriber (fan-out). Kafka is a partitioned, replayable log with consumer offsets and per-partition ordering; RabbitMQ is a broker with flexible routing and acknowledgements; SQS is a managed queue (with FIFO variants) that trades features for operational simplicity.</p>` },
    { q: `How do you handle ordering and backpressure?`, a: `<p>Ordering is typically guaranteed only within a partition or message group, so key messages by entity ID to keep one customer's events ordered while scaling out. For backpressure, bound queue sizes, limit consumer concurrency, scale consumers on queue depth, and monitor the age of the oldest message.</p>` },
  ],

  "integration/batch-files": [
    { q: `How would you design a robust CSV import?`, a: `<p>Stream the file, validate each row, load valid rows, and write rejects (line number, reason, original row) to a separate file instead of failing the whole batch. Track file name, checksum, and row counts in and out, and make the load idempotent with an upsert on a natural key so reruns are safe.</p>` },
    { q: `How do you make a batch load idempotent and restartable?`, a: `<p>Upsert on a unique key (<code>INSERT ... ON CONFLICT DO UPDATE</code>), record a checkpoint or processed-file marker, commit in chunks, and keep each run's input immutable. A rerun then converges to the same final state.</p>` },
    { q: `What problems come from encodings and spreadsheet exports?`, a: `<p>Wrong encodings (cp1252 read as UTF-8), BOMs, different delimiters and quoting, embedded newlines, dates in ambiguous formats, and identifiers with leading zeros lost when treated as numbers. Agree on the format with the customer, decode explicitly, and treat IDs as strings.</p>` },
    { q: `How do you avoid reading a file that is still being written?`, a: `<p>Have the producer write to a temporary name then atomically rename, or drop a completion marker file, or verify a stable size/checksum before processing. Then move processed files to an archive folder.</p>` },
  ],

  "integration/legacy-systems": [
    { q: `How would you integrate with a SOAP service?`, a: `<p>Obtain the WSDL and generate a client (for example with <code>zeep</code>), or build the XML envelope directly. Handle XML namespaces correctly, check for <code>Fault</code> elements even on HTTP 200, apply timeouts and retries, and wrap the client in an adapter that exposes your own types and errors.</p>` },
    { q: `What is an anti-corruption layer and the strangler pattern?`, a: `<p>An anti-corruption layer translates between a legacy model and your own so its quirks do not leak into your domain. The strangler pattern incrementally replaces a legacy system by routing functionality to new components one slice at a time until the old system can be retired.</p>` },
    { q: `How do you handle fixed-width or mainframe-style files and odd encodings?`, a: `<p>Use the record layout spec to slice by column positions, validate record length, decode with the specified encoding (cp1252, EBCDIC variants), and convert types explicitly (for example zero-padded cents to decimals). Test with realistic data including accented characters.</p>` },
    { q: `What are the risks of reading a customer's database directly?`, a: `<p>You couple to a schema they may change without notice, can affect their performance, and may breach their security policies. Prefer a read-only account on a replica or a view, parameterised queries, change data capture where available, and never write into their tables unless explicitly agreed.</p>` },
  ],

  "integration/resilience": [
    { q: `How do you design an idempotency key?`, a: `<p>The key should identify the intended operation, such as an order or request ID, not each attempt, so retries reuse it. The server stores the result keyed by it (with an expiry) and returns the stored result on repeats. Combine it with a unique constraint so concurrent duplicates cannot both succeed.</p>` },
    { q: `Why use exponential backoff with jitter?`, a: `<p>Backoff gives a struggling service time to recover; jitter (randomness) prevents many clients from retrying in lockstep and causing a thundering herd. Cap the delay and the number of attempts so a blip does not become an outage.</p>` },
    { q: `Describe the circuit breaker pattern and its states.`, a: `<p>Closed: calls pass through and failures are counted. Open: after a failure threshold, calls fail fast for a cooldown. Half-open: after the cooldown, a limited probe is allowed; success closes the circuit, failure reopens it. It protects the dependency and keeps your own latency low.</p>` },
    { q: `What other techniques keep an integration stable under partial failure?`, a: `<p>Timeouts on every call, bulkheads (separate pools so one slow dependency cannot exhaust all threads), retry budgets to avoid retry storms across layers, graceful degradation with cached or partial data, and dead-letter queues for work that cannot be completed.</p>` },
  ],

  "integration/data-mapping": [
    { q: `What is a canonical data model, and what are its pros and cons?`, a: `<p>A single internal representation that each external system is mapped to and from. It avoids N-by-N pairwise mappings and isolates vendor quirks, but it takes design effort, can become a lowest-common-denominator schema, and must be versioned as needs change.</p>` },
    { q: `How do you detect and handle schema drift?`, a: `<p>Compare incoming records with the expected schema: missing required fields fail the record and alert; unknown new fields are logged and counted. Keep the raw payload for re-mapping, version your mappers, and track drift metrics so you notice vendor changes before customers do.</p>` },
    { q: `Which data-mapping pitfalls do you watch for?`, a: `<p>Money as floats (use integer minor units or Decimal), naive or mixed time zones (normalise to UTC), null versus empty versus missing, enums and code lists that differ between systems, ID formats and leading zeros, and character encodings.</p>` },
    { q: `Explain an anti-corruption layer.`, a: `<p>A boundary made of adapters and mappers that translates an external system's model into your domain model, so business logic never sees vendor field names or units. Replacing a vendor then means rewriting one layer instead of the whole codebase.</p>` },
  ],

  "integration/security-observability": [
    { q: `How do you manage secrets in an integration?`, a: `<p>Use a secrets manager or injected environment variables, never source control, logs, or URLs. Grant the minimum scopes, use separate credentials per environment and per customer, support rotation, and audit access. Scan repositories and rotate anything that leaked.</p>` },
    { q: `What should you log, and what must you not log?`, a: `<p>Log structured events with a correlation ID, the operation, the target system, outcome, latency, and record identifiers useful for support. Do not log secrets, tokens, full request bodies, or personal data; redact centrally with a logging filter so it cannot be forgotten.</p>` },
    { q: `Which metrics and alerts matter for an integration?`, a: `<p>Success and failure counts, latency, retry and dead-letter counts, queue depth, and above all the age of the oldest unprocessed item, which catches stalled pipelines that produce no errors. Alert on symptoms customers feel (data late, error rate rising), not just CPU.</p>` },
    { q: `What will a customer security review typically ask?`, a: `<p>How data is encrypted in transit and at rest, where secrets live, what permissions you need and why (least privilege), what personal data you store and for how long, audit logging, incident response, and compliance posture such as SOC 2 or HIPAA. Prepare clear, honest answers and diagrams.</p>` },
  ],
});
