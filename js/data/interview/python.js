// Interview questions for the Python topic. Key = "topicId/pageId"; a = HTML.
window.INTERVIEW = window.INTERVIEW || {};
Object.assign(window.INTERVIEW, {
  "python/environment": [
    { q: `What is a virtual environment and why would you use one?`, a: `<p>A virtual environment is an isolated directory containing its own Python interpreter link and its own <code>site-packages</code>. Each project installs its dependencies there, so project A can use <code>requests 2.25</code> while project B uses <code>2.31</code> without conflict, and nothing is installed into the system Python. It also makes environments reproducible: you can delete and rebuild it from a dependency file.</p>` },
    { q: `How do you reproduce the same environment on another machine or in CI?`, a: `<p>Record dependencies in a file and install from it: <code>pip freeze &gt; requirements.txt</code>, then <code>pip install -r requirements.txt</code> in a fresh venv. Pin exact versions (<code>==</code>) for reproducibility. For transitive dependencies, use a lock file (pip-tools, Poetry, or uv) so builds are deterministic rather than "latest at the time".</p>` },
    { q: `What is the difference between <code>python app.py</code> and <code>python -m package.module</code>?`, a: `<p>Running a file puts that file's directory on <code>sys.path</code> and treats it as a standalone script. <code>-m</code> runs a module by its import path with the package context intact, so relative imports (<code>from .models import X</code>) work. Once code is organised into packages, prefer <code>-m</code>.</p>` },
    { q: `As a Java developer, what would you compare <code>pip</code>, <code>requirements.txt</code> and a venv to?`, a: `<p>Roughly: pip is Maven/Gradle's dependency downloader, <code>requirements.txt</code> is the <code>pom.xml</code>/<code>build.gradle</code> dependency list (without a build lifecycle), and a venv is a per-project classpath and JDK-like sandbox. Unlike Java, Python installs are global by default, which is exactly why venvs matter.</p>` },
  ],

  "python/syntax-variables": [
    { q: `Is Python statically or dynamically typed? Strongly or weakly?`, a: `<p>Dynamically and strongly typed. Types belong to objects and are checked at runtime, so a name can be rebound to a different type. It is strong because there is no implicit coercion between unrelated types: <code>"5" + 5</code> raises <code>TypeError</code>. Optional type hints add static checking through tools like mypy, but the interpreter does not enforce them.</p>` },
    { q: `What happens when you write <code>b = a</code> where <code>a</code> is a list?`, a: `<p>Nothing is copied. <code>b</code> becomes a second name bound to the same list object, so mutating through either name is visible via both. Make a shallow copy with <code>a[:]</code>, <code>list(a)</code> or <code>a.copy()</code>, and use <code>copy.deepcopy</code> when nested mutable objects must also be duplicated.</p>` },
    { q: `Explain the difference between <code>==</code> and <code>is</code>.`, a: `<p><code>==</code> compares values (it calls <code>__eq__</code>), like Java's <code>.equals()</code>. <code>is</code> compares identity, whether two names refer to the same object, like Java's <code>==</code> on references. Use <code>is</code> for singletons such as <code>None</code> (<code>x is None</code>) and <code>==</code> for everything else.</p>` },
    { q: `What are mutable and immutable types, and why does it matter?`, a: `<p>Immutable objects (<code>int</code>, <code>str</code>, <code>tuple</code>, <code>frozenset</code>, <code>bytes</code>) cannot change after creation; mutable ones (<code>list</code>, <code>dict</code>, <code>set</code>) can. It matters for hashing (only hashable, effectively immutable objects can be dict keys or set members), for aliasing bugs, for safe sharing across threads, and for default arguments.</p>` },
  ],

  "python/numbers-strings": [
    { q: `Can a Python integer overflow? What about floats?`, a: `<p>Integers have arbitrary precision, so they grow as needed and never overflow (limited only by memory), unlike Java's fixed-width <code>int</code>/<code>long</code>. Floats are IEEE 754 doubles, so they still have rounding error (<code>0.1 + 0.2 != 0.3</code>). Use <code>decimal.Decimal</code> or integer cents for money.</p>` },
    { q: `Why is building a string with <code>+=</code> in a loop discouraged?`, a: `<p>Strings are immutable, so each concatenation can allocate and copy the whole string so far, making the loop O(n&sup2;) in general (CPython has an optimisation for some cases, but it is not guaranteed). Collect the pieces in a list and use <code>"".join(parts)</code>, which computes the final size once and copies each piece once.</p>` },
    { q: `What is the difference between <code>str</code> and <code>bytes</code>?`, a: `<p><code>str</code> is a sequence of Unicode code points; <code>bytes</code> is a sequence of raw 8-bit values. You convert with <code>s.encode("utf-8")</code> and <code>b.decode("utf-8")</code>. Bugs appear when the encoding is assumed rather than specified, which is common with legacy files (cp1252) and network data, so always be explicit.</p>` },
    { q: `Compare f-strings, <code>str.format()</code> and <code>%</code> formatting.`, a: `<p>f-strings (3.6+) evaluate expressions inline (<code>f"{name} is {age}"</code>) and are the most readable and usually the fastest. <code>.format()</code> is older but useful when the template is separate from the data. <code>%</code> formatting is legacy. Note that f-strings evaluate immediately, so do not use them for logging calls that should be lazy; pass arguments to the logger instead.</p>` },
  ],

  "python/lists-tuples": [
    { q: `What are the differences between a list and a tuple, and when would you choose a tuple?`, a: `<p>Lists are mutable and typically homogeneous collections; tuples are immutable and often used as fixed-size records. Choose a tuple for values that should not change, for multiple return values, and when you need a hashable key (for example <code>(x, y)</code> as a dict key). Tuples are also slightly smaller and faster to create.</p>` },
    { q: `What is the time complexity of common list operations?`, a: `<p>Index and <code>append</code>/<code>pop()</code> at the end are O(1) (append is amortised). <code>insert(0, x)</code>, <code>pop(0)</code> and <code>x in list</code> are O(n). Sorting is O(n log n). For queue behaviour use <code>collections.deque</code>, which gives O(1) at both ends.</p>` },
    { q: `What is wrong with <code>grid = [[0] * 3] * 3</code>?`, a: `<p>The outer multiplication repeats a reference to one inner list three times, so all rows are the same object and <code>grid[0][0] = 1</code> changes every row. Build it with a comprehension so each row is a fresh list: <code>[[0] * 3 for _ in range(3)]</code>.</p>` },
    { q: `How is a Python list implemented, and why is append amortised O(1)?`, a: `<p>It is a dynamic array of object pointers. When it fills, CPython over-allocates (grows by a proportional factor) so that resizes happen rarely; the occasional O(n) copy is spread over many appends, giving O(1) amortised cost.</p>` },
  ],

  "python/dicts-sets": [
    { q: `How does a dict work and what is its lookup complexity?`, a: `<p>It is a hash table: the key's hash selects a slot, with collisions resolved by probing. Lookup, insert and delete are O(1) on average and O(n) in the pathological worst case. Since Python 3.7 dicts also preserve insertion order as a language guarantee.</p>` },
    { q: `Which objects can be dictionary keys or set members?`, a: `<p>Hashable ones: objects with a stable <code>__hash__</code> consistent with <code>__eq__</code>. Immutable built-ins (str, int, tuple of hashables, frozenset) qualify; lists, dicts and sets do not because their contents can change and would corrupt the hash table.</p>` },
    { q: `What are the ways to handle a missing key?`, a: `<ul><li><code>d.get(key, default)</code> for a read with a fallback</li><li><code>key in d</code> before access (LBYL)</li><li><code>try/except KeyError</code> (EAFP)</li><li><code>d.setdefault(key, default)</code> to insert if absent</li><li><code>collections.defaultdict</code> to auto-create values</li></ul>` },
    { q: `How would you remove duplicates from a list while preserving order?`, a: `<p><code>list(dict.fromkeys(items))</code>: dict keys are unique and ordered by insertion. <code>list(set(items))</code> also dedupes but loses order. For unhashable items you need a different approach, such as comparing with a list of seen values (O(n&sup2;)) or a key function.</p>` },
  ],

  "python/control-functions": [
    { q: `What do <code>*args</code> and <code>**kwargs</code> do?`, a: `<p><code>*args</code> collects extra positional arguments into a tuple; <code>**kwargs</code> collects extra keyword arguments into a dict. At a call site the same operators unpack an iterable or mapping into arguments. They let one function accept flexible signatures, which is how Python compensates for having no method overloading.</p>` },
    { q: `What is the mutable default argument problem?`, a: `<p>Default values are evaluated once, when the function is defined, so a mutable default such as <code>def f(x, cache=[])</code> is shared across every call. Use <code>None</code> as the default and create the object inside:</p><pre><code>def f(x, cache=None):
    if cache is None:
        cache = []</code></pre>` },
    { q: `Is Python pass-by-value or pass-by-reference?`, a: `<p>Neither exactly: it is "pass by object reference" (call by assignment). The parameter name is bound to the same object the caller passed. Mutating that object (for example appending to a list) is visible to the caller; rebinding the parameter to a new object is not.</p>` },
    { q: `Python has no method overloading. How do you handle multiple call shapes?`, a: `<p>Use default arguments, keyword arguments, <code>*args/**kwargs</code>, or <code>functools.singledispatch</code> to dispatch on the type of the first argument. Defining two functions with the same name simply replaces the first. For alternative constructors, use <code>@classmethod</code> factories.</p>` },
  ],

  "python/comprehensions": [
    { q: `What is the difference between a list comprehension and a generator expression?`, a: `<p><code>[x*x for x in data]</code> builds the whole list in memory immediately. <code>(x*x for x in data)</code> creates a lazy generator that yields one value at a time and can be consumed once. Use the generator form for large or streaming data and when feeding <code>sum()</code>, <code>any()</code> or <code>max()</code>.</p>` },
    { q: `Are comprehensions faster than for-loops?`, a: `<p>Slightly, by a constant factor, because they avoid repeated method lookups for <code>append</code>. They do not change the complexity class. The real benefit is that they express "build a new collection from another" declaratively. Prefer clarity over micro-optimisation.</p>` },
    { q: `Flatten a list of lists with a comprehension. In what order do the <code>for</code> clauses read?`, a: `<pre><code>flat = [x for row in grid for x in row]</code></pre><p>The clauses read in the same order as nested loops: outer loop first, inner loop second.</p>` },
    { q: `When should you avoid a comprehension?`, a: `<p>When it has side effects (printing, mutating external state), when the logic needs several statements, or when it nests three or more levels and becomes hard to scan. In those cases a plain loop is more readable and easier to debug.</p>` },
  ],

  "python/oop": [
    { q: `What is <code>self</code>, and how do <code>__init__</code> and <code>__new__</code> differ?`, a: `<p><code>self</code> is the explicit first parameter that refers to the instance (Java's implicit <code>this</code>). <code>__new__</code> creates the object and returns it; <code>__init__</code> then initialises it. You rarely override <code>__new__</code>, mainly for singletons or immutable subclasses.</p>` },
    { q: `How does Python do encapsulation without <code>private</code>?`, a: `<p>By convention: a leading underscore (<code>_x</code>) means "internal". A double underscore (<code>__x</code>) triggers name mangling to <code>_Class__x</code>, which avoids accidental subclass clashes but is not real protection. Use <code>@property</code> to expose controlled access while keeping the attribute syntax.</p>` },
    { q: `What is the method resolution order (MRO)?`, a: `<p>The order in which Python searches base classes for an attribute, computed with C3 linearisation, which keeps a consistent, monotonic order under multiple inheritance. Inspect it with <code>Class.__mro__</code>. <code>super()</code> follows the MRO, not just the direct parent, which is why cooperative multiple inheritance works.</p>` },
    { q: `Explain instance methods, <code>@classmethod</code> and <code>@staticmethod</code>.`, a: `<ul><li><strong>Instance method</strong>: receives <code>self</code>, works on one object.</li><li><strong><code>@classmethod</code></strong>: receives <code>cls</code>; used for alternative constructors and works correctly with subclasses.</li><li><strong><code>@staticmethod</code></strong>: receives nothing; just a function namespaced inside the class.</li></ul>` },
    { q: `Why does defining <code>__eq__</code> make instances unhashable, and how do you fix it?`, a: `<p>Python sets <code>__hash__ = None</code> when you define <code>__eq__</code> without <code>__hash__</code>, because equal objects must hash equally. If instances are effectively immutable, define <code>__hash__</code> over the same fields, or use <code>@dataclass(frozen=True)</code> which generates both.</p>` },
  ],

  "python/exceptions": [
    { q: `What are EAFP and LBYL, and which does Python prefer?`, a: `<p>LBYL ("look before you leap") checks preconditions first; EAFP ("easier to ask forgiveness than permission") tries the operation and handles the exception. Python favours EAFP: it avoids races between check and use, avoids double lookups, and is usually cleaner when failure is the rare case.</p>` },
    { q: `What do <code>else</code> and <code>finally</code> do in try/except, and does <code>finally</code> run after <code>return</code>?`, a: `<p><code>else</code> runs only if the try block raised nothing, keeping the happy path out of the guarded block. <code>finally</code> always runs, including when the try block returns, breaks, or raises, which makes it the place for cleanup (though <code>with</code> is usually better).</p>` },
    { q: `Why is a bare <code>except:</code> a problem, and what is exception chaining?`, a: `<p>A bare except also catches <code>KeyboardInterrupt</code> and <code>SystemExit</code> and hides real bugs; catch specific types. Chaining preserves the cause when you translate errors: <code>raise ServiceError("sync failed") from exc</code> keeps the original traceback attached, which is important when wrapping vendor exceptions.</p>` },
    { q: `Java has checked exceptions. What is the Python equivalent?`, a: `<p>There is none: all exceptions are unchecked and nothing forces callers to handle them. The trade-off is that you must document what a function can raise and be deliberate about where errors are caught, typically at boundaries where you can act on them.</p>` },
  ],

  "python/modules-packages": [
    { q: `What does <code>if __name__ == "__main__":</code> do?`, a: `<p>When a file is run directly, <code>__name__</code> is <code>"__main__"</code>; when it is imported, it is the module name. The guard lets one file be both an importable library and a runnable script, without running the script code on import.</p>` },
    { q: `How does the import system find and cache modules?`, a: `<p>It checks <code>sys.modules</code> first (imports are cached, so a module body runs once per process), then searches <code>sys.path</code> (script directory, <code>PYTHONPATH</code>, site-packages). This caching is also why module-level objects act like singletons.</p>` },
    { q: `What are circular imports and how do you fix them?`, a: `<p>Module A imports B while B imports A, so one sees a half-initialised module. Fixes: move the shared code to a third module, import inside the function that needs it, or restructure so dependencies point one way. Frequent circular imports usually signal a design smell.</p>` },
    { q: `What is the difference between absolute and relative imports?`, a: `<p>Absolute imports name the full path from the top-level package (<code>from myapp.models import User</code>); relative imports are anchored to the current package (<code>from .models import User</code>). Relative imports only work inside a package, so running the file directly breaks them; use <code>python -m</code>.</p>` },
  ],

  "python/stdlib-dsa": [
    { q: `Why use <code>collections.deque</code> instead of a list for a queue?`, a: `<p><code>list.pop(0)</code> is O(n) because every element shifts. <code>deque</code> supports O(1) <code>append</code>, <code>appendleft</code>, <code>pop</code> and <code>popleft</code>. The trade-off is O(n) random indexing in the middle.</p>` },
    { q: `When would you use <code>Counter</code> and <code>defaultdict</code>?`, a: `<p><code>Counter(iterable)</code> tallies frequencies and offers <code>.most_common(k)</code>. <code>defaultdict(list)</code> (or <code>int</code>, <code>set</code>) removes the "if key not in d" boilerplate when grouping or accumulating. Beware that merely reading a missing key inserts it.</p>` },
    { q: `Python's <code>heapq</code> is a min-heap. How do you get a max-heap and what are the complexities?`, a: `<p>Push negated values (or tuples with a negated priority) and negate on pop. <code>heappush</code>/<code>heappop</code> are O(log n), <code>heapify</code> is O(n), and <code>nlargest(k, xs)</code> is roughly O(n log k).</p>` },
    { q: `How does <code>functools.lru_cache</code> work and what are its limits?`, a: `<p>It memoises results in a dict keyed by the call arguments, with least-recently-used eviction if <code>maxsize</code> is set. Arguments must be hashable, and the cache lives for the process lifetime unless <code>cache_clear()</code> is called. It turns exponential recursive Fibonacci into O(n).</p>` },
  ],

  "python/functional": [
    { q: `What is a closure and what does <code>nonlocal</code> do?`, a: `<p>A closure is a function that keeps access to variables from its enclosing scope after that scope has returned. Reading such variables just works; to <em>reassign</em> one from the inner function you must declare it <code>nonlocal</code>, otherwise assignment creates a new local variable.</p>` },
    { q: `Write a decorator that logs how long a function takes. Why use <code>functools.wraps</code>?`, a: `<pre><code>import time
from functools import wraps

def timed(fn):
    @wraps(fn)
    def wrapper(*args, **kwargs):
        t0 = time.perf_counter()
        try:
            return fn(*args, **kwargs)
        finally:
            print(fn.__name__, time.perf_counter() - t0)
    return wrapper</code></pre><p><code>wraps</code> copies <code>__name__</code>, <code>__doc__</code> and other metadata onto the wrapper so debugging, docs and introspection still show the original function.</p>` },
    { q: `How do you write a decorator that takes arguments?`, a: `<p>Add one more layer: a decorator factory that receives the arguments and returns the actual decorator, which returns the wrapper. <code>@retry(times=3)</code> means <code>retry(3)</code> is evaluated first and its result is applied to the function.</p>` },
    { q: `What is the late-binding closure gotcha?`, a: `<p>Closures capture variables, not values. <code>fs = [lambda: i for i in range(3)]</code> makes all three return <code>2</code>, because <code>i</code> is looked up when called. Bind the current value with a default argument: <code>lambda i=i: i</code>, or use <code>functools.partial</code>.</p>` },
  ],

  "python/iterators-generators": [
    { q: `Explain iterable, iterator and generator.`, a: `<p>An iterable can produce an iterator (<code>__iter__</code>), such as a list. An iterator has <code>__next__</code> and raises <code>StopIteration</code> when exhausted. A generator is an iterator created by a function using <code>yield</code> (or a generator expression); Python saves its local state between values. All generators are iterators, but not all iterators are generators.</p>` },
    { q: `When would you use a generator instead of returning a list?`, a: `<p>When the sequence is large, unbounded, or expensive, and you only need to process items one at a time: reading a big file line by line, paginating an API, or streaming records. It keeps memory O(1), but a generator can be consumed only once and does not support indexing.</p>` },
    { q: `How do you implement a context manager, and what does returning <code>True</code> from <code>__exit__</code> do?`, a: `<p>Either a class with <code>__enter__</code>/<code>__exit__</code>, or a generator function decorated with <code>@contextlib.contextmanager</code> (code before <code>yield</code> is setup, after is cleanup). <code>__exit__</code> runs even if the block raised; returning <code>True</code> suppresses that exception, so do it only deliberately.</p>` },
    { q: `What does <code>yield from</code> do?`, a: `<p>It delegates to a sub-iterator, yielding each of its values and propagating <code>send</code>/<code>throw</code>/return values. <code>yield from items</code> is the tidy replacement for <code>for x in items: yield x</code>, and is what made composing generators (and early coroutines) practical.</p>` },
  ],

  "python/concurrency-memory": [
    { q: `What is the GIL, and when do threads still help?`, a: `<p>The Global Interpreter Lock is a mutex in CPython that lets only one thread execute Python bytecode at a time, which simplifies memory management but prevents CPU-bound threads from running in parallel. It is released during blocking I/O, so threads help I/O-bound work (network, disk). CPython 3.13 added an experimental free-threaded build, so check the current status for your version.</p>` },
    { q: `When do you choose threading, multiprocessing or asyncio?`, a: `<ul><li><strong>threading</strong>: moderate I/O-bound concurrency, or blocking libraries.</li><li><strong>multiprocessing</strong> (or <code>ProcessPoolExecutor</code>): CPU-bound work; separate processes have separate GILs, at the cost of pickling and memory.</li><li><strong>asyncio</strong>: very many concurrent I/O tasks on one thread with cooperative <code>await</code>; needs async-aware libraries.</li></ul>` },
    { q: `How does CPython manage memory?`, a: `<p>Primarily reference counting: an object is freed the moment its count reaches zero. A cyclic garbage collector periodically finds and frees reference cycles that counting alone cannot. Weak references (<code>weakref</code>) let you refer to an object without keeping it alive.</p>` },
    { q: `Why can <code>x is y</code> be True for <code>200</code> but False for <code>1000</code>?`, a: `<p>CPython caches small integers (-5 to 256) as singletons, and may intern some strings. That is an implementation detail, not a language guarantee, so it makes <code>is</code> look like value equality by accident. Always use <code>==</code> for values.</p>` },
  ],
});
